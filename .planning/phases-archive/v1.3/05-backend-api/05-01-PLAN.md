---
phase: 05-backend-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java
  - springboot/src/main/java/cn/coderstory/springboot/service/UserService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/impl/UserServiceImpl.java
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05

must_haves:
  truths:
    - "GET /api/users 返回分页用户列表，支持 username 模糊筛选"
    - "GET /api/users 支持 phone 模糊筛选"
    - "GET /api/users 支持 enabled 精确筛选"
    - "GET /api/users 返回 code:200, message:success, data{records,total,size,current,pages}"
    - "GET /api/users/{id} 返回单个用户基本信息，无角色信息"
    - "POST /api/users 可创建用户，包含 phone 字段"
    - "PUT /api/users/{id} 可更新用户信息"
    - "DELETE /api/users/{id} 可删除用户"
  artifacts:
    - path: "springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java"
      provides: "REST API endpoints"
      contains: "@GetMapping, @PostMapping, @PutMapping, @DeleteMapping"
    - path: "springboot/src/main/java/cn/coderstory/springboot/service/UserService.java"
      provides: "UserService interface"
      contains: "getUserPage, getUserById, saveUser, updateUser, deleteUser"
    - path: "springboot/src/main/java/cn/coderstory/springboot/service/impl/UserServiceImpl.java"
      provides: "UserService implementation"
      contains: "LambdaQueryWrapper with phone like filter"
  key_links:
    - from: "UserController.getUserPage"
      to: "UserService.getUserPage"
      via: "phone parameter"
      pattern: "phone.*like"
    - from: "UserController.createUser"
      to: "UserService.saveUser"
      via: "phone field in request"
      pattern: "user.setPhone"
---

<objective>
实现用户管理模块的 RESTful API 后端实现，包括：
1. 扩展 GET /api/users 分页查询支持 phone 筛选
2. 确保 POST /api/users 创建用户时包含 phone 字段
3. 重命名 getUserWithRole 为 getUserById（语义更准确）

Purpose: 为 Phase 6/7/8 提供数据接口
Output: 完整的用户 CRUD API
</objective>

<context>
@.planning/phases/05-backend-api/05-CONTEXT.md
@.planning/REQUIREMENTS.md

# Existing Code Context

From springboot/src/main/java/cn/coderstory/springboot/entity/User.java:
```java
@Data
@TableName("sys_user")
public class User {
    @TableId(type = IdType.AUTO)
    private Long id;
    private String username;
    private String password;
    private String name;
    private Integer gender;
    private String avatar;
    private String phone;           // 需添加到 createUser
    private Long roleId;
    private String email;
    private String department;
    private String position;
    private Integer enabled;
    @TableLogic
    private Integer deleted;
    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;
    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;
}
```

From springboot/src/main/java/cn/coderstory/springboot/service/UserService.java:
```java
IPage<User> getUserPage(Page<User> page, String username, String name, String department, Integer enabled);
User getUserWithRole(Long id);  // 重命名为 getUserById
boolean saveUser(User user, String rawPassword);
boolean updateUser(User user);
boolean deleteUser(Long id);
```

Current getUserPage in UserServiceImpl uses:
```java
wrapper.like(User::getUsername, username);
wrapper.like(User::getName, name);
wrapper.eq(User::getDepartment, department);
wrapper.eq(User::getEnabled, enabled);
wrapper.orderByDesc(User::getCreateTime);
```
</context>

<tasks>

<task type="auto">
  <name>task 1: Add phone fuzzy filter to GET /api/users</name>
  <files>springboot/src/main/java/cn/coderstory/springboot/service/UserService.java</files>
  <files>springboot/src/main/java/cn/coderstory/springboot/service/impl/UserServiceImpl.java</files>
  <files>springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java</files>
  <read_first>
    - springboot/src/main/java/cn/coderstory/springboot/service/UserService.java
    - springboot/src/main/java/cn/coderstory/springboot/service/impl/UserServiceImpl.java
    - springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java
  </read_first>
  <action>
    **UserService.java** - 添加 phone 参数到方法签名:
    ```java
    IPage<User> getUserPage(Page<User> page, String username, String name, String department, Integer enabled, String phone);
    ```

    **UserServiceImpl.java** - 在 getUserPage 方法中添加 phone 筛选:
    在 `if (enabled != null) { wrapper.eq(User::getEnabled, enabled); }` 后添加:
    ```java
    if (phone != null && !phone.isEmpty()) {
        wrapper.like(User::getPhone, phone);
    }
    ```

    **UserController.java** - 在 getUserPage endpoint 添加 phone 参数:
    ```java
    @GetMapping
    public ResponseEntity<Map<String, Object>> getUserPage(
            @RequestParam(required = false) String username,
            @RequestParam(required = false) String name,
            @RequestParam(required = false) String department,
            @RequestParam(required = false) Integer enabled,
            @RequestParam(required = false) String phone,  // 新增
            @RequestParam(defaultValue = "1") Integer page,
            @RequestParam(defaultValue = "20") Integer size) {
        ...
        IPage<User> result = userService.getUserPage(pageParam, username, name, department, enabled, phone);  // 添加 phone
    ```
  </action>
  <acceptance_criteria>
    - UserService.java 方法签名包含 phone 参数
    - UserServiceImpl.getUserPage() 包含 `wrapper.like(User::getPhone, phone)`
    - UserController.getUserPage() 接收 phone 请求参数并传递给 service
  </acceptance_criteria>
  <verify>
    <automated>grep -n "String phone" springboot/src/main/java/cn/coderstory/springboot/service/UserService.java</automated>
    <automated>grep -n "User::getPhone, phone" springboot/src/main/java/cn/coderstory/springboot/service/impl/UserServiceImpl.java</automated>
    <automated>grep -n "required = false) String phone" springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java</automated>
  </verify>
  <done>GET /api/users 支持 phone 模糊筛选参数</done>
</task>

<task type="auto">
  <name>task 2: Fix createUser to handle phone field</name>
  <files>springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java</files>
  <read_first>
    - springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java
  </read_first>
  <action>
    **UserController.java** - 在 createUser 方法中添加 phone 设置:

    找到:
    ```java
    user.setAvatar((String) request.get("avatar"));
    
    String password = (String) request.get("password");
    ```

    在 `user.setAvatar` 后添加:
    ```java
    user.setPhone((String) request.get("phone"));
    ```

    同时确保 phone 可以为 null 的情况下 setPhone 不会出错（phone 在 User 实体中是 String 类型，本就是 nullable）
  </action>
  <acceptance_criteria>
    - UserController.createUser() 调用 user.setPhone((String) request.get("phone"))
    - request body 可以包含 phone 字段
  </acceptance_criteria>
  <verify>
    <automated>grep -n "user.setPhone" springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java</automated>
  </verify>
  <done>POST /api/users 创建用户时正确处理 phone 字段</done>
</task>

<task type="auto">
  <name>task 3: Rename getUserWithRole to getUserById</name>
  <files>springboot/src/main/java/cn/coderstory/springboot/service/UserService.java</files>
  <files>springboot/src/main/java/cn/coderstory/springboot/service/impl/UserServiceImpl.java</files>
  <files>springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java</files>
  <read_first>
    - springboot/src/main/java/cn/coderstory/springboot/service/UserService.java
    - springboot/src/main/java/cn/coderstory/springboot/service/impl/UserServiceImpl.java
    - springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java
  </read_first>
  <action>
    **UserService.java** - 将方法名 `getUserWithRole` 改为 `getUserById`:
    ```java
    User getUserById(Long id);  // 改名
    ```

    **UserServiceImpl.java** - 将方法名 `getUserWithRole` 改为 `getUserById`:
    ```java
    @Override
    public User getUserById(Long id) {  // 改名
        return userMapper.selectById(id);
    }
    ```

    **UserController.java** - 同步更新调用:
    ```java
    User user = userService.getUserById(id);  // getUserWithRole -> getUserById
    ```
  </action>
  <acceptance_criteria>
    - UserService 接口方法名为 getUserById
    - UserServiceImpl 实现方法名为 getUserById
    - UserController 调用 getUserById
    - grep -n "getUserWithRole" 返回空（无残留）
  </acceptance_criteria>
  <verify>
    <automated>grep -n "getUserWithRole" springboot/src/main/java/cn/coderstory/springboot/service/</automated>
    <automated>grep -n "getUserById" springboot/src/main/java/cn/coderstory/springboot/service/UserService.java</automated>
  </verify>
  <done>方法命名语义准确，getUserById 正确返回用户基本信息</done>
</task>

</tasks>

<verification>
编译验证:
```bash
cd springboot; mvn compile -q
```

API 功能验证（需要启动后端）:
1. GET /api/users?phone=138 应该返回 phone 包含 138 的用户
2. POST /api/users 带 phone 字段创建用户
3. GET /api/users/{id} 返回该用户基本信息
</verification>

<success_criteria>
1. GET /api/users 支持 phone 模糊筛选参数
2. POST /api/users 创建用户时正确处理 phone 字段
3. getUserWithRole 方法已重命名为 getUserById
4. 所有代码编译通过
5. API 符合 RESTful 规范，返回格式为 {code:200, message:"success", data:{...}}
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-api/05-01-SUMMARY.md`
</output>
