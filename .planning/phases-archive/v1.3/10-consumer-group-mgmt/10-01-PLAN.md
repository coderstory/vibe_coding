---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app-vue/src/api/rocketmq.ts
  - app-vue/src/views/rocketmq/ConsumerGroupList.vue
  - app-vue/src/views/rocketmq/ConsumerGroupDetail.vue
  - app-vue/src/views/rocketmq/ResetOffsetDialog.vue
  - springboot/src/main/java/cn/coderstory/springboot/controller/RocketMQController.java
  - springboot/src/main/java/cn/coderstory/springboot/service/RocketMQAdminService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/impl/RocketMQAdminServiceImpl.java
autonomous: true
requirements:
  - CONS-01
  - CONS-02
  - CONS-03

must_haves:
  truths:
    - 用户可以在表格中看到所有 Consumer Group 的名称、类型、状态
    - 用户可以点击 Group 查看详细信息（消费进度、订阅关系、位点）
    - 用户可以重置消费位点（按时间戳，仅 CLUSTERING 类型）
  artifacts:
    - path: "app-vue/src/api/rocketmq.ts"
      provides: "Consumer Group API 调用封装"
      exports: "getConsumerGroupList, getConsumerGroupDetail, resetConsumerOffset"
    - path: "app-vue/src/views/rocketmq/ConsumerGroupList.vue"
      provides: "Consumer Group 列表页面组件"
      contains: "el-table, 搜索表单, 状态标签"
    - path: "app-vue/src/views/rocketmq/ConsumerGroupDetail.vue"
      provides: "Consumer Group 详情弹窗组件"
      contains: "el-tabs (消费进度/订阅关系/位点信息)"
    - path: "app-vue/src/views/rocketmq/ResetOffsetDialog.vue"
      provides: "位点重置弹窗组件"
      contains: "el-select (Topic), el-date-picker"
    - path: "springboot/src/main/java/cn/coderstory/springboot/controller/RocketMQController.java"
      provides: "Consumer Group REST API 控制器"
      exports: "GET/POST /api/rocketmq/consumer-groups"
    - path: "springboot/src/main/java/cn/coderstory/springboot/service/RocketMQAdminService.java"
      provides: "RocketMQ Admin 服务接口 (扩展)"
---

<objective>
实现 Consumer Group 管理功能：列表查看、详情查看、时间戳位点重置。
</objective>

<context>
## 已锁定的实现决策

### API 端点
- GET /api/rocketmq/consumer-groups — 列表查询
- GET /api/rocketmq/consumer-groups/{group} — Group 详情
- POST /api/rocketmq/consumer-groups/{group}/reset-offset — 重置位点

### Consumer Group 数据结构
| 字段 | 类型 | 描述 |
|------|------|------|
| group | String | Consumer Group 名称 |
| groupType | String | BROADCASTING(广播) / CLUSTERING(集群) |
| status | String | OK / REBALANCE_NOT_INIT / OFFLINE |
| consumerCount | Integer | 在线消费者数量 |
| accumulatedDiff | Long | 消息堆积量 |

### 位点重置约束
- 仅 CLUSTERING 类型支持位点重置
- BROADCASTING 类型调用会返回错误
- 重置方式：时间戳重置（不支持指定位点）

## 后端 API 模式参考

```java
// GET /api/rocketmq/consumer-groups?keyword=xxx
@GetMapping("/consumer-groups")
public ResponseEntity<ApiResponse<Map<String, Object>>> getConsumerGroupList(
        @RequestParam(required = false) String keyword) {
    List<Map<String, Object>> list = rocketMQAdminService.getConsumerGroupList(keyword);
    Map<String, Object> data = new HashMap<>();
    data.put("records", list);
    data.put("total", list.size());
    return ResponseEntity.ok(ApiResponse.success(data));
}

// POST /api/rocketmq/consumer-groups/{group}/reset-offset
@PostMapping("/consumer-groups/{group}/reset-offset")
public ResponseEntity<ApiResponse<Void>> resetConsumerOffset(
        @PathVariable String group,
        @RequestBody Map<String, Object> request) {
    String topic = (String) request.get("topic");
    Long timestamp = ((Number) request.get("timestamp")).longValue();
    rocketMQAdminService.resetConsumerOffset(topic, group, timestamp);
    return ResponseEntity.ok(ApiResponse.success("位点重置成功", null));
}
```

## 前端 API 模式参考

```typescript
// app-vue/src/api/rocketmq.ts
export function getConsumerGroupList(keyword?: string) {
  return request.get<ApiResponse<{ records: ConsumerGroupVO[]; total: number }>>(
    '/rocketmq/consumer-groups',
    { params: { keyword } }
  )
}

export function getConsumerGroupDetail(group: string) {
  return request.get<ApiResponse<ConsumerGroupDetailVO>>(
    `/rocketmq/consumer-groups/${encodeURIComponent(group)}`
  )
}

export function resetConsumerOffset(group: string, topic: string, timestamp: number) {
  return request.post<ApiResponse<void>>(
    `/rocketmq/consumer-groups/${encodeURIComponent(group)}/reset-offset`,
    { topic, timestamp }
  )
}
```

## UI-SPEC 关键设计

### ConsumerGroupList.vue 布局
- Page container: white background, 20px padding, 4px border-radius
- Page title: "Consumer Group 管理" (20px, weight 600, #303133)
- Search section: #f5f7fa background, 20px padding, 16px margin-bottom
- 无新建按钮（本 phase 不创建 Group）
- el-table with stripe border
- Pagination right-aligned

### Table Columns
| Column | Prop | Width | Notes |
|--------|------|-------|-------|
| 序号 | index | 60px | center aligned |
| Group 名称 | group | min-width 200px | click to open detail |
| 类型 | groupType | 100px | Tag: 广播(蓝色)/集群(紫色) |
| 状态 | status | 100px | Tag: 正常(绿色)/重试中(橙色)/离线(红色) |
| 消费者数 | consumerCount | 100px | center aligned |
| 堆积量 | accumulatedDiff | 120px | center aligned |
| 操作 | - | 120px | fixed right: 查看 |

### ConsumerGroupDetail.vue (el-tabs)
- Tab 1: 消费进度 — 表格展示 Topic/队列/位点/堆积
- Tab 2: 订阅关系 — 表格展示 Topic/过滤表达式/起始位置
- Tab 3: 位点信息 — 卡片展示各队列消费状态

### ResetOffsetDialog.vue
- Width: 500px
- Topic 选择: el-select (订阅的 Topic 列表)
- 时间选择: el-date-picker (datetime)
- 仅 CLUSTERING 类型可用
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端 - 扩展 RocketMQAdminService</name>
  <files>
    springboot/src/main/java/cn/coderstory/springboot/service/RocketMQAdminService.java
    springboot/src/main/java/cn/coderstory/springboot/service/impl/RocketMQAdminServiceImpl.java
  </files>
  <read_first>
    springboot/src/main/java/cn/coderstory/springboot/service/RocketMQAdminService.java
    springboot/src/main/java/cn/coderstory/springboot/service/impl/RocketMQAdminServiceImpl.java
  </read_first>
  <action>
## 1.1 扩展 RocketMQAdminService.java 接口

在现有接口中添加 Consumer Group 相关方法：

```java
/**
 * 获取所有 Consumer Group 列表
 * @param keyword 关键字筛选（可选，模糊匹配）
 * @return Consumer Group 列表
 */
List<Map<String, Object>> getConsumerGroupList(String keyword);

/**
 * 获取 Consumer Group 详情
 * @param groupName Group 名称
 * @return Group 详细信息（消费进度、订阅关系、位点）
 */
Map<String, Object> getConsumerGroupDetail(String groupName);

/**
 * 重置消费位点（按时间戳）
 * @param topic Topic 名称
 * @param groupName Group 名称
 * @param timestamp 重置时间戳（毫秒）
 * @throws BusinessException 如果是广播模式或消费者不在线
 */
void resetConsumerOffset(String topic, String groupName, long timestamp);
```

## 1.2 实现 RocketMQAdminServiceImpl.java

实现 Consumer Group 相关方法：

```java
@Override
public List<Map<String, Object>> getConsumerGroupList(String keyword) {
    try {
        Set<String> groups = defaultMQAdminExt.fetchAllSubscriptionGroups();
        List<Map<String, Object>> result = new ArrayList<>();

        for (String group : groups) {
            if (keyword != null && !group.toLowerCase().contains(keyword.toLowerCase())) {
                continue;
            }

            Map<String, Object> item = new HashMap<>();
            item.put("group", group);

            // 获取消费统计
            try {
                ConsumeStats stats = defaultMQAdminExt.getConsumeStats(null, group, 3000);
                item.put("consumerCount", stats.getConsumerList() != null ? stats.getConsumerList().size() : 0);
                item.put("accumulatedDiff", stats.getTotalDiff());
            } catch (Exception e) {
                item.put("consumerCount", 0);
                item.put("accumulatedDiff", 0L);
            }

            // 获取 Group 配置（类型）
            try {
                SubscriptionGroupConfig config = defaultMQAdminExt.examineSubscriptionGroupConfig(null, group);
                if (config != null) {
                    item.put("groupType", config.getGroupType() == 0 ? "BROADCASTING" : "CLUSTERING");
                } else {
                    item.put("groupType", "UNKNOWN");
                }
                item.put("status", "OK");
            } catch (Exception e) {
                item.put("groupType", "UNKNOWN");
                item.put("status", "OFFLINE");
            }

            result.add(item);
        }

        return result;
    } catch (Exception e) {
        log.error("获取 Consumer Group 列表失败", e);
        throw new BusinessException("获取数据失败，请稍后重试");
    }
}

@Override
public Map<String, Object> getConsumerGroupDetail(String groupName) {
    try {
        Map<String, Object> detail = new HashMap<>();
        detail.put("group", groupName);

        // 消费进度
        ConsumeStats stats = defaultMQAdminExt.getConsumeStats(null, groupName, 3000);
        detail.put("consumerCount", stats.getConsumerList() != null ? stats.getConsumerList().size() : 0);
        detail.put("totalDiff", stats.getTotalDiff());

        // 队列位点信息
        Map<String, Long> offsetTable = new HashMap<>();
        if (stats.getOffsetTable() != null) {
            for (Map.Entry<MessageQueue, OffsetWrapper> entry : stats.getOffsetTable().entrySet()) {
                offsetTable.put(entry.getKey().getTopic() + "-" + entry.getKey().getQueueId(),
                    entry.getValue().getConsumerOffset());
            }
        }
        detail.put("offsetTable", offsetTable);

        // 订阅关系
        SubscriptionGroupConfig config = defaultMQAdminExt.examineSubscriptionGroupConfig(null, groupName);
        if (config != null) {
            detail.put("groupType", config.getGroupType() == 0 ? "BROADCASTING" : "CLUSTERING");
        }

        return detail;
    } catch (BusinessException e) {
        throw e;
    } catch (Exception e) {
        log.error("获取 Consumer Group 详情失败: {}", groupName, e);
        throw new BusinessException("获取数据失败，请稍后重试");
    }
}

@Override
public void resetConsumerOffset(String topic, String groupName, long timestamp) {
    try {
        // 检查 Group 类型
        SubscriptionGroupConfig config = defaultMQAdminExt.examineSubscriptionGroupConfig(null, groupName);
        if (config != null && config.getGroupType() == 0) {
            throw new BusinessException("广播模式不支持位点重置");
        }

        // 检查消费者是否在线
        ConsumeStats stats = defaultMQAdminExt.getConsumeStats(null, groupName, 3000);
        if (stats.getConsumerList() == null || stats.getConsumerList().isEmpty()) {
            log.warn("消费者不在线，重置可能生效: topic={}, group={}", topic, groupName);
        }

        // 执行重置
        defaultMQAdminExt.resetOffsetByTimestamp(topic, groupName, timestamp);
        log.info("位点重置成功: topic={}, group={}, timestamp={}", topic, groupName, timestamp);
    } catch (BusinessException e) {
        throw e;
    } catch (Exception e) {
        log.error("位点重置失败: topic={}, group={}", topic, groupName, e);
        throw new BusinessException("位点重置失败: " + e.getMessage());
    }
}
```
  </action>
  <acceptance_criteria>
    - RocketMQAdminService.java 包含 getConsumerGroupList, getConsumerGroupDetail, resetConsumerOffset 方法
    - RocketMQAdminServiceImpl.java 实现上述方法
    - resetConsumerOffset 对 BROADCASTING 类型抛出 BusinessException
  </acceptance_criteria>
  <verify>
    <automated>grep -E "getConsumerGroupList|getConsumerGroupDetail|resetConsumerOffset" springboot/src/main/java/cn/coderstory/springboot/service/RocketMQAdminService.java</automated>
  </verify>
  <done>后端服务层实现完成，Consumer Group API 准备就绪</done>
</task>

<task type="auto">
  <name>Task 2: 后端 - RocketMQController 扩展</name>
  <files>
    springboot/src/main/java/cn/coderstory/springboot/controller/RocketMQController.java
  </files>
  <read_first>
    springboot/src/main/java/cn/coderstory/springboot/controller/RocketMQController.java
  </read_first>
  <action>
## 2.1 扩展 RocketMQController.java

在现有 Controller 中添加 Consumer Group 相关端点：

```java
/**
 * 获取 Consumer Group 列表
 * GET /api/rocketmq/consumer-groups?keyword=xxx
 */
@GetMapping("/consumer-groups")
public ResponseEntity<ApiResponse<Map<String, Object>>> getConsumerGroupList(
        @RequestParam(required = false) String keyword) {
    List<Map<String, Object>> list = rocketMQAdminService.getConsumerGroupList(keyword);

    Map<String, Object> data = new HashMap<>();
    data.put("records", list);
    data.put("total", list.size());

    return ResponseEntity.ok(ApiResponse.success(data));
}

/**
 * 获取 Consumer Group 详情
 * GET /api/rocketmq/consumer-groups/{group}
 */
@GetMapping("/consumer-groups/{group}")
public ResponseEntity<ApiResponse<Map<String, Object>>> getConsumerGroupDetail(
        @PathVariable String group) {
    Map<String, Object> detail = rocketMQAdminService.getConsumerGroupDetail(group);
    return ResponseEntity.ok(ApiResponse.success(detail));
}

/**
 * 重置消费位点
 * POST /api/rocketmq/consumer-groups/{group}/reset-offset
 * Body: { "topic": "xxx", "timestamp": 1715000000000 }
 */
@PostMapping("/consumer-groups/{group}/reset-offset")
public ResponseEntity<ApiResponse<Void>> resetConsumerOffset(
        @PathVariable String group,
        @RequestBody Map<String, Object> request) {
    String topic = (String) request.get("topic");
    Long timestamp = ((Number) request.get("timestamp")).longValue();

    rocketMQAdminService.resetConsumerOffset(topic, group, timestamp);

    return ResponseEntity.ok(ApiResponse.success("位点重置成功", null));
}
```
  </action>
  <acceptance_criteria>
    - RocketMQController.java 包含 GET/POST /api/rocketmq/consumer-groups 端点
    - API 响应格式为 ApiResponse.success(data)
    - 返回值包含 records 列表和 total 数量
  </acceptance_criteria>
  <verify>
    <automated>grep -E "consumer-groups|getConsumerGroupList|resetConsumerOffset" springboot/src/main/java/cn/coderstory/springboot/controller/RocketMQController.java</automated>
  </verify>
  <done>REST API 实现完成，端点准备就绪</done>
</task>

<task type="auto">
  <name>Task 3: 前端 - API 层扩展</name>
  <files>
    app-vue/src/api/rocketmq.ts
  </files>
  <read_first>
    app-vue/src/api/rocketmq.ts
    app-vue/src/api/request.ts
    app-vue/src/api/types.ts
  </read_first>
  <action>
## 3.1 扩展 app-vue/src/api/rocketmq.ts

添加 Consumer Group 相关 API：

```typescript
export interface ConsumerGroupVO {
  group: string
  groupType: 'BROADCASTING' | 'CLUSTERING' | 'UNKNOWN'
  status: 'OK' | 'REBALANCE_NOT_INIT' | 'OFFLINE' | 'UNKNOWN'
  consumerCount: number
  accumulatedDiff: number
}

export interface ConsumerGroupDetailVO extends ConsumerGroupVO {
  totalDiff: number
  offsetTable: Record<string, number>
}

export interface ResetOffsetParams {
  topic: string
  timestamp: number
}

/**
 * 获取 Consumer Group 列表
 */
export function getConsumerGroupList(keyword?: string) {
  return request.get<ApiResponse<{ records: ConsumerGroupVO[]; total: number }>>(
    '/rocketmq/consumer-groups',
    { params: { keyword } }
  )
}

/**
 * 获取 Consumer Group 详情
 */
export function getConsumerGroupDetail(group: string) {
  return request.get<ApiResponse<ConsumerGroupDetailVO>>(
    `/rocketmq/consumer-groups/${encodeURIComponent(group)}`
  )
}

/**
 * 重置消费位点
 */
export function resetConsumerOffset(group: string, params: ResetOffsetParams) {
  return request.post<ApiResponse<void>>(
    `/rocketmq/consumer-groups/${encodeURIComponent(group)}/reset-offset`,
    params
  )
}
```

注意：保留原有的 Topic API (getTopicList, getTopicDetail, createTopic, deleteTopic)
  </action>
  <acceptance_criteria>
    - rocketmq.ts 包含 getConsumerGroupList, getConsumerGroupDetail, resetConsumerOffset 函数
    - 函数签名正确：keyword 可选、group URL 编码、timestamp 为 number
  </acceptance_criteria>
  <verify>
    <automated>grep -E "getConsumerGroupList|getConsumerGroupDetail|resetConsumerOffset" app-vue/src/api/rocketmq.ts</automated>
  </verify>
  <done>前端 API 层实现完成</done>
</task>

<task type="auto">
  <name>Task 4: 前端 - ConsumerGroupList.vue 列表页</name>
  <files>
    app-vue/src/views/rocketmq/ConsumerGroupList.vue
  </files>
  <read_first>
    app-vue/src/views/rocketmq/TopicList.vue
    app-vue/src/api/rocketmq.ts
  </read_first>
  <action>
## 4.1 创建 ConsumerGroupList.vue

基于 TopicList.vue 模式，创建 Consumer Group 列表页：

```vue
<script setup lang="ts">
import { ref, reactive, onMounted } from 'vue'
import { ElMessage } from 'element-plus'
import { getConsumerGroupList, type ConsumerGroupVO } from '@/api/rocketmq'
import ConsumerGroupDetail from './ConsumerGroupDetail.vue'

// 状态
const loading = ref(false)
const consumerGroupList = ref<ConsumerGroupVO[]>([])
const total = ref(0)

// 搜索表单
const searchForm = reactive({
  keyword: ''
})

// 详情弹窗
const detailDialogVisible = ref(false)
const currentGroup = ref<string>('')

// 表格列定义
const columns = [
  { prop: 'index', label: '序号', width: 60, align: 'center' },
  { prop: 'group', label: 'Group 名称', minWidth: 200 },
  { prop: 'groupType', label: '类型', width: 100, align: 'center' },
  { prop: 'status', label: '状态', width: 100, align: 'center' },
  { prop: 'consumerCount', label: '消费者数', width: 100, align: 'center' },
  { prop: 'accumulatedDiff', label: '堆积量', width: 120, align: 'center' },
  { prop: 'actions', label: '操作', width: 120, fixed: 'right' }
]

// 类型标签
const groupTypeTagType = (type: string) => {
  return type === 'BROADCASTING' ? 'primary' : ''
}

// 状态标签
const statusTagType = (status: string) => {
  switch (status) {
    case 'OK': return 'success'
    case 'REBALANCE_NOT_INIT': return 'warning'
    case 'OFFLINE': return 'danger'
    default: return 'info'
  }
}

// 加载数据
async function loadData() {
  loading.value = true
  try {
    const res = await getConsumerGroupList(searchForm.keyword || undefined)
    consumerGroupList.value = res.data.records
    total.value = res.data.total
  } catch (error: any) {
    ElMessage.error(error.message || '获取数据失败，请稍后重试')
  } finally {
    loading.value = false
  }
}

// 搜索
function handleSearch() {
  loadData()
}

// 重置
function handleReset() {
  searchForm.keyword = ''
  loadData()
}

// 查看详情
function handleView(row: ConsumerGroupVO) {
  currentGroup.value = row.group
  detailDialogVisible.value = true
}

onMounted(() => {
  loadData()
})
</script>

<template>
  <div class="page-container">
    <h2 class="page-title">Consumer Group 管理</h2>

    <!-- 搜索区域 -->
    <div class="search-section">
      <el-form :inline="true" :model="searchForm">
        <el-form-item label="Group 名称">
          <el-input
            v-model="searchForm.keyword"
            placeholder="输入 Group 名称搜索"
            clearable
            style="width: 200px"
            @keyup.enter="handleSearch"
          />
        </el-form-item>
        <el-form-item>
          <el-button type="primary" @click="handleSearch">查询</el-button>
          <el-button @click="handleReset">重置</el-button>
        </el-form-item>
      </el-form>
    </div>

    <!-- 表格 -->
    <el-table
      v-loading="loading"
      :data="consumerGroupList"
      stripe
      border
      style="width: 100%"
    >
      <el-table-column
        v-for="col in columns"
        :key="col.prop"
        :prop="col.prop"
        :label="col.label"
        :width="col.width"
        :min-width="col.minWidth"
        :align="col.align || 'left'"
        :fixed="col.fixed"
      >
        <template #default="{ row, $index }">
          <template v-if="col.prop === 'index'">
            {{ $index + 1 }}
          </template>
          <template v-else-if="col.prop === 'group'">
            <el-button link type="primary" @click="handleView(row)">
              {{ row.group }}
            </el-button>
          </template>
          <template v-else-if="col.prop === 'groupType'">
            <el-tag :type="groupTypeTagType(row.groupType)" size="small">
              {{ row.groupType === 'BROADCASTING' ? '广播' : '集群' }}
            </el-tag>
          </template>
          <template v-else-if="col.prop === 'status'">
            <el-tag :type="statusTagType(row.status)" size="small">
              {{ row.status === 'OK' ? '正常' : row.status === 'REBALANCE_NOT_INIT' ? '重试中' : '离线' }}
            </el-tag>
          </template>
          <template v-else-if="col.prop === 'accumulatedDiff'">
            {{ row.accumulatedDiff > 0 ? (row.accumulatedDiff / 10000).toFixed(1) + '万' : '0' }}
          </template>
          <template v-else-if="col.prop === 'actions'">
            <el-button link type="primary" size="small" @click="handleView(row)">
              查看
            </el-button>
          </template>
          <template v-else>
            {{ row[col.prop] }}
          </template>
        </template>
      </el-table-column>
    </el-table>

    <!-- 空状态 -->
    <el-empty v-if="!loading && consumerGroupList.length === 0" description="暂无 Consumer Group" />

    <!-- 详情弹窗 -->
    <ConsumerGroupDetail
      v-model="detailDialogVisible"
      :group-name="currentGroup"
      @refresh="loadData"
    />
  </div>
</template>

<style scoped>
.page-container {
  background: #fff;
  padding: 20px;
  border-radius: 4px;
}

.page-title {
  font-size: 20px;
  font-weight: 600;
  color: #303133;
  margin: 0 0 16px 0;
}

.search-section {
  background: #f5f7fa;
  padding: 20px;
  margin-bottom: 16px;
  border-radius: 4px;
}
</style>
```
  </action>
  <acceptance_criteria>
    - ConsumerGroupList.vue 使用 el-table, el-form, el-button 组件
    - 表格列包含: 序号, Group名称, 类型, 状态, 消费者数, 堆积量, 操作
    - 类型标签: 广播(蓝色)/集群(紫色)
    - 状态标签: 正常(绿色)/重试中(橙色)/离线(红色)
    - 点击 Group 名称打开详情弹窗
  </acceptance_criteria>
  <verify>
    <automated>test -f app-vue/src/views/rocketmq/ConsumerGroupList.vue && echo "EXISTS"</automated>
  </verify>
  <done>Consumer Group 列表页实现完成</done>
</task>

<task type="auto">
  <name>Task 5: 前端 - ConsumerGroupDetail.vue 详情弹窗</name>
  <files>
    app-vue/src/views/rocketmq/ConsumerGroupDetail.vue
  </files>
  <read_first>
    app-vue/src/views/rocketmq/TopicList.vue
  </read_first>
  <action>
## 5.1 创建 ConsumerGroupDetail.vue

详情弹窗，包含 3 个 Tab：消费进度、订阅关系、位点信息

```vue
<script setup lang="ts">
import { ref, watch } from 'vue'
import { ElMessage, ElMessageBox } from 'element-plus'
import { getConsumerGroupDetail, type ConsumerGroupDetailVO } from '@/api/rocketmq'
import ResetOffsetDialog from './ResetOffsetDialog.vue'

const props = defineProps<{
  modelValue: boolean
  groupName: string
}>()

const emit = defineEmits<{
  'update:modelValue': [value: boolean]
  'refresh': []
}>()

const dialogVisible = ref(props.modelValue)
watch(() => props.modelValue, (val) => {
  dialogVisible.value = val
})
watch(() => dialogVisible.value, (val) => {
  emit('update:modelValue', val)
})

const loading = ref(false)
const detailData = ref<ConsumerGroupDetailVO | null>(null)
const activeTab = ref('progress')

// 加载详情
async function loadDetail() {
  if (!props.groupName) return
  loading.value = true
  try {
    const res = await getConsumerGroupDetail(props.groupName)
    detailData.value = res.data
  } catch (error: any) {
    ElMessage.error(error.message || '获取详情失败')
  } finally {
    loading.value = false
  }
}

watch(() => props.groupName, () => {
  if (dialogVisible.value) {
    loadDetail()
  }
}, { immediate: true })

// 重置位点弹窗
const resetDialogVisible = ref(false)

function openResetDialog() {
  resetDialogVisible.value = true
}

function onResetSuccess() {
  resetDialogVisible.value = false
  emit('refresh')
}

const isClustering = (type: string) => type === 'CLUSTERING'
</script>

<template>
  <el-dialog
    v-model="dialogVisible"
    title="Consumer Group 详情"
    width="800px"
    @close="emit('update:modelValue', false)"
  >
    <div v-loading="loading">
      <!-- 基本信息 -->
      <el-descriptions v-if="detailData" :column="3" border style="margin-bottom: 20px">
        <el-descriptions-item label="Group 名称">{{ detailData.group }}</el-descriptions-item>
        <el-descriptions-item label="类型">
          <el-tag :type="detailData.groupType === 'BROADCASTING' ? 'primary' : ''" size="small">
            {{ detailData.groupType === 'BROADCASTING' ? '广播' : '集群' }}
          </el-tag>
        </el-descriptions-item>
        <el-descriptions-item label="状态">
          <el-tag :type="detailData.status === 'OK' ? 'success' : detailData.status === 'REBALANCE_NOT_INIT' ? 'warning' : 'danger'" size="small">
            {{ detailData.status === 'OK' ? '正常' : detailData.status === 'REBALANCE_NOT_INIT' ? '重试中' : '离线' }}
          </el-tag>
        </el-descriptions-item>
        <el-descriptions-item label="消费者数">{{ detailData.consumerCount }}</el-descriptions-item>
        <el-descriptions-item label="堆积量">{{ detailData.totalDiff > 0 ? (detailData.totalDiff / 10000).toFixed(1) + '万' : '0' }}</el-descriptions-item>
      </el-descriptions>

      <!-- Tabs -->
      <el-tabs v-model="activeTab">
        <el-tab-pane label="消费进度" name="progress">
          <el-table :data="[]" max-height="300">
            <el-table-column prop="topic" label="Topic" />
            <el-table-column prop="queueId" label="队列ID" width="100" />
            <el-table-column prop="consumerOffset" label="消费位点" width="120" />
            <el-table-column prop="brokerOffset" label="存储位点" width="120" />
            <el-table-column prop="diff" label="堆积量" width="100" />
          </el-table>
        </el-tab-pane>

        <el-tab-pane label="订阅关系" name="subscription">
          <el-table :data="[]" max-height="300">
            <el-table-column prop="topic" label="Topic" />
            <el-table-column prop="filterExpression" label="过滤表达式" />
            <el-table-column prop="startPosition" label="起始位置" width="100" />
          </el-table>
        </el-tab-pane>

        <el-tab-pane label="位点信息" name="offset">
          <div v-if="detailData && detailData.offsetTable">
            <el-card v-for="(offset, key) in detailData.offsetTable" :key="key" style="margin-bottom: 8px">
              <template #header>{{ key }}</template>
              <span>消费位点: {{ offset }}</span>
            </el-card>
          </div>
          <el-empty v-else description="暂无位点信息" />
        </el-tab-pane>
      </el-tabs>
    </div>

    <template #footer>
      <el-button @click="emit('update:modelValue', false)">关闭</el-button>
      <el-button
        v-if="detailData && isClustering(detailData.groupType)"
        type="primary"
        @click="openResetDialog"
      >
        重置位点
      </el-button>
    </template>
  </el-dialog>

  <!-- 重置位点弹窗 -->
  <ResetOffsetDialog
    v-model="resetDialogVisible"
    :group-name="props.groupName"
    @success="onResetSuccess"
  />
</template>
```
  </action>
  <acceptance_criteria>
    - ConsumerGroupDetail.vue 使用 el-dialog, el-tabs, el-descriptions 组件
    - 3 个 Tab: 消费进度、订阅关系、位点信息
    - 仅 CLUSTERING 类型显示"重置位点"按钮
  </acceptance_criteria>
  <verify>
    <automated>test -f app-vue/src/views/rocketmq/ConsumerGroupDetail.vue && echo "EXISTS"</automated>
  </verify>
  <done>Consumer Group 详情弹窗实现完成</done>
</task>

<task type="auto">
  <name>Task 6: 前端 - ResetOffsetDialog.vue 重置弹窗</name>
  <files>
    app-vue/src/views/rocketmq/ResetOffsetDialog.vue
  </files>
  <read_first>
    app-vue/src/views/rocketmq/ConsumerGroupDetail.vue
  </read_first>
  <action>
## 6.1 创建 ResetOffsetDialog.vue

```vue
<script setup lang="ts">
import { ref, watch } from 'vue'
import { ElMessage } from 'element-plus'
import { resetConsumerOffset, getTopicList } from '@/api/rocketmq'

const props = defineProps<{
  modelValue: boolean
  groupName: string
}>()

const emit = defineEmits<{
  'update:modelValue': [value: boolean]
  'success': []
}>()

const dialogVisible = ref(props.modelValue)
const loading = ref(false)
const topicList = ref<{ topicName: string }[]>([])

watch(() => props.modelValue, (val) => {
  dialogVisible.value = val
  if (val) {
    loadTopics()
  }
})
watch(() => dialogVisible.value, (val) => {
  emit('update:modelValue', val)
})

const form = reactive({
  topic: '',
  timestamp: null as Date | null,
  force: false
})

async function loadTopics() {
  try {
    const res = await getTopicList()
    topicList.value = res.data.records
  } catch (error) {
    // 忽略错误
  }
}

async function handleSubmit() {
  if (!form.topic || !form.timestamp) {
    ElMessage.warning('请选择 Topic 和时间')
    return
  }

  loading.value = true
  try {
    const timestamp = form.timestamp.getTime()
    await resetConsumerOffset(props.groupName, {
      topic: form.topic,
      timestamp
    })
    ElMessage.success('位点重置成功')
    emit('success')
    emit('update:modelValue', false)
  } catch (error: any) {
    ElMessage.error(error.message || '重置失败')
  } finally {
    loading.value = false
  }
}

import { reactive } from 'vue'
</script>

<template>
  <el-dialog
    v-model="dialogVisible"
    title="重置消费位点"
    width="500px"
    @close="emit('update:modelValue', false)"
  >
    <el-form :model="form" label-width="100px">
      <el-form-item label="Topic" required>
        <el-select v-model="form.topic" placeholder="选择 Topic" style="width: 100%">
          <el-option
            v-for="t in topicList"
            :key="t.topicName"
            :label="t.topicName"
            :value="t.topicName"
          />
        </el-select>
      </el-form-item>
      <el-form-item label="重置时间" required>
        <el-date-picker
          v-model="form.timestamp"
          type="datetime"
          placeholder="选择日期和时间"
          style="width: 100%"
        />
      </el-form-item>
      <el-form-item>
        <el-checkbox v-model="form.force">强制重置</el-checkbox>
      </el-form-item>
    </el-form>

    <template #footer>
      <el-button @click="emit('update:modelValue', false)">取消</el-button>
      <el-button type="primary" :loading="loading" @click="handleSubmit">
        确定
      </el-button>
    </template>
  </el-dialog>
</template>
```
  </action>
  <acceptance_criteria>
    - ResetOffsetDialog.vue 使用 el-dialog, el-form, el-select, el-date-picker 组件
    - Topic 下拉选择已订阅的 Topic
    - 时间选择支持日期和时间
    - 提交后调用 resetConsumerOffset API
  </acceptance_criteria>
  <verify>
    <automated>test -f app-vue/src/views/rocketmq/ResetOffsetDialog.vue && echo "EXISTS"</automated>
  </verify>
  <done>重置位点弹窗实现完成</done>
</task>

<task type="auto">
  <name>Task 7: 前端 - 路由配置</name>
  <files>
    app-vue/src/router/index.js
  </files>
  <read_first>
    app-vue/src/router/index.js
  </read_first>
  <action>
## 7.1 添加路由

在 router/index.js 中添加 Consumer Group 管理页面路由:

```javascript
{
  path: '/rocketmq/consumer-groups',
  name: 'ConsumerGroupList',
  component: () => import('@/views/rocketmq/ConsumerGroupList.vue'),
  meta: {
    title: 'Consumer Group 管理',
    requiresAuth: true
  }
}
```

注意：已在 Phase 9 中配置了 /rocketmq 父路由，不需要重复配置。
  </action>
  <acceptance_criteria>
    - 路由 /rocketmq/consumer-groups 指向 ConsumerGroupList.vue
    - 页面需要登录后才能访问
  </acceptance_criteria>
  <verify>
    <automated>grep -r "ConsumerGroupList" app-vue/src/router/</automated>
  </verify>
  <done>路由配置完成</done>
</task>

</tasks>

<verification>
## 验证步骤

### 后端验证
1. 启动 Spring Boot 应用: `cd springboot && ./mvnw.cmd spring-boot:run`
2. 测试 API 端点:
   - GET http://localhost:8080/api/rocketmq/consumer-groups (需 JWT token)
   - GET http://localhost:8080/api/rocketmq/consumer-groups/{group}
   - POST http://localhost:8080/api/rocketmq/consumer-groups/{group}/reset-offset

### 前端验证
1. 启动前端: `cd app-vue && npm run dev`
2. 访问 http://localhost:5173/rocketmq/consumer-groups
3. 验证页面显示正确的 Consumer Group 列表

### 功能验证清单
- [ ] 列表页面显示所有 Consumer Group
- [ ] 按名称搜索可以过滤结果
- [ ] 点击 Group 名称打开详情弹窗
- [ ] 详情弹窗显示 3 个 Tab（消费进度/订阅关系/位点信息）
- [ ] CLUSTERING 类型显示"重置位点"按钮
- [ ] BROADCASTING 类型不显示"重置位点"按钮
- [ ] 重置功能正常工作
</verification>

<success_criteria>
1. 后端编译成功，无语法错误
2. 前端构建成功，无 TypeScript 错误
3. API 端点响应正确格式的数据
4. 页面按照 UI-SPEC 正确渲染
5. 重置功能正常工作（仅 CLUSTERING 类型）
</success_criteria>

<output>
After completion, create `.planning/phases/10-consumer-group-mgmt/10-01-SUMMARY.md`
</output>