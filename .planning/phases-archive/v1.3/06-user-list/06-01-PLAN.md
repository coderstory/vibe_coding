---
phase: 06-user-list
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app-vue/src/views/system/UserManagement.vue
  - app-vue/src/api/user.js
  - app-vue/src/router/index.js
  - springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java
  - springboot/src/main/java/cn/coderstory/springboot/service/UserService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/impl/UserServiceImpl.java
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
  - LIST-04
  - LIST-05
  - LIST-06
  - STATUS-03
  - API-06

must_haves:
  truths:
    - "PATCH /api/users/{id}/status 可更新用户状态"
    - "搜索表单包含 phone 精确筛选输入框"
    - "用户列表表格显示手机号列"
    - "状态列使用 el-switch 内联切换，切换后显示 ElMessage.success/failure"
    - "点击表格行导航到 /system/user/:id 详情页"
    - "分页组件支持切换每页条数 [10, 20, 50, 100]"
  artifacts:
    - path: "app-vue/src/views/system/UserManagement.vue"
      provides: "用户列表页面"
      contains: "phone 筛选, phone 列, el-switch 状态切换, 行点击导航"
    - path: "app-vue/src/api/user.js"
      provides: "前端 API 调用"
      contains: "updateUserStatus"
  key_links:
    - from: "UserManagement.vue handleSearch"
      to: "/api/users"
      via: "getUserList({ phone })"
      pattern: "getUserList.*phone"
    - from: "UserManagement.vue el-switch change"
      to: "updateUserStatus API"
      via: "await updateUserStatus(id, enabled)"
      pattern: "updateUserStatus"
---

<objective>
实现用户列表页面完整功能，包括 phone 筛选、手机号列显示、状态切换、点击行导航到详情页。

Purpose: 为用户提供完整的用户列表浏览和筛选体验，支持状态快速切换
Output: UserManagement.vue 增强、user.js API、router 配置、UserDetail.vue 存根
</objective>

<context>
@.planning/phases/06-user-list/06-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-backend-api/05-01-SUMMARY.md

# 现有代码上下文

From app-vue/src/views/system/UserManagement.vue (当前状态):
- 搜索表单: username, name, department, enabled (缺少 phone)
- 表格列: 序号、用户名、姓名、性别、部门、岗位、邮箱、状态、创建时间、操作 (缺少手机)
- 状态显示: el-tag (需要改为 el-switch)
- 分页: :page-sizes="[10, 20, 50, 100]" (已是正确配置)

From app-vue/src/api/user.js (当前状态):
- getUserList(params) - 缺少 updateUserStatus
- 无 updateUserStatus(id, enabled) 函数

From app-vue/src/router/index.js (当前状态):
- /system/user -> UserManagement.vue
- 缺少 /system/user/:id -> UserDetail.vue

From springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java:
- PATCH /api/users/{id}/status 端点不存在 (需要新增)
</context>

<tasks>

<task type="auto">
  <name>task 0: Add PATCH /api/users/{id}/status backend endpoint</name>
  <files>springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java</files>
  <files>springboot/src/main/java/cn/coderstory/springboot/service/UserService.java</files>
  <files>springboot/src/main/java/cn/coderstory/springboot/service/impl/UserServiceImpl.java</files>
  <read_first>
    - springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java
    - springboot/src/main/java/cn/coderstory/springboot/service/UserService.java
    - springboot/src/main/java/cn/coderstory/springboot/service/impl/UserServiceImpl.java
  </read_first>
  <action>
    **UserService.java** - 添加状态更新方法签名:
    ```java
    boolean updateUserStatus(Long id, Integer enabled);
    ```

    **UserServiceImpl.java** - 添加状态更新实现:
    ```java
    @Override
    public boolean updateUserStatus(Long id, Integer enabled) {
        User user = userMapper.selectById(id);
        if (user == null) {
            return false;
        }
        user.setEnabled(enabled);
        return userMapper.updateById(user) > 0;
    }
    ```

    **UserController.java** - 添加 PATCH 端点:
    ```java
    @PatchMapping("/{id}/status")
    public ResponseEntity<Map<String, Object>> updateUserStatus(
            @PathVariable Long id,
            @RequestBody Map<String, Integer> request) {
        Integer enabled = request.get("enabled");
        boolean success = userService.updateUserStatus(id, enabled);

        Map<String, Object> response = new HashMap<>();
        response.put("code", success ? 200 : 500);
        response.put("message", success ? "success" : "状态更新失败");

        return ResponseEntity.ok(response);
    }
    ```
  </action>
  <acceptance_criteria>
    - UserService.java 包含 updateUserStatus 方法签名
    - UserServiceImpl.java 实现 updateUserStatus 方法
    - UserController.java 包含 @PatchMapping("/{id}/status") 端点
    - grep "@PatchMapping" UserController.java 能找到
  </acceptance_criteria>
  <verify>
    <automated>grep -n "updateUserStatus" springboot/src/main/java/cn/coderstory/springboot/service/UserService.java</automated>
    <automated>grep -n "updateUserStatus" springboot/src/main/java/cn/coderstory/springboot/service/impl/UserServiceImpl.java</automated>
    <automated>grep -n "PatchMapping.*status" springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java</automated>
  </verify>
  <done>后端支持 PATCH /api/users/{id}/status 状态更新 API</done>
</task>

<task type="auto">
  <name>task 1: Add phone exact filter to search form</name>
  <files>app-vue/src/views/system/UserManagement.vue</files>
  <read_first>
    - app-vue/src/views/system/UserManagement.vue
    - app-vue/src/api/user.js
  </read_first>
  <action>
    **UserManagement.vue - 搜索表单添加 phone 筛选:**

    1. 在 searchForm 中添加 phone 字段:
    ```javascript
    const searchForm = reactive({
      username: '',
      name: '',
      department: '',
      enabled: null,
      phone: ''  // 新增
    })
    ```

    2. 在搜索表单模板中添加 phone 输入框 (放在部门后面):
    ```html
    <el-form-item label="手机号">
      <el-input v-model="searchForm.phone" placeholder="请输入手机号" clearable style="width: 150px" />
    </el-form-item>
    ```

    3. 在 loadUserList() 中添加 phone 参数:
    ```javascript
    if (searchForm.phone) params.phone = searchForm.phone
    ```

    4. 在 handleReset() 中重置 phone: `searchForm.phone = ''`

    **user.js - 添加 updateUserStatus API:**

    在 user.js 末尾添加:
    ```javascript
    export function updateUserStatus(id, enabled) {
      return request({
        url: `/users/${id}/status`,
        method: 'patch',
        data: { enabled }
      })
    }
    ```
  </action>
  <acceptance_criteria>
    - searchForm 包含 phone 字段
    - 模板中有 phone 输入框
    - loadUserList() 传递 phone 参数给 API
    - handleReset() 重置 phone
    - user.js 导出 updateUserStatus 函数
  </acceptance_criteria>
  <verify>
    <automated>grep -n "phone" app-vue/src/views/system/UserManagement.vue | head -10</automated>
    <automated>grep -n "updateUserStatus" app-vue/src/api/user.js</automated>
  </verify>
  <done>搜索表单支持手机号精确筛选</done>
</task>

<task type="auto">
  <name>task 2: Add phone column to table and status switch toggle</name>
  <files>app-vue/src/views/system/UserManagement.vue</files>
  <read_first>
    - app-vue/src/views/system/UserManagement.vue
  </read_first>
  <action>
    **UserManagement.vue - 表格添加手机号列:**

    在岗位 (position) 列后面、邮箱列前面添加手机列:
    ```html
    <el-table-column prop="phone" label="手机" min-width="120" />
    ```

    **UserManagement.vue - 状态列改为 el-switch 内联切换:**

    将当前状态列:
    ```html
    <el-table-column prop="enabled" label="状态" width="100" align="center">
      <template #default="{ row }">
        <el-tag :type="row.enabled === 1 ? 'success' : 'info'" size="small">
          {{ formatStatus(row.enabled) }}
        </el-tag>
      </template>
    </el-table-column>
    ```

    替换为:
    ```html
    <el-table-column prop="enabled" label="状态" width="100" align="center">
      <template #default="{ row }">
        <el-switch
          v-model="row.enabled"
          :active-value="1"
          :inactive-value="0"
          @change="handleStatusChange(row)"
        />
      </template>
    </el-table-column>
    ```

    **添加 handleStatusChange 方法:**
    ```javascript
    async function handleStatusChange(row) {
      try {
        await updateUserStatus(row.id, row.enabled)
        ElMessage.success(row.enabled === 1 ? '用户已启用' : '用户已禁用')
      } catch (error) {
        // 恢复原状态
        row.enabled = row.enabled === 1 ? 0 : 1
        ElMessage.error('状态更新失败')
      }
    }
    ```

    **在 import 中添加 updateUserStatus:**
    ```javascript
    import { getUserList, createUser, updateUser, deleteUser, resetUserPassword, getAllRoles, updateUserStatus } from '@/api/user'
    ```
  </action>
  <acceptance_criteria>
    - 表格有手机号列 (prop="phone")
    - 状态列使用 el-switch 而非 el-tag
    - el-switch 绑定 row.enabled
    - @change="handleStatusChange(row)" 存在
    - handleStatusChange 方法正确处理成功/失败
  </acceptance_criteria>
  <verify>
    <automated>grep -n "prop=\"phone\"" app-vue/src/views/system/UserManagement.vue</automated>
    <automated>grep -n "el-switch" app-vue/src/views/system/UserManagement.vue</automated>
    <automated>grep -n "handleStatusChange" app-vue/src/views/system/UserManagement.vue</automated>
  </verify>
  <done>表格显示手机号，状态可内联切换并显示反馈</done>
</task>

<task type="auto">
  <name>task 3: Add row click navigation to user detail page</name>
  <files>app-vue/src/views/system/UserManagement.vue</files>
  <files>app-vue/src/router/index.js</files>
  <read_first>
    - app-vue/src/views/system/UserManagement.vue
    - app-vue/src/router/index.js
  </read_first>
  <action>
    **UserManagement.vue - 添加行点击导航:**

    1. 引入 useRouter:
    ```javascript
    import { ref, reactive, onMounted } from 'vue'
    import { useRouter } from 'vue-router'
    const router = useRouter()
    ```

    2. 给 el-table 添加 @row-click 事件:
    ```html
    <el-table :data="userList" stripe border v-loading="loading" class="user-table" @row-click="handleRowClick">
    ```

    3. 添加 handleRowClick 方法 (放在 handleSizeChange 后面):
    ```javascript
    function handleRowClick(row) {
      router.push('/system/user/' + row.id)
    }
    ```

    **router/index.js - 添加用户详情路由:**

    在 children 数组中、UserManage 路由后面添加:
    ```javascript
    {
      path: 'system/user/:id',
      name: 'UserDetail',
      component: () => import('@/views/system/UserDetail.vue'),
      meta: { title: '用户详情', requiresAuth: true }
    }
    ```

    **创建 UserDetail.vue 存根组件:**

    创建 app-vue/src/views/system/UserDetail.vue:
    ```vue
    <script setup>
    import { ref, onMounted } from 'vue'
    import { useRoute, useRouter } from 'vue-router'
    import { ElMessage } from 'element-plus'
    import { getUserDetail } from '@/api/user'

    const route = useRoute()
    const router = useRouter()
    const user = ref(null)
    const loading = ref(false)

    async function loadUser() {
      loading.value = true
      try {
        const res = await getUserDetail(route.params.id)
        user.value = res.data
      } catch (error) {
        ElMessage.error('加载用户详情失败')
      } finally {
        loading.value = false
      }
    }

    function goBack() {
      router.push('/system/user')
    }

    onMounted(() => {
      loadUser()
    })
    </script>

    <template>
      <div class="page-container" v-loading="loading">
        <h2 class="page-title">用户详情</h2>
        <el-button @click="goBack">返回列表</el-button>
        <el-descriptions v-if="user" :column="2" border style="margin-top: 20px">
          <el-descriptions-item label="用户名">{{ user.username }}</el-descriptions-item>
          <el-descriptions-item label="姓名">{{ user.name }}</el-descriptions-item>
          <el-descriptions-item label="手机">{{ user.phone }}</el-descriptions-item>
          <el-descriptions-item label="邮箱">{{ user.email }}</el-descriptions-item>
          <el-descriptions-item label="部门">{{ user.department }}</el-descriptions-item>
          <el-descriptions-item label="岗位">{{ user.position }}</el-descriptions-item>
          <el-descriptions-item label="状态">
            <el-tag :type="user.enabled === 1 ? 'success' : 'info'">
              {{ user.enabled === 1 ? '启用' : '禁用' }}
            </el-tag>
          </el-descriptions-item>
          <el-descriptions-item label="创建时间">{{ user.createTime }}</el-descriptions-item>
        </el-descriptions>
      </div>
    </template>

    <style scoped>
    .page-container {
      background: #fff;
      padding: 20px;
      border-radius: 4px;
    }
    .page-title {
      margin: 0 0 20px 0;
      font-size: 20px;
      font-weight: 600;
      color: #303133;
    }
    </style>
    ```
  </action>
  <acceptance_criteria>
    - el-table 有 @row-click="handleRowClick"
    - handleRowClick 使用 router.push('/system/user/' + row.id)
    - router/index.js 包含 /system/user/:id 路由
    - UserDetail.vue 存在且可访问
    - 点击表格行能导航到详情页
  </acceptance_criteria>
  <verify>
    <automated>grep -n "row-click" app-vue/src/views/system/UserManagement.vue</automated>
    <automated>grep -n "router.push" app-vue/src/views/system/UserManagement.vue</automated>
    <automated>grep -n "UserDetail" app-vue/src/router/index.js</automated>
    <automated>test -f app-vue/src/views/system/UserDetail.vue && echo "UserDetail.vue exists"</automated>
  </verify>
  <done>点击表格行能导航到用户详情页</done>
</task>

</tasks>

<verification>
前端编译验证:
```bash
cd app-vue; npm run build 2>&1 | head -30
```

功能验证 (需要启动前后端):
1. 访问 /system/user 页面
2. 在搜索框输入手机号，点击查询 - 应显示匹配用户
3. 点击表格状态列的 switch - 应切换状态并显示成功提示
4. 点击表格任意行 - 应导航到 /system/user/:id 详情页
5. 点击返回按钮 - 应回到用户列表
</verification>

<success_criteria>
1. 搜索表单包含 username、name、department、enabled、phone 筛选
2. 用户列表表格显示手机号列
3. 状态列使用 el-switch 内联切换
4. 切换状态后显示 ElMessage.success/failure 反馈
5. 点击表格行导航到 /system/user/:id
6. 分页支持切换每页条数 [10, 20, 50, 100]
7. npm run build 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/06-user-list/06-01-SUMMARY.md`
</output>
