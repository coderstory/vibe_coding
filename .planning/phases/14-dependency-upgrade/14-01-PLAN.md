---
phase: 14-dependency-upgrade
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - springboot/gradle/libs.versions.toml
  - springboot/build.gradle.kts
autonomous: true
requirements:
  - BOOT-01
  - BOOT-02
  - BOOT-03
  - DEPS-01
  - DEPS-02
  - DEPS-03
  - DEPS-04
  - DEPS-05
  - DEPS-06
  - DEPS-07
user_setup: []

must_haves:
  truths:
    - "Spring Boot plugin shows version 4.1.0-RC1"
    - "All dependency versions updated to 4.1 compatible"
    - "libs.versions.toml contains updated version references"
    - "build.gradle.kts references updated versions"
  artifacts:
    - path: "springboot/gradle/libs.versions.toml"
      provides: "Centralized version catalog"
      contains: "spring-boot = \"4.1.0-RC1\""
    - path: "springboot/build.gradle.kts"
      provides: "Build configuration with updated plugin version"
      contains: "id(\"org.springframework.boot\") version \"4.1.0-RC1\""
  key_links:
    - from: "springboot/build.gradle.kts"
      to: "springboot/gradle/libs.versions.toml"
      via: "version.ref references"
      pattern: "version.ref = \"spring-boot\""
---

<objective>
升级 Spring Boot 到 4.1.0-RC1，更新所有依赖到最新兼容版本。保持项目功能不变，仅更新依赖版本。

Purpose: 统一依赖版本到 Spring Boot 4.1 生态系统
Output: Updated build.gradle.kts and libs.versions.toml with new versions
</objective>

<execution_context>
@D:/Data/桌面/vibe_coding/.claude/get-shit-done/workflows/execute-plan.md
@D:/Data/桌面/vibe_coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-dependency-upgrade/14-CONTEXT.md
@springboot/build.gradle.kts
@springboot/gradle/libs.versions.toml

<interfaces>
Key version references after update:

# libs.versions.toml versions section
[versions]
spring-boot = "4.1.0-RC1"
spring-aop = "4.1.0"  # Spring Boot 4.1 manages this
redisson = "4.3.1"  # Latest compatible
mysql = "9.7.0"  # Upgrade from 9.6.0
lombok = "1.18.50"  # Latest stable

# build.gradle.kts plugins
id("org.springframework.boot") version "4.1.0-RC1"
id("io.spring.dependency-management") version "1.1.8"  # Latest for Spring Boot 4.1

# Key dependencies to update in build.gradle.kts
- spring-boot-starter-aop: remove explicit version (let BOM manage)
- mysql-connector-j: 9.6.0 → 9.7.0
- lombok: 1.18.44 → 1.18.50
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update libs.versions.toml with new versions</name>
  <files>springboot/gradle/libs.versions.toml</files>
  <action>
Update the [versions] section in libs.versions.toml:

1. Change spring-boot from "4.0.5" to "4.1.0-RC1"
2. Change spring-aop from "3.5.13" to "4.1.0" (Spring Boot 4.1 manages AOP)
3. Change mysql from "9.6.0" to "9.7.0" (per DEPS-03)
4. Change lombok from "1.18.44" to "1.18.50" (latest stable per D-03)
5. Keep flyway at "12.4.0" (already correct per DEPS-01 and D-04)
6. Keep redisson at "4.3.1" (latest compatible per DEPS-02)
7. Keep other versions unchanged (mybatis-plus 3.5.16, rocketmq versions, jjwt 0.13.0, zstd 1.5.7-7)

Update the [plugins] section:
- Change spring-boot version ref to "4.1.0-RC1"
- Update dependency-management from "1.1.7" to "1.1.8" (latest for Spring Boot 4.1 compatibility)

DO NOT change the [libraries] section - those reference version.ref correctly.
</action>
  <verify>
<automated>grep -c "spring-boot = \"4.1.0-RC1\"" springboot/gradle/libs.versions.toml && grep -c "mysql = \"9.7.0\"" springboot/gradle/libs.versions.toml && grep -c "lombok = \"1.18.50\"" springboot/gradle/libs.versions.toml</automated>
  </verify>
  <done>
libs.versions.toml has updated versions for spring-boot (4.1.0-RC1), mysql (9.7.0), lombok (1.18.50), spring-aop (4.1.0), and dependency-management plugin (1.1.8)
</done>
</task>

<task type="auto">
  <name>Task 2: Update build.gradle.kts plugin and dependency versions</name>
  <files>springboot/build.gradle.kts</files>
  <action>
Update build.gradle.kts following D-01 and D-02:

1. Change Spring Boot plugin version:
   FROM: id("org.springframework.boot") version "4.0.5"
   TO:   id("org.springframework.boot") version "4.1.0-RC1"

2. Update dependency-management plugin:
   FROM: id("io.spring.dependency-management") version "1.1.7"
   TO:   id("io.spring.dependency-management") version "1.1.8"

3. Remove explicit version from spring-boot-starter-aop (let BOM manage it):
   FROM: implementation("org.springframework.boot:spring-boot-starter-aop:3.5.13")
   TO:   implementation("org.springframework.boot:spring-boot-starter-aop")

4. Update MySQL connector version:
   FROM: implementation("com.mysql:mysql-connector-j:9.6.0")
   TO:   implementation("com.mysql:mysql-connector-j:9.7.0")

5. Update Lombok version:
   FROM: compileOnly("org.projectlombok:lombok:1.18.44")
   FROM: annotationProcessor("org.projectlombok:lombok:1.18.44")
   TO:   compileOnly("org.projectlombok:lombok:1.18.50")
   TO:   annotationProcessor("org.projectlombok:lombok:1.18.50")

Keep these unchanged:
- Flyway 12.4.0 (already correct per DEPS-01)
- Redisson 4.3.1 (latest compatible per DEPS-02)
- MyBatis-Plus 3.5.16 (per DEPS-04)
- RocketMQ versions (5.3.2, 2.3.5) - will be verified per DEPS-05
- JJWT 0.13.0 - will be verified per DEPS-06
- ZSTD 1.5.7-7 - will be verified per DEPS-07
</action>
  <verify>
<automated>grep -c "id(\"org.springframework.boot\") version \"4.1.0-RC1\"" springboot/build.gradle.kts && grep -c "mysql-connector-j:9.7.0" springboot/build.gradle.kts && grep -c "lombok:1.18.50" springboot/build.gradle.kts</automated>
  </verify>
  <done>
build.gradle.kts has updated Spring Boot plugin (4.1.0-RC1), dependency-management plugin (1.1.8), MySQL (9.7.0), Lombok (1.18.50), and spring-aop without explicit version
</done>
</task>

<task type="auto">
  <name>Task 3: Verify dependency resolution with Gradle</name>
  <files>springboot/build.gradle.kts</files>
  <action>
Run Gradle dependency command to verify all versions resolve correctly:

1. Execute: cd springboot && ./gradlew.bat dependencies --configuration compileClasspath

2. Verify output shows:
   - Spring Boot 4.1.0-RC1
   - Spring AOP 4.1.x
   - MySQL Connector 9.7.0
   - Lombok 1.18.50

3. Check for any "could not find" or version conflict errors

If resolution fails, diagnose and fix version incompatibilities.
</action>
  <verify>
<automated>cd springboot && ./gradlew.bat dependencies --configuration compileClasspath 2>&1 | grep -E "(spring-boot|4.1.0-RC1|FAILED|ERROR|could not resolve)" | head -20</automated>
  </verify>
  <done>
Gradle successfully resolves all dependencies without conflicts, showing Spring Boot 4.1.0-RC1 as the managed version
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {build system → mavenCentral} | Downloads dependencies from external repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Availability | mavenCentral | mitigate | Verify connectivity before upgrade; use working versions if central is slow |
| T-14-02 | Integrity | dependency download | accept | Gradle verifies checksums; rely on mavenCentral authenticity |
</threat_model>

<verification>
## Phase 14 Verification

1. Run: cd springboot && ./gradlew.bat dependencies --configuration compileClasspath | grep spring-boot
   Expected: Shows spring-boot-4.1.0-RC1

2. Run: cd springboot && ./gradlew.bat dependencies --configuration compileClasspath | grep mysql-connector-j
   Expected: Shows mysql-connector-j-9.7.x

3. Run: cd springboot && ./gradlew.bat dependencies --configuration compileClasspath | grep lombok
   Expected: Shows lombok-1.18.50

4. Check build.gradle.kts for correct plugin versions
5. Check libs.versions.toml for updated version references
</verification>

<success_criteria>
1. Spring Boot plugin version shows 4.1.0-RC1 in build.gradle.kts
2. All dependency versions updated per version table
3. libs.versions.toml contains correct version references
4. Gradle dependency resolution completes without conflicts
5. All 11 requirements (BOOT-01~04, DEPS-01~07) addressed in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/14-dependency-upgrade/14-01-SUMMARY.md`
</output>
