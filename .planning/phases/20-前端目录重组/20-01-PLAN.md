# Phase 20 Plan: 前端目录重组

## 目标

按业务域组织前端 components/api/router/types 目录结构，消除 7KB 单一路由文件，清理无用脚手架组件。

## 涉及需求

- FRNT-01: components/ 拆分为 common/layout/business
- FRNT-02: api/modules/ 按业务域组织
- FRNT-03: router/ 拆分为 modules/*.ts + guards.ts
- FRNT-04: npm run build 验证

## 执行结果

**components/ 重组:**
- layout/ — AppHeader.vue, AppMenu.vue, AppTabs.vue
- common/ — Icon*.vue 图标组件
- business/knowledge/ — ArticleEditor.vue, CategoryTree.vue
- 已清理: HelloWorld.vue, TheWelcome.vue, WelcomeItem.vue

**api/ 重组:**
- modules/ — 13 个业务域 API 文件
- 根目录保留: request.ts, types.ts, index.ts（重导出）
- 所有 30+ 处 import 已更新

**router/ 拆分:**
- modules/routes.ts — 27 条路由定义
- guards.ts — 导航守卫
- index.ts — 创建路由实例

**验证:** npm run build 通过，27 个懒加载路由全部生成
