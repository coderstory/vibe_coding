---
phase: 03-business-data
plan: '02'
type: execute
wave: 2
depends_on:
  - '03-01'
files_modified:
  - springboot/src/main/java/cn/coderstory/springboot/mapper/KnowledgeCategoryMapper.java
  - springboot/src/main/java/cn/coderstory/springboot/mapper/KnowledgeArticleMapper.java
  - springboot/src/main/java/cn/coderstory/springboot/mapper/KnowledgeTagMapper.java
  - springboot/src/main/java/cn/coderstory/springboot/mapper/KnowledgeFileMapper.java
  - springboot/src/main/resources/mapper/KnowledgeArticleMapper.xml
  - springboot/src/main/java/cn/coderstory/springboot/service/KnowledgeService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/impl/KnowledgeServiceImpl.java
  - springboot/src/main/java/cn/coderstory/springboot/controller/KnowledgeController.java
  - springboot/src/main/java/cn/coderstory/springboot/util/ZstdUtil.java
autonomous: true
requirements:
  - BIZ-01
  - BIZ-02
  - BIZ-03
  - BIZ-04
  - BIZ-05
---

<objective>
实现知识库后端 API，包含分类树形结构、文章 CRUD、标签管理、文件上传（zstd压缩）、全文搜索。

Purpose: 提供完整的知识库后端接口，支持前端调用
Output: 4 个 Mapper + Mapper XML + Service + Controller + ZstdUtil
</objective>

<context>
@./springboot/src/main/java/cn/coderstory/springboot/entity/KnowledgeCategory.java
@./springboot/src/main/java/cn/coderstory/springboot/entity/KnowledgeArticle.java
@./springboot/src/main/java/cn/coderstory/springboot/entity/KnowledgeTag.java
@./springboot/src/main/java/cn/coderstory/springboot/entity/KnowledgeFile.java
@./springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java
@./springboot/src/main/resources/mapper/MenuMapper.xml
@./AGENTS.md

# 后端规范：
# 1. SQL 写到 mapper.xml，不写在接口类上
# 2. 使用 MyBatis-Plus BaseService 简化增删改查
# 3. Controller 返回 Map 格式：{code, message, data}
</context>

<tasks>

<task type="auto">
  <name>task 1: 创建 Mapper 接口和 XML</name>
  <read_first>
    - springboot/src/main/java/cn/coderstory/springboot/mapper/MenuMapper.java
    - springboot/src/main/resources/mapper/MenuMapper.xml
  </read_first>
  <action>
创建 4 个 Mapper 接口：

1. **KnowledgeCategoryMapper.java**
   - 继承 BaseMapper<KnowledgeCategory>
   - 方法：selectTree() - 查询所有分类构成树形结构

2. **KnowledgeArticleMapper.java**
   - 继承 BaseMapper<KnowledgeArticle>
   - 方法：searchArticles(keyword, categoryId, page, size) - 全文搜索

3. **KnowledgeTagMapper.java**
   - 继承 BaseMapper<KnowledgeTag>
   - 方法：selectByName(name), selectByArticleId(articleId)

4. **KnowledgeFileMapper.java**
   - 继承 BaseMapper<KnowledgeFile>
   - 方法：selectByArticleId(articleId)

创建 **KnowledgeArticleMapper.xml**，包含：
- searchArticles 查询：使用 MATCH AGAINST 全文搜索，关联分类表和标签表返回文章列表
- 支持按 categoryId 筛选和分页

所有 SQL 遵循 MenuMapper.xml 的风格，使用 resultMap 处理列名映射。
  </action>
  <verify>
    <automated>ls springboot/src/main/java/cn/coderstory/springboot/mapper/Knowledge*.java</automated>
  </verify>
  <done>
    4 个 Mapper 接口 + 1 个 Mapper XML 已创建
  </done>
</task>

<task type="auto">
  <name>task 2: 创建 ZstdUtil 压缩工具类</name>
  <read_first>
    - springboot/pom.xml
  </read_first>
  <action>
创建 **ZstdUtil.java** 在 util 包：

```java
package cn.coderstory.springboot.util;

import com.github.luben.zstd.Zstd;
import lombok.extern.slf4j.Slf4j;
import java.nio.charset.StandardCharsets;

@Slf4j
public class ZstdUtil {
    
    public static byte[] compress(byte[] data) {
        if (data == null) return null;
        return Zstd.compress(data);
    }
    
    public static byte[] decompress(byte[] compressed, long originalSize) {
        if (compressed == null) return null;
        return Zstd.decompress(new byte[originalSize], compressed);
    }
    
    public static byte[] compressString(String str) {
        if (str == null) return null;
        return compress(str.getBytes(StandardCharsets.UTF_8));
    }
    
    public static String decompressToString(byte[] compressed, long originalSize) {
        if (compressed == null) return null;
        byte[] decompressed = decompress(compressed, originalSize);
        return new String(decompressed, StandardCharsets.UTF_8);
    }
}
```

注意：需要添加 zstd-jni 依赖到 pom.xml。
  </action>
  <verify>
    <automated>ls springboot/src/main/java/cn/coderstory/springboot/util/ZstdUtil.java</automated>
  </verify>
  <done>
    ZstdUtil 工具类已创建
  </done>
</task>

<task type="auto">
  <name>task 3: 创建 KnowledgeService 和实现</name>
  <read_first>
    - springboot/src/main/java/cn/coderstory/springboot/service/UserService.java
    - springboot/src/main/java/cn/coderstory/springboot/service/impl/UserServiceImpl.java
  </read_first>
  <action>
创建 **KnowledgeService.java** 接口：
- getCategoryTree() - 获取分类树
- getArticlePage(keyword, categoryId, page, size) - 分页查询文章
- getArticleById(id) - 获取文章详情（含标签和附件）
- createArticle(data) - 创建文章
- updateArticle(id, data) - 更新文章
- deleteArticle(id) - 删除文章（软删除）
- getTagsByArticleId(articleId) - 获取文章标签
- createTag(name) - 创建标签
- getAllTags() - 获取所有标签
- uploadFile(articleId, file) - 上传文件（zstd压缩）
- getFile(fileId) - 获取文件（解压）
- deleteFile(fileId) - 删除文件

创建 **KnowledgeServiceImpl.java** 实现类：
- 继承 ServiceImpl<KnowledgeArticleMapper, KnowledgeArticle>
- 实现 KnowledgeService 接口
- 使用 Mapper 进行数据库操作
- 文件上传时使用 ZstdUtil.compress() 压缩
- 文件下载时使用 ZstdUtil.decompress() 解压
- 标签处理：创建文章时创建标签，创建关联

遵循 UserServiceImpl 的代码风格。
  </action>
  <verify>
    <automated>ls springboot/src/main/java/cn/coderstory/springboot/service/KnowledgeService.java springboot/src/main/java/cn/coderstory/springboot/service/impl/KnowledgeServiceImpl.java</automated>
  </verify>
  <done>
    KnowledgeService 接口和实现已创建
  </done>
</task>

<task type="auto">
  <name>task 4: 创建 KnowledgeController</name>
  <read_first>
    - springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java
  </read_first>
  <action>
创建 **KnowledgeController.java**：

RESTful API 设计（遵循 UserController 的响应格式）：

```
GET    /api/knowledge/categories/tree    - 获取分类树
GET    /api/knowledge/articles            - 分页查询文章（支持 keyword, categoryId, page, size）
GET    /api/knowledge/articles/{id}       - 获取文章详情
POST   /api/knowledge/articles            - 创建文章
PUT    /api/knowledge/articles/{id}       - 更新文章
DELETE /api/knowledge/articles/{id}       - 删除文章

GET    /api/knowledge/tags                - 获取所有标签
GET    /api/knowledge/articles/{id}/tags  - 获取文章标签

POST   /api/knowledge/files              - 上传文件（MultipartFile）
GET    /api/knowledge/files/{id}         - 下载文件
DELETE /api/knowledge/files/{id}        - 删除文件
```

Controller 返回 Map 格式：{code: 200, message: "success", data: ...}
文件下载使用 ResponseEntity<byte[]> 返回，设置正确的 Content-Type 和 Content-Disposition。

@PreAuthorize("isAuthenticated()") 保护所有接口。
  </action>
  <verify>
    <automated>ls springboot/src/main/java/cn/coderstory/springboot/controller/KnowledgeController.java</automated>
  </verify>
  <done>
    KnowledgeController 已创建，包含所有 RESTful API
  </done>
</task>

</tasks>

<verification>
- `mvn compile` 编译通过
- 所有 Mapper、Service、Controller 文件存在
</verification>

<success_criteria>
1. 4 个 Mapper 接口 + 1 个 Mapper XML 存在
2. ZstdUtil 工具类实现压缩/解压功能
3. KnowledgeService 接口和实现类完整
4. KnowledgeController 实现所有 RESTful API
5. pom.xml 包含 zstd-jni 依赖
</success_criteria>

<output>
After completion, create `.planning/phases/03-business-data/03-02-SUMMARY.md`
</output>
