---
phase: 03-business-data
plan: '03'
type: execute
wave: 3
depends_on:
  - '03-02'
files_modified:
  - app-vue/src/api/knowledge.js
  - app-vue/src/views/business/BusinessData.vue
  - app-vue/src/components/knowledge/CategoryTree.vue
  - app-vue/src/components/knowledge/ArticleEditor.vue
  - app-vue/package.json
autonomous: false
requirements:
  - BIZ-01
  - BIZ-02
  - BIZ-03
  - BIZ-04
  - BIZ-05
  - UI-06
---

<objective>
实现知识库前端界面，包含分类树、知识列表、富文本编辑器、文件管理、响应式布局。

Purpose: 提供完整的知识库用户界面，支持知识的增删改查和分类管理
Output: API模块 + 4个Vue组件
</objective>

<context>
@./app-vue/src/api/user.js
@./app-vue/src/views/system/UserManagement.vue
@./app-vue/src/views/business/BusinessData.vue
@./AGENTS.md

# 前端规范：
# 1. 使用 Vue 3 Composition API (<script setup>)
# 2. 使用 Element Plus 组件
# 3. API 调用遵循 user.js 的请求/响应模式
# 4. 富文本编辑器使用 Tiptap
</context>

<tasks>

<task type="auto">
  <name>task 1: 安装 Tiptap 依赖</name>
  <read_first>
    - app-vue/package.json
  </read_first>
  <action>
在 app-vue 目录安装 Tiptap 相关依赖：

```bash
cd app-vue
npm install @tiptap/vue-3@3.22.1 @tiptap/starter-kit@3.22.1 @tiptap/extension-image@3.22.1 @tiptap/markdown@3.22.1 @tiptap/pm@3.22.1
```

确保安装成功，检查 package.json dependencies 中是否包含这些包。
  </action>
  <verify>
    <automated>grep -c "@tiptap" app-vue/package.json</automated>
  </verify>
  <done>
    Tiptap 依赖已添加到 package.json
  </done>
</task>

<task type="auto">
  <name>task 2: 创建知识库 API 模块</name>
  <read_first>
    - app-vue/src/api/user.js
  </read_first>
  <action>
创建 **app-vue/src/api/knowledge.js**：

参考 user.js 的请求/响应模式，创建以下 API：

```javascript
import axios from 'axios'

const request = axios.create({
  baseURL: '/api',
  timeout: 10000
})
// ... 拦截器同 user.js

// 分类 API
export function getCategoryTree() {
  return request({ url: '/knowledge/categories/tree', method: 'get' })
}

// 文章 API
export function getArticlePage(params) {
  return request({ url: '/knowledge/articles', method: 'get', params })
}
export function getArticleDetail(id) {
  return request({ url: `/knowledge/articles/${id}`, method: 'get' })
}
export function createArticle(data) {
  return request({ url: '/knowledge/articles', method: 'post', data })
}
export function updateArticle(id, data) {
  return request({ url: `/knowledge/articles/${id}`, method: 'put', data })
}
export function deleteArticle(id) {
  return request({ url: `/knowledge/articles/${id}`, method: 'delete' })
}

// 标签 API
export function getAllTags() {
  return request({ url: '/knowledge/tags', method: 'get' })
}
export function getArticleTags(articleId) {
  return request({ url: `/knowledge/articles/${articleId}/tags`, method: 'get' })
}

// 文件 API
export function uploadFile(articleId, file) {
  const formData = new FormData()
  formData.append('file', file)
  formData.append('articleId', articleId)
  return request({ url: '/knowledge/files', method: 'post', data: formData })
}
export function downloadFile(fileId) {
  return request({ url: `/knowledge/files/${fileId}`, method: 'get', responseType: 'blob' })
}
export function deleteFile(fileId) {
  return request({ url: `/knowledge/files/${fileId}`, method: 'delete' })
}

export default request
```
  </action>
  <verify>
    <automated>ls app-vue/src/api/knowledge.js</automated>
  </verify>
  <done>
    knowledge.js API 模块已创建
  </done>
</task>

<task type="auto">
  <name>task 3: 创建 CategoryTree 组件</name>
  <read_first>
    - app-vue/src/views/system/UserManagement.vue
  </read_first>
  <action>
创建 **app-vue/src/components/knowledge/CategoryTree.vue**：

功能：左侧可折叠树形分类组件

```vue
<template>
  <div class="category-tree">
    <div class="tree-header">
      <span>分类</span>
      <el-button text @click="handleAddRoot">
        <el-icon><Plus /></el-icon>
      </el-button>
    </div>
    <el-tree
      :data="treeData"
      :props="{ label: 'name', children: 'children' }"
      node-key="id"
      :expand-on-click-node="false"
      :default-expand-all="true"
      @node-click="handleNodeClick"
    >
      <template #default="{ node, data }">
        <span class="tree-node">
          <span>{{ node.label }}</span>
          <span class="node-actions">
            <el-button text size="small" @click.stop="handleAddChild(data)">+</el-button>
            <el-button text size="small" type="danger" @click.stop="handleDelete(data)">×</el-button>
          </span>
        </span>
      </template>
    </el-tree>
  </div>
</template>
```

要求：
- 使用 el-tree 组件，支持展开/折叠
- 点击节点选中并加载该分类下的文章列表
- 显示新增子分类和删除按钮
- emit('select', category) 事件

响应式：在小屏幕（<768px）下隐藏或作为抽屉显示（按 UI-06 要求）。
  </action>
  <verify>
    <automated>ls app-vue/src/components/knowledge/CategoryTree.vue</automated>
  </verify>
  <done>
    CategoryTree.vue 组件已创建
  </done>
</task>

<task type="auto">
  <name>task 4: 创建 ArticleEditor 组件（富文本编辑器）</name>
  <read_first>
    - app-vue/src/views/system/UserManagement.vue
  </read_first>
  <action>
创建 **app-vue/src/components/knowledge/ArticleEditor.vue**：

功能：全屏弹窗中的 Tiptap 富文本编辑器

```vue
<template>
  <el-dialog
    v-model="visible"
    :title="isEdit ? '编辑知识' : '新建知识'"
    width="90%"
    fullscreen
    :close-on-click-modal="false"
  >
    <el-form :model="form" label-width="80px">
      <el-form-item label="分类">
        <el-select v-model="form.categoryId" placeholder="请选择分类" style="width: 100%">
          <el-option v-for="cat in flatCategories" :key="cat.id" :label="cat.name" :value="cat.id" />
        </el-select>
      </el-form-item>
      <el-form-item label="标题">
        <el-input v-model="form.title" placeholder="请输入标题" />
      </el-form-item>
      <el-form-item label="标签">
        <el-select v-model="form.tagIds" multiple placeholder="请选择标签" style="width: 100%">
          <el-option v-for="tag in allTags" :key="tag.id" :label="tag.name" :value="tag.id" />
        </el-select>
      </el-form-item>
      <el-form-item label="内容">
        <div class="editor-container">
          <editor-content :editor="editor" />
        </div>
      </el-form-item>
      <el-form-item label="附件">
        <el-upload
          :file-list="fileList"
          :on-remove="handleFileRemove"
          :before-upload="handleFileUpload"
        >
          <el-button>上传文件</el-button>
        </el-upload>
      </el-form-item>
    </el-form>
    <template #footer>
      <el-button @click="visible = false">取消</el-button>
      <el-button type="primary" @click="handleSave">保存</el-button>
    </template>
  </el-dialog>
</template>

<script setup>
import { useEditor, EditorContent } from '@tiptap/vue-3'
import StarterKit from '@tiptap/starter-kit'
import Image from '@tiptap/extension-image'
import Markdown from '@tiptap/markdown'

// props: modelValue (Boolean), articleId (Number)
// emits: update:modelValue, success
// 
// 编辑器配置使用 StarterKit + Image + Markdown
// contentType: 'markdown'
// onUpdate: 更新 form.content
</script>
```

要求：
- 使用 Tiptap 的 useEditor 和 EditorContent
- 支持 Markdown 格式
- 支持图片粘贴/上传（base64 或服务器上传）
- 弹窗全屏显示（符合 D-10）
- 支持选择分类、标签
- 支持上传附件
  </action>
  <verify>
    <automated>ls app-vue/src/components/knowledge/ArticleEditor.vue</automated>
  </verify>
  <done>
    ArticleEditor.vue 组件已创建，包含 Tiptap 富文本编辑器
  </done>
</task>

<task type="auto">
  <name>task 5: 重构 BusinessData.vue 主页面</name>
  <read_first>
    - app-vue/src/views/business/BusinessData.vue
    - app-vue/src/views/system/UserManagement.vue
  </read_first>
  <action>
重构 **app-vue/src/views/business/BusinessData.vue**，替换占位内容：

布局：左侧分类树 + 右侧知识列表（响应式）

```vue
<template>
  <div class="knowledge-layout">
    <!-- 移动端分类切换按钮 -->
    <el-button class="mobile-category-btn" @click="showMobileTree = true">
      选择分类
    </el-button>
    
    <!-- 左侧分类树 -->
    <div class="category-panel" :class="{ 'mobile-show': showMobileTree }">
      <CategoryTree @select="handleCategorySelect" />
      <el-button v-if="showMobileTree" text @click="showMobileTree = false">
        关闭
      </el-button>
    </div>
    
    <!-- 右侧知识列表 -->
    <div class="article-panel">
      <!-- 搜索栏 -->
      <div class="search-bar">
        <el-input
          v-model="searchKeyword"
          placeholder="搜索标题、内容、标签..."
          clearable
          @clear="handleSearch"
        >
          <template #append>
            <el-button @click="handleSearch">搜索</el-button>
          </template>
        </el-input>
        <el-button type="primary" @click="handleCreate">新建知识</el-button>
      </div>
      
      <!-- 知识列表 -->
      <el-table :data="articleList" stripe>
        <el-table-column prop="title" label="标题" min-width="200" />
        <el-table-column prop="categoryName" label="分类" width="120" />
        <el-table-column prop="tags" label="标签" width="150">
          <template #default="{ row }">
            <el-tag v-for="tag in row.tags" :key="tag" size="small">{{ tag }}</el-tag>
          </template>
        </el-table-column>
        <el-table-column prop="createTime" label="创建时间" width="160" />
        <el-table-column label="操作" width="150" fixed="right">
          <template #default="{ row }">
            <el-button link type="primary" size="small" @click="handleEdit(row)">编辑</el-button>
            <el-button link type="danger" size="small" @click="handleDelete(row)">删除</el-button>
          </template>
        </el-table-column>
      </el-table>
      
      <!-- 分页 -->
      <el-pagination
        v-model:current-page="pagination.page"
        v-model:page-size="pagination.size"
        :page-sizes="[10, 20, 50]"
        :total="pagination.total"
        layout="total, sizes, prev, pager, next"
        @size-change="loadArticles"
        @current-change="loadArticles"
      />
    </div>
    
    <!-- 编辑弹窗 -->
    <ArticleEditor
      v-model="editorVisible"
      :article-id="currentArticleId"
      @success="loadArticles"
    />
  </div>
</template>
```

响应式处理（UI-06）：
- 桌面端：左侧分类树固定宽度，右侧列表自适应
- 移动端（<768px）：分类树隐藏，点击按钮显示为抽屉

点击知识行打开编辑（覆盖当前页签内容，符合 D-09）。
  </action>
  <verify>
    <automated>ls app-vue/src/views/business/BusinessData.vue</automated>
  </verify>
  <done>
    BusinessData.vue 已重构为完整的知识库界面
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>task 6: 验证知识库功能</name>
  <what-built>完整的知识库系统：后端 API + 前端界面 + Tiptap 编辑器</what-built>
  <how-to-verify>
    1. 启动后端服务：`cd springboot && mvnw.cmd spring-boot:run`
    2. 启动前端服务：`cd app-vue && npm run dev`
    3. 访问 http://localhost:5173/login，使用 admin/admin123 登录
    4. 点击"业务数据" -> "数据列表"
    5. 测试分类树：新增分类、删除分类
    6. 测试知识列表：搜索、新建、编辑、删除
    7. 测试富文本编辑器：输入 Markdown 内容，保存后显示
    8. 测试响应式：缩小窗口到移动端宽度，验证布局变化
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述问题</resume-signal>
</task>

</tasks>

<verification>
- npm run build 在 app-vue 目录执行成功
- 所有 Vue 组件文件存在
- API 模块正确导出所有函数
</verification>

<success_criteria>
1. Tiptap 依赖已安装
2. knowledge.js API 模块完整
3. CategoryTree.vue 可折叠树形组件
4. ArticleEditor.vue 全屏富文本编辑器
5. BusinessData.vue 响应式布局主页面
6. 移动端适配（<768px）分类树显示为抽屉
</success_criteria>

<output>
After completion, create `.planning/phases/03-business-data/03-03-SUMMARY.md`
</output>
