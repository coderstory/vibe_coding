---
phase: 32-sse前端实时推送
plan: 03
type: execute
wave: 2
depends_on:
  - 32-02-PLAN
files_modified:
  - app-vue/src/views/monitor/hardware/HardwareMonitorPage.vue
  - app-vue/src/views/monitor/hardware/CpuGauge.vue
  - app-vue/src/views/monitor/hardware/MemoryGauge.vue
  - app-vue/src/views/monitor/hardware/DiskPartitions.vue
  - app-vue/src/views/monitor/hardware/SystemInfo.vue
  - app-vue/src/router/modules/routes.ts
  - springboot/src/main/resources/db/migration/V25__hardware_monitor_menu.sql
autonomous: true
requirements:
  - HWM-12
  - HWM-13
  - HWM-14
  - HWM-15
  - HWM-17

must_haves:
  truths:
    - "用户通过侧边栏菜单或直接输入 URL 访问 /monitor/hardware 后看到硬件监控页面"
    - "页面左侧显示指标类别导航菜单（CPU/内存/磁盘/系统信息），点击切换右侧内容"
    - "CPU 面板显示 ECharts 环形图展示使用率，下方信息卡片显示型号/核心数/频率/架构"
    - "内存面板显示 ECharts 环形图展示使用率，下方信息卡片显示总量/已用/可用"
    - "磁盘面板以 El-Progress 进度条列表展示每个分区的容量使用情况"
    - "系统信息面板以标签-值对展示 OS 版本、运行时间、进程数、系统架构"
    - "硬件监控页面通过 useHardwareMetrics() composable 连接 SSE，数据到达即 UI 更新"
    - "页面卸载时自动调用 disconnect() 清理 SSE 连接"
  artifacts:
    - path: "app-vue/src/views/monitor/hardware/HardwareMonitorPage.vue"
      provides: "SSE composable 编排 + 左导航 + 右内容区布局"
      min_lines: 100
    - path: "app-vue/src/views/monitor/hardware/CpuGauge.vue"
      provides: "CPU 环形图（ECharts PieChart）+ 处理器信息卡片"
      min_lines: 80
    - path: "app-vue/src/views/monitor/hardware/MemoryGauge.vue"
      provides: "内存环形图（ECharts PieChart）+ 内存信息卡片"
      min_lines: 80
    - path: "app-vue/src/views/monitor/hardware/DiskPartitions.vue"
      provides: "磁盘分区 El-Progress 进度条列表"
      min_lines: 60
    - path: "app-vue/src/views/monitor/hardware/SystemInfo.vue"
      provides: "系统信息标签-值展示卡片"
      min_lines: 50
    - path: "app-vue/src/router/modules/routes.ts"
      provides: "/monitor/hardware 路由注册"
      contains: "HardwareMonitor"
    - path: "springboot/src/main/resources/db/migration/V25__hardware_monitor_menu.sql"
      provides: "硬件监控侧边栏菜单注册"
      contains: "hardware_monitor_menu"
  key_links:
    - from: "HardwareMonitorPage.vue"
      to: "composables/useHardwareMetrics"
      pattern: "import.*useHardwareMetrics"
    - from: "HardwareMonitorPage.vue"
      to: "api/modules/hardware"
      pattern: "import.*HardwareMetricsDTO"
    - from: "CpuGauge.vue"
      to: "ECharts (PieChart)"
      pattern: "use.*\\[.*PieChart"
    - from: "MemoryGauge.vue"
      to: "ECharts (PieChart)"
      pattern: "use.*\\[.*PieChart"
    - from: "DiskPartitions.vue"
      to: "Element Plus El-Progress"
      pattern: "el-progress"
---

<objective>
创建前端硬件监控页面，包含左导航布局、CPU/内存环形图、磁盘分区进度条和系统信息卡片。

**Purpose:** 实现前端页面路由（HWM-12）、CPU 环形图（HWM-13）、内存环形图（HWM-14）、磁盘进度条（HWM-15）、系统信息卡片（HWM-17）。

**Output:**
- `HardwareMonitorPage.vue` — 主页面（SSE composable 编排 + 左导航 + 右内容区）
- `CpuGauge.vue` — CPU 环形图 + 处理器信息卡片
- `MemoryGauge.vue` — 内存环形图 + 内存信息卡片
- `DiskPartitions.vue` — 磁盘分区 El-Progress 进度条列表
- `SystemInfo.vue` — 系统信息标签-值展示
- `routes.ts` — 新增 `/monitor/hardware` 路由
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-sse前端实时推送/32-CONTEXT.md
@.planning/phases/32-sse前端实时推送/32-RESEARCH.md
@.planning/phases/32-sse前端实时推送/32-UI-SPEC.md
@.planning/phases/32-sse前端实时推送/32-01-SUMMARY.md (after Plan 01 completes)
@.planning/phases/32-sse前端实时推送/32-02-SUMMARY.md (after Plan 02 completes)

<interfaces>
From app-vue/src/composables/useHardwareMetrics.ts (created in Plan 02):
```typescript
export function useHardwareMetrics(options: UseHardwareMetricsOptions = {}) {
  return { connect, disconnect, reconnect, connected, metrics, error }
}
// Usage:
// const { connect, disconnect, connected, metrics, error } = useHardwareMetrics()
// connect() on mount, disconnect() on unmount
// metrics.value -> HardwareMetricsDTO | null
// connected.value -> boolean
// error.value -> string | null
```

From app-vue/src/api/modules/hardware.ts (created in Plan 02):
```typescript
export interface HardwareMetricsDTO {
  timestamp: number
  cpu: CpuMetricsDTO | null
  memory: MemoryMetricsDTO | null
  disks: DiskMetricsDTO[] | null
  network: NetworkMetricsDTO[] | null
  system: SystemInfoDTO | null
}

// Sub-types: CpuMetricsDTO (systemLoad, perCoreLoad, logicalCores, physicalCores, processorName, processorFrequency, cpu64bit)
// MemoryMetricsDTO (total: MetricValue, available: MetricValue, used: MetricValue, usagePercent: MetricValue)
// DiskMetricsDTO (name, model, size: MetricValue, mount, totalSpace: MetricValue, usedSpace: MetricValue, usableSpace: MetricValue)
// SystemInfoDTO (osFamily, osVersion, osVersionInfo, systemUptime, systemUptimeFormatted, processCount)
```

From app-vue/src/router/modules/routes.ts (existing route pattern):
```typescript
{
  path: '/monitor',
  name: 'MonitorDashboard',
  component: () => import('@/views/monitor/MonitorDashboard.vue'),
  meta: { title: '监控大盘', requiresAuth: true }
},
```

From app-vue/src/views/rocketmq/QpsChart.vue (ECharts vue-echarts pattern):
```typescript
import VChart from 'vue-echarts'
import { use } from 'echarts/core'
import { CanvasRenderer } from 'echarts/renderers'
import { LineChart } from 'echarts/charts'
import { GridComponent, LegendComponent, TooltipComponent } from 'echarts/components'

use([CanvasRenderer, LineChart, GridComponent, TooltipComponent, LegendComponent])
```

Existing page layout pattern from MonitorDashboard.vue:
```vue
<template>
  <div class="monitor-dashboard">
    <el-card v-loading="loading">
      <template #header><span>监控大盘</span></template>
      <el-row :gutter="20"> ... </el-row>
    </el-card>
  </div>
</template>
<style scoped>
.monitor-dashboard { padding: 20px; }
</style>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 HardwareMonitorPage.vue + 注册路由</name>
  <files>
    app-vue/src/views/monitor/hardware/HardwareMonitorPage.vue
    app-vue/src/router/modules/routes.ts
  </files>
  <read_first>
    app-vue/src/router/modules/routes.ts
    app-vue/src/views/monitor/MonitorDashboard.vue
  </read_first>
  <action>
    创建页面主组件并在路由中注册。

    **Part A — 创建 HardwareMonitorPage.vue：**

    ```vue
    <script lang="ts">
    /**
     * 硬件监控页面组件。左导航 + 右内容区布局，通过 SSE 实时接收硬件指标数据。
     * 数据到达即 UI 更新（D-04），组件卸载时自动断开 SSE（D-07）。
     */
    </script>

    <script lang="ts" setup>
    import { onMounted, ref } from 'vue'
    import { useHardwareMetrics } from '@/composables/useHardwareMetrics'
    import { Cpu, DataBoard, Disk, InfoFilled } from '@element-plus/icons-vue'
    import CpuGauge from './CpuGauge.vue'
    import MemoryGauge from './MemoryGauge.vue'
    import DiskPartitions from './DiskPartitions.vue'
    import SystemInfo from './SystemInfo.vue'

    const { connect, disconnect, connected, metrics } = useHardwareMetrics()

    const activeSection = ref('cpu')

    const navItems = [
      { key: 'cpu', label: 'CPU', icon: Cpu },
      { key: 'memory', label: '内存', icon: DataBoard },
      { key: 'disk', label: '磁盘', icon: Disk },
      { key: 'system', label: '系统信息', icon: InfoFilled }
    ]

    function handleNavSelect(key: string) {
      activeSection.value = key
    }

    onMounted(() => {
      connect()
    })

    // disconnect() 在 composable 内部通过 onUnmounted 自动调用
    </script>

    <template>
      <div class="hardware-monitor-page">
        <div class="hw-layout">
          <!-- 左侧导航 -->
          <div class="hw-nav">
            <el-menu
              :default-active="activeSection"
              @select="handleNavSelect"
              class="hw-nav-menu"
            >
              <el-menu-item v-for="item in navItems" :key="item.key" :index="item.key">
                <el-icon><component :is="item.icon" /></el-icon>
                <span>{{ item.label }}</span>
              </el-menu-item>
            </el-menu>
          </div>

          <!-- 右侧内容区 -->
          <div class="hw-content">
            <!-- SSE 连接指示器 -->
            <div class="hw-connection-indicator">
              <span
                class="hw-status-dot"
                :class="connected ? 'hw-status-dot--connected' : 'hw-status-dot--disconnected'"
                :title="connected ? '已连接' : '连接已断开'"
              />
              <span class="hw-status-text">{{ connected ? '已连接' : '连接已断开' }}</span>
            </div>

            <!-- CPU 面板 -->
            <div v-show="activeSection === 'cpu'" class="hw-section">
              <CpuGauge :cpu-data="metrics?.cpu ?? null" :connected="connected" />
            </div>

            <!-- 内存面板 -->
            <div v-show="activeSection === 'memory'" class="hw-section">
              <MemoryGauge :memory-data="metrics?.memory ?? null" :connected="connected" />
            </div>

            <!-- 磁盘面板 -->
            <div v-show="activeSection === 'disk'" class="hw-section">
              <DiskPartitions :disks="metrics?.disks ?? null" :connected="connected" />
            </div>

            <!-- 系统信息面板 -->
            <div v-show="activeSection === 'system'" class="hw-section">
              <SystemInfo :system-data="metrics?.system ?? null" />
            </div>
          </div>
        </div>
      </div>
    </template>

    <style scoped>
    .hardware-monitor-page {
      padding: 24px;
      height: 100%;
    }

    .hw-layout {
      display: flex;
      gap: 24px;
      height: 100%;
    }

    .hw-nav {
      flex: 0 0 180px;
      background: var(--ocean-pale, #dbeafe);
      border-radius: 8px;
      padding: 8px 0;
    }

    .hw-nav-menu {
      border-right: none;
      background: transparent;
    }

    .hw-nav-menu .el-menu-item {
      height: 44px;
      padding-left: 16px;
      border-radius: 6px;
      margin: 2px 8px;
    }

    .hw-nav-menu .el-menu-item.is-active {
      background: rgba(253, 230, 138, 0.6);
      color: var(--ocean-deep, #1e3a8a);
      font-weight: 600;
    }

    .hw-content {
      flex: 1;
      min-width: 0;
    }

    .hw-connection-indicator {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-bottom: 16px;
      font-size: 13px;
      color: var(--el-text-color-secondary, #64748b);
    }

    .hw-status-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      display: inline-block;
    }

    .hw-status-dot--connected {
      background-color: var(--el-color-success, #10b981);
    }

    .hw-status-dot--disconnected {
      background-color: var(--el-color-warning, #d97706);
    }

    .hw-status-text {
      vertical-align: middle;
    }

    .hw-section {
      animation: hw-fade-in 0.3s ease;
    }

    @keyframes hw-fade-in {
      from { opacity: 0; transform: translateY(8px); }
      to { opacity: 1; transform: translateY(0); }
    }
    </style>
    ```

    **Part B — 修改 routes.ts：**

    在 `routes.ts` 中 `/monitor` 路由条目**之后**（在 "监控面板" RocketMQ Dashboard 条目之后，约 line 201 处）添加新的子路由条目：

    ```typescript
    // 硬件监控（需要认证）
    {
      path: '/monitor/hardware',
      name: 'HardwareMonitor',
      component: () => import('@/views/monitor/hardware/HardwareMonitorPage.vue'),
      meta: { title: '硬件监控', requiresAuth: true }
    },
    ```

    注意：`/monitor/hardware` 作为根布局 `/` 下的独立子路由（与 `/monitor` 同级），路径嵌套只用于 URL 结构。route name 使用 `HardwareMonitor`。

    约束：
    - `HardwareMonitorPage.vue` 不需要手动调用 `disconnect()` — composable 的 `onUnmounted(disconnect)` 已处理
    - `activeSection` 默认 `'cpu'`，切换时使用 `v-show` 保留组件状态（不销毁重建）
    - 连接指示器显示在内容区顶部：绿点（已连接）/ 橙点（断开），带文本标签
    - 注释规范：L2 级（鼓励）— 组件职责 JSDoc，`navItems` 行内注释说明
  </action>
  <verify>
    <automated>cd app-vue && npx vue-tsc --noEmit 2>&1 | head -20</automated>
    <criteria>
      - TypeScript 类型检查通过，无错误
    </criteria>
  </verify>
  <acceptance_criteria>
    - 文件 `app-vue/src/views/monitor/hardware/HardwareMonitorPage.vue` 存在
    - 文件 `app-vue/src/router/modules/routes.ts` 包含 `HardwareMonitor` 路由（grep 确认）
    - 路由 path 包含 `/monitor/hardware`
    - 页面组件通过 `onMounted` 调用 `connect()`（grep 确认）
    - `npx vue-tsc --noEmit` 通过
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 创建 CpuGauge.vue 和 MemoryGauge.vue 环形图组件</name>
  <files>
    app-vue/src/views/monitor/hardware/CpuGauge.vue
    app-vue/src/views/monitor/hardware/MemoryGauge.vue
  </files>
  <read_first>
    app-vue/src/views/rocketmq/QpsChart.vue
  </read_first>
  <action>
    创建两个 ECharts 环形图组件，遵循 RESEARCH.md Pattern 4（PieChart radius: ['55%', '75%']）。

    **CpuGauge.vue 规范（遵循 UI-SPEC §CpuGauge.vue）：**

    ```vue
    <script lang="ts">
    /**
     * CPU 使用率仪表盘组件。ECharts 环形图展示总体使用率，信息卡片展示处理器详情。
     * 使用 vue-echarts VChart 组件，PieChart 实现环形图效果。
     * 数据通过 SSE 实时推送更新，animationDurationUpdate: 500 平滑过渡。
     */
    </script>

    <script lang="ts" setup>
    import VChart from 'vue-echarts'
    import { use } from 'echarts/core'
    import { CanvasRenderer } from 'echarts/renderers'
    import { PieChart } from 'echarts/charts'
    import { computed } from 'vue'
    import type { CpuMetricsDTO } from '@/api/modules/hardware'

    use([CanvasRenderer, PieChart])

    const props = defineProps<{
      cpuData: CpuMetricsDTO | null
      connected: boolean
    }>()

    const usagePercent = computed(() => {
      if (!props.cpuData) return 0
      return Math.round(props.cpuData.systemLoad * 10) / 10
    })

    const chartOption = computed(() => ({
      series: [{
        type: 'pie',
        radius: ['55%', '75%'],
        avoidLabelOverlap: false,
        animationDurationUpdate: 500,
        label: {
          show: true,
          position: 'center',
          formatter: () => `${usagePercent.value}%`,
          fontSize: 28,
          fontWeight: 700,
          color: '#1e3a8a'
        },
        emphasis: { label: { show: true } },
        data: [
          {
            value: usagePercent.value,
            name: '已使用',
            itemStyle: { color: '#3b82f6' }
          },
          {
            value: Math.max(100 - usagePercent.value, 0),
            name: '剩余',
            itemStyle: { color: '#f0f9ff' }
          }
        ]
      }]
    }))

    // 格式化处理器频率：Hz -> GHz
    const frequencyFormatted = computed(() => {
      if (!props.cpuData) return '-'
      const freq = props.cpuData.processorFrequency
      if (freq >= 1_000_000_000) return (freq / 1_000_000_000).toFixed(2) + ' GHz'
      if (freq >= 1_000_000) return (freq / 1_000_000).toFixed(0) + ' MHz'
      return freq + ' Hz'
    })
    </script>

    <template>
      <div class="cpu-gauge">
        <el-card shadow="never">
          <template #header>
            <span class="section-title">CPU</span>
          </template>
          <el-row :gutter="24" align="middle">
            <el-col :span="8">
              <div class="chart-container" style="height: 180px; width: 180px; margin: 0 auto;">
                <v-chart :option="chartOption" autoresize style="height: 100%; width: 100%;" />
              </div>
            </el-col>
            <el-col :span="16">
              <div class="info-list">
                <div class="info-item">
                  <span class="info-label">型号</span>
                  <span class="info-value">{{ cpuData?.processorName || '暂无数据' }}</span>
                </div>
                <div class="info-item">
                  <span class="info-label">物理核心</span>
                  <span class="info-value">{{ cpuData?.physicalCores ?? '-' }}</span>
                </div>
                <div class="info-item">
                  <span class="info-label">逻辑核心</span>
                  <span class="info-value">{{ cpuData?.logicalCores ?? '-' }}</span>
                </div>
                <div class="info-item">
                  <span class="info-label">频率</span>
                  <span class="info-value">{{ frequencyFormatted }}</span>
                </div>
                <div class="info-item">
                  <span class="info-label">架构</span>
                  <span class="info-value">{{ cpuData?.cpu64bit ? '64-bit' : '32-bit' }}</span>
                </div>
              </div>
            </el-col>
          </el-row>
        </el-card>
      </div>
    </template>

    <style scoped>
    .cpu-gauge { margin-bottom: 16px; }
    .section-title { font-size: 16px; font-weight: 600; color: var(--ocean-deep, #1e3a8a); }
    .info-list { display: flex; flex-direction: column; gap: 12px; }
    .info-item { display: flex; align-items: center; }
    .info-label { width: 100px; font-size: 13px; color: var(--el-text-color-secondary, #64748b); flex-shrink: 0; }
    .info-value { font-size: 14px; font-weight: 600; color: var(--el-text-color-primary, #1e293b); }
    </style>
    ```

    **MemoryGauge.vue 规范（遵循 UI-SPEC §MemoryGauge.vue）：**

    与 CpuGauge.vue 结构相同，但：
    - props 不同：`memoryData: MemoryMetricsDTO | null`
    - 信息卡片展示：总量、已用、可用（从 `MemoryMetricsDTO` 的 `total.format` / `used.format` / `available.format` 读取）
    - chart center 子标签显示"内存"而非"CPU"
    - 数据来源：`memoryData?.usagePercent.value` 作为环形图百分比
    - 信息卡片字段：
      - `总量` → `memoryData?.total?.format || '-'`
      - `已用` → `memoryData?.used?.format || '-'`
      - `可用` → `memoryData?.available?.format || '-'`
    - 标题：`<span class="section-title">内存</span>`
    - 英文注释文本使用"内存"替代"Memory"

    约束：
    - 两个组件都使用 `use([CanvasRenderer, PieChart])` 注册 ECharts 模块
    - ECharts `radius: ['55%', '75%']` 实现环形图效果
    - 已使用色：`#3b82f6`（ocean-medium），剩余色：`#f0f9ff`（ocean-pale）
    - 中心标签 28px bold，参考 UI-SPEC 副本约定
    - 注释规范：L2 级（鼓励）— 组件职责 JSDoc，关键 computed 属性行内注释
  </action>
  <verify>
    <automated>cd app-vue && npx vue-tsc --noEmit 2>&1 | head -20</automated>
    <criteria>
      - TypeScript 类型检查通过
    </criteria>
  </verify>
  <acceptance_criteria>
    - `CpuGauge.vue` 存在，接收 `cpuData: CpuMetricsDTO | null` 和 `connected: boolean` props
    - `MemoryGauge.vue` 存在，接收 `memoryData: MemoryMetricsDTO | null` 和 `connected: boolean` props
    - 两个组件都使用 `use([CanvasRenderer, PieChart])` 注册 ECharts
    - 两个组件都包含 `<v-chart :option="chartOption"` 标签
    - `npx vue-tsc --noEmit` 通过
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 创建 DiskPartitions.vue 和 SystemInfo.vue</name>
  <files>
    app-vue/src/views/monitor/hardware/DiskPartitions.vue
    app-vue/src/views/monitor/hardware/SystemInfo.vue
  </files>
  <read_first>
    app-vue/src/api/modules/hardware.ts (created in Plan 02)
  </read_first>
  <action>
    创建磁盘分区进度条列表和系统信息卡片。

    **DiskPartitions.vue 规范（遵循 UI-SPEC §DiskPartitions.vue）：**

    ```vue
    <script lang="ts">
    /**
     * 磁盘分区容量进度条列表组件。使用 Element Plus El-Progress 展示每个分区使用率。
     * 使用率 >= 90% 时显示异常状态（红色），>= 80% 时显示警告状态（琥珀色）。
     */
    </script>

    <script lang="ts" setup>
    import { computed } from 'vue'
    import type { DiskMetricsDTO } from '@/api/modules/hardware'

    const props = defineProps<{
      disks: DiskMetricsDTO[] | null
      connected: boolean
    }>()

    function diskPercentage(disk: DiskMetricsDTO): number {
      if (!disk.totalSpace?.value || !disk.usedSpace?.value) return 0
      return Math.round((disk.usedSpace.value / disk.totalSpace.value) * 100)
    }

    function diskStatus(pct: number): 'exception' | 'warning' | '' {
      if (pct >= 90) return 'exception'
      if (pct >= 80) return 'warning'
      return ''
    }
    </script>

    <template>
      <div class="disk-partitions">
        <el-card shadow="never">
          <template #header>
            <span class="section-title">磁盘分区</span>
          </template>
          <div v-if="!disks || disks.length === 0" class="empty-state">
            <span>暂无磁盘信息</span>
          </div>
          <div v-else v-for="disk in disks" :key="disk.name" class="disk-card">
            <div class="disk-header">
              <span class="disk-name">{{ disk.name || disk.mount }}</span>
              <span class="disk-model">{{ disk.model || '' }}</span>
            </div>
            <el-progress
              :percentage="diskPercentage(disk)"
              :text-inside="true"
              :stroke-width="22"
              :status="diskStatus(diskPercentage(disk))"
            />
            <div class="disk-details">
              <span>已用: {{ disk.usedSpace?.format || '-' }}</span>
              <span>可用: {{ disk.usableSpace?.format || '-' }}</span>
              <span>总量: {{ disk.totalSpace?.format || '-' }}</span>
            </div>
          </div>
        </el-card>
      </div>
    </template>

    <style scoped>
    .disk-partitions { margin-bottom: 16px; }
    .section-title { font-size: 16px; font-weight: 600; color: var(--ocean-deep, #1e3a8a); }
    .disk-card { margin-bottom: 16px; }
    .disk-card:last-child { margin-bottom: 0; }
    .disk-header { display: flex; align-items: baseline; gap: 8px; margin-bottom: 8px; }
    .disk-name { font-size: 14px; font-weight: 600; color: var(--el-text-color-primary, #1e293b); }
    .disk-model { font-size: 12px; color: var(--el-text-color-secondary, #64748b); }
    .disk-details { display: flex; gap: 16px; margin-top: 6px; font-size: 13px; color: var(--el-text-color-secondary, #64748b); }
    .empty-state { display: flex; align-items: center; justify-content: center; height: 100px; color: var(--el-text-color-placeholder, #c0c4cc); }
    </style>
    ```

    **SystemInfo.vue 规范（遵循 UI-SPEC §SystemInfo.vue）：**

    ```vue
    <script lang="ts">
    /**
     * 系统信息展示卡片组件。使用自定义 CSS Grid 布局展示标签-值对。
     * 展示操作系统版本、运行时间、进程数和系统架构。
     */
    </script>

    <script lang="ts" setup>
    import type { SystemInfoDTO } from '@/api/modules/hardware'

    defineProps<{
      systemData: SystemInfoDTO | null
    }>()
    </script>

    <template>
      <div class="system-info">
        <el-card shadow="never">
          <template #header>
            <span class="section-title">系统信息</span>
          </template>
          <div v-if="!systemData" class="empty-state">
            <span>暂无系统信息</span>
          </div>
          <div v-else class="info-grid">
            <div class="info-row">
              <span class="info-label">操作系统版本</span>
              <span class="info-value">{{ systemData.osFamily }} {{ systemData.osVersion }} {{ systemData.osVersionInfo || '' }}</span>
            </div>
            <div class="info-row">
              <span class="info-label">运行时间</span>
              <span class="info-value">{{ systemData.systemUptimeFormatted || '-' }}</span>
            </div>
            <div class="info-row">
              <span class="info-label">进程数</span>
              <span class="info-value">{{ systemData.processCount ?? '-' }} 个进程</span>
            </div>
            <div class="info-row" v-if="systemData.processCount">
              <span class="info-label">系统架构</span>
              <span class="info-value">64-bit</span>
            </div>
          </div>
        </el-card>
      </div>
    </template>

    <style scoped>
    .system-info { margin-bottom: 16px; }
    .section-title { font-size: 16px; font-weight: 600; color: var(--ocean-deep, #1e3a8a); }
    .info-grid { display: flex; flex-direction: column; gap: 12px; }
    .info-row { display: flex; align-items: center; }
    .info-label { width: 100px; font-size: 14px; color: var(--el-text-color-secondary, #64748b); flex-shrink: 0; }
    .info-value { font-size: 14px; font-weight: 500; color: var(--el-text-color-primary, #1e293b); }
    .empty-state { display: flex; align-items: center; justify-content: center; height: 100px; color: var(--el-text-color-placeholder, #c0c4cc); }
    </style>
    ```

    约束：
    - DiskPartitions.vue 使用 `el-progress` 组件（text-inside + stroke-width=22）
    - 使用率 >= 90% 返回 `exception` 状态，>= 80% 返回 `warning` 状态（使用 `diskStatus()` 函数计算）
    - SystemInfo.vue 使用自定义 CSS grid（非 `el-descriptions`），标签列固定 100px
    - 两个组件都有空数据状态（"暂无磁盘信息"/"暂无系统信息"）
    - 注释规范：L2 级（鼓励）— 组件职责 JSDoc，关键辅助函数行内注释
  </action>
  <verify>
    <automated>cd app-vue && npx vue-tsc --noEmit 2>&1 | head -20</automated>
    <criteria>
      - TypeScript 类型检查通过
    </criteria>
  </verify>
  <acceptance_criteria>
    - `DiskPartitions.vue` 存在，接收 `disks: DiskMetricsDTO[] | null` 和 `connected: boolean` props
    - `SystemInfo.vue` 存在，接收 `systemData: SystemInfoDTO | null` props
    - `DiskPartitions.vue` 包含 `<el-progress` 标签和 `:status` 绑定
    - `SystemInfo.vue` 包含 `.info-grid` 布局
    - 两个组件都处理 null 数据（显示"暂无"占位文本）
    - `npx vue-tsc --noEmit` 通过
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: 创建 Flyway 迁移脚本添加硬件监控菜单</name>
  <files>
    springboot/src/main/resources/db/migration/V25__hardware_monitor_menu.sql
  </files>
  <read_first>
    springboot/src/main/resources/db/migration/V24__rocketmq_dashboard_menu.sql
  </read_first>
  <action>
    创建 Flyway 迁移脚本，在监控大盘菜单（id=14）下添加硬件监控子菜单。

    文件 `V25__hardware_monitor_menu.sql`：

    ```sql
    -- Flyway 迁移脚本 V25 - 硬件监控页面菜单
    -- 添加硬件监控菜单位于监控大盘父菜单下

    USE admin_system;

    -- 创建硬件监控子菜单（id=25）
    -- 监控大盘父菜单已存在（id=14）
    INSERT IGNORE INTO sys_menu (id, parent_id, name, path, icon, sort_order) VALUES
    (25, 14, '硬件监控', '/monitor/hardware', 'Monitor', 1);

    -- 为超级管理员角色分配新菜单权限
    INSERT IGNORE INTO sys_role_menu (role_id, menu_id) VALUES
    (1, 25);
    ```

    约束：
    - 使用 `INSERT IGNORE INTO` 保证幂等性（防止重复执行）
    - 菜单 icon 使用 `Monitor`（Element Plus 图标）
    - sort_order=1 作为监控大盘下第一个子菜单
    - 注释规范：L3 级（强制）— 文件级 SQL 注释说明迁移目的
  </action>
  <verify>
    <automated>cd springboot && ./gradlew.bat flywayMigrate -i 2>&1 | tail -10</automated>
    <criteria>
      - Flyway 迁移成功应用
    </criteria>
  </verify>
  <acceptance_criteria>
    - 文件 `springboot/src/main/resources/db/migration/V25__hardware_monitor_menu.sql` 存在
    - 文件包含 `INSERT IGNORE INTO sys_menu` 语句（grep 确认）
    - 文件包含 `INSERT IGNORE INTO sys_role_menu` 语句（grep 确认）
    - parent_id 为 14（监控大盘菜单 ID）
    - path 包含 `/monitor/hardware`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> SSE EventSource | EventSource 请求到后端 SSE 端点 |
| Vue SPA -> Route navigation | 用户通过路由导航到硬件监控页面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Denial of Service | SSE EventSource 连接 | accept | 指数退避限制重连频率，onUnmounted 确保卸载时断开 |
| T-32-07 | Information Disclosure | 环形图展示 | accept | 硬件指标非敏感数据 |
</threat_model>

<verification>
1. 类型检查：`cd app-vue && npx vue-tsc --noEmit`
2. 构建验证：`cd app-vue && npm run build`
</verification>

<success_criteria>
1. `/monitor/hardware` 路由注册完成，可通过浏览器导航访问
2. 硬件监控菜单项（V25 Flyway 迁移）成功应用，侧边栏可看到"硬件监控"菜单
3. HardwareMonitorPage 包含左导航 + 右内容区（D-05），四个导航项可切换面板
4. CpuGauge 显示 ECharts 环形图（使用率）+ 处理器信息卡片（型号/核心/频率/架构）
5. MemoryGauge 显示 ECharts 环形图（使用率）+ 内存详情（总量/已用/可用）
6. DiskPartitions 以 El-Progress 进度条列表展示各分区容量（>=90% 红色，>=80% 橙色）
7. SystemInfo 以标签-值对展示 OS/运行时间/进程数/架构
8. 页面挂载时自动建立 SSE 连接，卸载时自动断开
9. CSS 变量引用 oceon-pale/ocean-deep/ocean-medium 等主题变量
10. `npm run build` 构建成功
</success_criteria>

<output>
After completion, create `.planning/phases/32-sse前端实时推送/32-03-SUMMARY.md`
</output>
