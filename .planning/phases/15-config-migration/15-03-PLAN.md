---
phase: 15-config-migration
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - springboot/src/main/resources/application.yaml
autonomous: true
requirements:
  - CONF-06

must_haves:
  truths:
    - "确认 management.httpexchanges.recording.include 默认值变更状态"
    - "如需要迁移，配置已更新"
  artifacts:
    - path: "springboot/src/main/resources/application.yaml"
      provides: "HTTP exchanges 监控配置（如果需要）"
      contains: "httpexchanges"

objectives:
  - 检查 Spring Boot 4.1 中 management.httpexchanges.recording.include 默认值变更
  - 验证是否需要在 application.yaml 中显式配置
---

<objective>
检查 Spring Boot 4.1 中 management.httpexchanges.recording.include 默认值变更（CONF-06）。验证 HTTP exchanges 监控配置是否需要调整。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 检查 httpexchanges 配置现状</name>
  <files>springboot/src/main/resources/application.yaml</files>
  <action>
1. 检查 application.yaml 中是否已有 management.httpexchanges 配置:

搜索以下模式:
- `management.httpexchanges`
- `httpexchanges.recording`
- `recording.include`

2. 在 springboot/src/main/java 中搜索 HttpExchange 注解使用:
- 搜索 `@HttpExchange` 注解
- 搜索 `recorded` 参数使用

3. 了解 Spring Boot 4.1 默认值变更:
- Spring Boot 3.x 默认: `management.httpexchanges.recording.include=ANNOTATED`
- Spring Boot 4.x 可能变更了默认值
- 如果项目使用 @HttpExchange 注解，需要确保配置正确
</action>
  <read_first>
springboot/src/main/resources/application.yaml
</read_first>
  <acceptance_criteria>
1. 确认 application.yaml 中是否已有 httpexchanges 配置
2. 确认项目中 @HttpExchange 注解的使用情况
3. 评估是否需要添加或修改配置
</acceptance_criteria>
  <done>
httpexchanges 配置现状已检查
</done>
</task>

<task type="auto">
  <name>Task 2: 验证/添加 httpexchanges 配置</name>
  <files>springboot/src/main/resources/application.yaml</files>
  <action>
基于 Task 1 的检查结果:

A. 如果发现 @HttpExchange 使用但无配置:
   - 添加显式配置以确保行为一致:
   ```yaml
   management:
     httpexchanges:
       recording:
         include: ANNOTATED
         max-age: 30
     endpoints:
       web:
         exposure:
           include: health,info,httpexchanges
   ```

B. 如果无 @HttpExchange 使用:
   - 记录确认: "CONF-06: 无需迁移，httpexchanges 录制未使用"
   - 可选择添加配置以支持未来使用

C. 如果已正确配置:
   - 记录确认: "CONF-06: 已正确配置"

更新 application.yaml 如需要。
</action>
  <read_first>
springboot/src/main/resources/application.yaml
</read_first>
  <acceptance_criteria>
1. 如果需要配置: application.yaml 包含正确的 httpexchanges 配置
2. 如果不需要配置: 确认文件中存在 "CONF-06: 无需迁移" 注释
</acceptance_criteria>
  <done>
HTTP exchanges 配置检查完成
</done>
</task>

</tasks>

<verification>
## Phase 15 Plan 03 Verification

1. 检查当前 httpexchanges 配置:
   ```bash
   grep -A5 "httpexchanges" springboot/src/main/resources/application.yaml
   ```

2. 检查 @HttpExchange 使用:
   ```bash
   grep -r "@HttpExchange\|@Recorded" springboot/src/main/java --include="*.java" | head -10
   ```

3. 确认配置状态
</verification>

<success_criteria>
1. 已检查项目中 httpexchanges 的使用情况
2. 已确认 Spring Boot 4.1 默认值变更影响（或确认无影响）
3. 如需配置，已添加到 application.yaml
</success_criteria>
