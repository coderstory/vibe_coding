---
phase: 15-config-migration
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - springboot/src/main/resources/application.yaml
autonomous: true
requirements:
  - CONF-05

must_haves:
  truths:
    - "确认 ReactorClientHttpRequestFactoryBuilder 默认值变更状态"
    - "如需要迁移，配置已更新"
  artifacts:
    - path: "springboot/src/main/resources/application.yaml"
      provides: "HTTP 客户端配置（如果需要）"
      contains: "reactor"

objectives:
  - 检查 Spring Boot 4.x 中 ReactorClientHttpRequestFactoryBuilder 默认值变更
  - 验证 WebClient/RestTemplate 配置是否需要调整
---

<objective>
检查 Spring Boot 4.1 中 ReactorClientHttpRequestFactoryBuilder 默认值变更（CONF-05）。验证项目中的 WebClient 或 RestTemplate 配置是否受影响。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 检查 WebClient/RestTemplate 使用情况</name>
  <files>springboot/src/main/java</files>
  <action>
1. 搜索项目中 WebClient 和 RestTemplate 的使用:

在 springboot/src/main/java 目录下搜索:
- 包含 "WebClient" 的文件
- 包含 "RestTemplate" 的文件
- 包含 "ReactorClientHttpRequestFactoryBuilder" 的文件

2. 检查 application.yaml 是否已有相关配置:
- spring.httpclient.reactor.*
- spring.webclient.*
- 其他 HTTP 客户端相关配置

3. 评估是否需要迁移或添加配置

如果找到使用 WebClient 且无显式配置，需要检查 Spring Boot 4.1 的默认值变更。
</action>
  <read_first>
springboot/src/main/resources/application.yaml
</read_first>
  <acceptance_criteria>
1. 找到所有使用 WebClient 或 RestTemplate 的 Java 文件
2. 确认 application.yaml 中是否有相关 HTTP 客户端配置
3. 如有默认值变更影响，记录需要添加的配置
</acceptance_criteria>
  <done>
WebClient/RestTemplate 使用情况已检查
</done>
</task>

<task type="auto">
  <name>Task 2: 验证/添加必要配置</name>
  <files>springboot/src/main/resources/application.yaml</files>
  <action>
基于 Task 1 的检查结果:

A. 如果发现 WebClient 使用但无显式配置:
   - 检查 Spring Boot 4.1 ReactorClientHttpRequestFactoryBuilder 的默认值变更
   - 如需要，在 application.yaml 中添加:
   ```yaml
   spring:
     httpclient:
       reactor:
         connect-timeout: 5000
         read-timeout: 30000
   ```

B. 如果无 WebClient 使用或已正确配置:
   - 记录确认: "CONF-05: 无需迁移，WebClient 未使用或已正确配置"

更新 application.yaml 如需要。
</action>
  <read_first>
springboot/src/main/resources/application.yaml
</read_first>
  <acceptance_criteria>
1. 如果需要配置: application.yaml 包含 reactor HTTP 客户端配置
2. 如果不需要配置: 确认文件中存在 "CONF-05: 无需迁移" 注释
</acceptance_criteria>
  <done>
HTTP 客户端配置检查完成
</done>
</task>

</tasks>

<verification>
## Phase 15 Plan 02 Verification

1. 搜索 WebClient/RestTemplate 使用: `grep -r "WebClient\|RestTemplate" springboot/src/main/java --include="*.java" | head -20`
2. 检查 application.yaml 配置: `grep -A5 "httpclient\|webclient\|reactor" springboot/src/main/resources/application.yaml`
3. 确认配置状态
</verification>

<success_criteria>
1. 已检查项目中 WebClient/RestTemplate 的使用情况
2. 已确认 Spring Boot 4.1 默认值变更影响（或确认无影响）
3. 如需配置，已添加到 application.yaml
</success_criteria>
