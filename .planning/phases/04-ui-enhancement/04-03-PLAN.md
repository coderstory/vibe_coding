---
phase: 04-ui-enhancement
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - app-vue/src/views/auth/Login.vue
  - app-vue/src/views/layout/Layout.vue
  - app-vue/src/assets/glass.css
  - app-vue/src/App.vue
autonomous: true
requirements:
  - UI-THEME-01
  - UI-THEME-02
  - UI-GLASS-01
  - UI-DYNAMIC-01
user_setup: []
must_haves:
  truths:
    - 登录页具有完整液态玻璃效果
    - 管理后台有现代动态视觉效果
    - 主题切换在登录页和管理后台均可用
  artifacts:
    - path: "app-vue/src/views/auth/Login.vue"
      provides: "完整液态玻璃登录页"
    - path: "app-vue/src/views/layout/Layout.vue"
      provides: "完整动态效果管理后台"
---

<objective>
完善登录页液态玻璃效果和管理后台动态视觉效果。
</objective>

<context>
@.planning/phases/04-ui-enhancement/04-ui-CONTEXT.md
@.planning/phases/04-ui-enhancement/04-02-PLAN.md
@./AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 完善 Login.vue 液态玻璃效果</name>
  <files>app-vue/src/views/auth/Login.vue</files>
  <read_first>app-vue/src/views/auth/Login.vue</read_first>
  <action>
完善 Login.vue，添加完整的液态玻璃和动态背景效果:

1. 动态粒子背景（3-5个浮动粒子）
2. 渐变背景动画
3. 玻璃卡片效果
4. 输入框玻璃效果
5. 按钮悬停动画
6. 主题切换按钮

效果要求：
- 背景从紫蓝色渐变到粉色持续动画
- 粒子缓慢浮动
- 登录卡片有玻璃模糊效果和发光边框
- 按钮有悬停上浮和发光效果
- 整体视觉现代、炫酷
  </action>
  <verify>
<code>grep -q "dynamic-gradient" app-vue/src/views/auth/Login.vue && echo "PASS" || echo "FAIL"</code>
  </verify>
  <verify>
<code>grep -q "glass" app-vue/src/views/auth/Login.vue && echo "PASS" || echo "FAIL"</code>
  </verify>
  <done>Login.vue 液态玻璃效果已完善</done>
</task>

<task type="auto">
  <name>Task 2: 完善 Layout.vue 动态效果</name>
  <files>app-vue/src/views/layout/Layout.vue</files>
  <read_first>app-vue/src/views/layout/Layout.vue</read_first>
  <action>
完善 Layout.vue，添加现代动态视觉效果:

1. 侧边栏玻璃效果（半透明背景 + 模糊）
2. 顶部栏玻璃效果
3. 内容区动态背景
4. 菜单项悬停动画
5. 主题切换按钮

效果要求：
- 侧边栏有玻璃模糊效果
- 顶部栏有玻璃背景
- 侧边栏可折叠动画
- 菜单项有悬停高亮动画
- 整体布局流畅、现代
  </action>
  <verify>
<code>grep -q "glass" app-vue/src/views/layout/Layout.vue && echo "PASS" || echo "FAIL"</code>
  </verify>
  <verify>
<code>grep -q "dynamic-gradient" app-vue/src/views/layout/Layout.vue && echo "PASS" || echo "FAIL"</code>
  </verify>
  <done>Layout.vue 动态效果已完善</done>
</task>

<task type="auto">
  <name>Task 3: 更新 glass.css 添加更多动画</name>
  <files>app-vue/src/assets/glass.css</files>
  <read_first>app-vue/src/assets/glass.css</read_first>
  <action>
更新 glass.css，添加更多液态玻璃和动态效果:

```css
/* 动态渐变背景 */
.dynamic-gradient {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
  background-size: 400% 400%;
  animation: gradientShift 15s ease infinite;
}

@keyframes gradientShift {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

/* 暗色主题 */
[data-theme="dark"] .dynamic-gradient {
  background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
}

/* 液态玻璃 */
.glass {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 16px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
}

[data-theme="dark"] .glass {
  background: rgba(0, 0, 0, 0.3);
  border: 1px solid rgba(255, 255, 255, 0.1);
}

/* 玻璃按钮 */
.glass-button {
  background: rgba(255, 255, 255, 0.2);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.3);
  border-radius: 12px;
  padding: 12px 24px;
  color: white;
  font-weight: 600;
  transition: all 0.3s ease;
}

.glass-button:hover {
  background: rgba(255, 255, 255, 0.3);
  transform: translateY(-2px);
  box-shadow: 0 12px 40px rgba(0, 0, 0, 0.2);
}

/* 玻璃输入框 */
.glass-input input {
  background: transparent !important;
  border: none !important;
  color: white !important;
}

.glass-input input::placeholder {
  color: rgba(255, 255, 255, 0.6);
}

/* 浮动动画 */
@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-10px); }
}

.floating { animation: float 3s ease-in-out infinite; }

/* 脉冲动画 */
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.7; }
}

.pulse { animation: pulse 2s ease-in-out infinite; }

/* 发光效果 */
.glow {
  box-shadow: 0 0 20px rgba(102, 126, 234, 0.5);
}

.glow:hover {
  box-shadow: 0 0 40px rgba(102, 126, 234, 0.8);
}

/* 侧边栏玻璃 */
.sidebar-glass {
  background: rgba(48, 65, 86, 0.85);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-right: 1px solid rgba(255, 255, 255, 0.1);
}

[data-theme="dark"] .sidebar-glass {
  background: rgba(22, 33, 62, 0.85);
}

/* 顶部栏玻璃 */
.header-glass {
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-bottom: 1px solid rgba(0, 0, 0, 0.1);
}

[data-theme="dark"] .header-glass {
  background: rgba(26, 26, 46, 0.8);
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

/* 卡片玻璃 */
.card-glass {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 12px;
  transition: all 0.3s ease;
}

.card-glass:hover {
  background: rgba(255, 255, 255, 0.15);
  transform: translateY(-4px);
  box-shadow: 0 16px 48px rgba(0, 0, 0, 0.15);
}
```
  </action>
  <verify>
<code>grep -q "sidebar-glass" app-vue/src/assets/glass.css && echo "PASS" || echo "FAIL"</code>
  </verify>
  <verify>
<code>grep -q "header-glass" app-vue/src/assets/glass.css && echo "PASS" || echo "FAIL"</code>
  </verify>
  <done>glass.css 已更新更多动画效果</done>
</task>

<task type="auto">
  <name>Task 4: 更新 user store 登录后加载主题</name>
  <files>app-vue/src/stores/user.js</files>
  <read_first>app-vue/src/stores/user.js</read_first>
  <action>
更新 user.js store，在登录成功后加载用户主题偏好:

在 login 方法的成功回调中添加：
```javascript
import { useThemeStore } from './theme'

async login(username, password) {
  // ... 现有登录逻辑
  const themeStore = useThemeStore()
  themeStore.loadTheme()
}
```
  </action>
  <verify>
<code>grep -q "themeStore.loadTheme" app-vue/src/stores/user.js && echo "PASS" || echo "FAIL"</code>
  </verify>
  <done>user store 已更新登录后加载主题</done>
</task>

<task type="auto">
  <name>Task 5: 更新 App.vue 添加主题初始化</name>
  <files>app-vue/src/App.vue</files>
  <read_first>app-vue/src/App.vue</read_first>
  <action>
更新 App.vue，确保应用启动时初始化主题:

```vue
<script setup>
import { onMounted } from 'vue'
import { useThemeStore } from '@/stores/theme'
import { useUserStore } from '@/stores/user'

const themeStore = useThemeStore()
const userStore = useUserStore()

onMounted(() => {
  // 应用启动时应用保存的主题
  if (userStore.isLoggedIn) {
    themeStore.loadTheme()
  } else {
    // 未登录时默认亮色主题
    themeStore.applyTheme('light')
  }
})
</script>
```
  </action>
  <verify>
<code>grep -q "loadTheme" app-vue/src/App.vue && echo "PASS" || echo "FAIL"</code>
  </verify>
  <done>App.vue 已添加主题初始化</done>
</task>

<task type="auto">
  <name>Task 6: 测试完整 UI 流程</name>
  <files>app-vue/src</files>
  <action>
执行完整 UI 测试流程:

1. 启动后端: `cd springboot && ./mvnw spring-boot:run`
2. 启动前端: `cd app-vue && npm run dev`
3. 验证登录页:
   - 动态渐变背景动画正常
   - 粒子浮动动画正常
   - 玻璃卡片效果正常
   - 玻璃按钮悬停效果正常
   - 主题切换按钮可用
4. 登录后验证:
   - 管理后台布局正常
   - 侧边栏玻璃效果正常
   - 顶部栏玻璃效果正常
   - 主题切换生效
   - 页面间切换正常
5. 刷新页面验证主题保持
  </action>
  <verify>
<code>echo "Manual verification required - run tests described above"</code>
  </verify>
  <done>完整 UI 流程测试完成</done>
</task>

</tasks>

<verification>
手动验证步骤：
1. 启动 MySQL 和后端
2. 启动前端开发服务器
3. 访问登录页检查视觉效果
4. 登录后检查管理后台效果
5. 测试主题切换
6. 刷新页面验证主题持久化
</verification>

<success_criteria>
- [ ] 登录页有完整液态玻璃效果
- [ ] 动态背景动画流畅
- [ ] 管理后台有现代视觉效果
- [ ] 主题切换在登录页和管理后台均可用
- [ ] 主题偏好持久化到后端
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-enhancement/04-03-SUMMARY.md`
</output>
