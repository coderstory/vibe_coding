---
phase: 13
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - springboot/settings.gradle.kts
  - springboot/libs.versions.toml
  - springboot/gradle.properties
  - springboot/build.gradle.kts
autonomous: false
requirements:
  - MIGR-03
  - MIGR-04
  - MIGR-05
---

<objective>
创建 Gradle 项目的基础构建配置文件，包括 settings.gradle.kts、libs.versions.toml、gradle.properties 和 build.gradle.kts，将 pom.xml 的 Maven 配置等效翻译为 Gradle Kotlin DSL 配置
</objective>

<read_first>
- springboot/pom.xml (当前 Maven 配置，用于翻译依赖到 Gradle)
- .planning/phases/13-migration-prep/13-CONTEXT.md (设计决策: Kotlin DSL, Version Catalog)
- .planning/phases/13-migration-prep/13-RESEARCH.md (技术研究和 Gradle 配置示例)
</read_first>

<tasks>

<task>
<id>1</id>
<type>execute</type>
<files>
  - springboot/settings.gradle.kts
</files>
<objective>
创建 settings.gradle.kts 项目配置文件
</objective>
<action>
创建 `springboot/settings.gradle.kts` 文件:

```kotlin
rootProject.name = "springboot"
```
</action>
<verify>
pwsh -Command "Get-Content springboot/settings.gradle.kts"
</verify>
<acceptance_criteria>
- [ ] 文件存在: springboot/settings.gradle.kts
- [ ] 内容为: rootProject.name = "springboot"
- [ ] 文件编码为 UTF-8
</acceptance_criteria>
</task>

<task>
<id>2</id>
<type>execute</type>
<files>
  - springboot/libs.versions.toml
</files>
<objective>
创建 libs.versions.toml 版本目录文件
</objective>
<action>
创建 `springboot/libs.versions.toml` 文件，基于 pom.xml 的 properties 和 dependencies:

```toml
[versions]
# Gradle
gradle = "9.4"

# Java
java = "26"

# Spring Boot
spring-boot = "4.0.5"
spring-aop = "3.5.13"

# Database
mysql = "9.6.0"
flyway = "12.4.0"
mybatis-plus = "3.5.16"

# Messaging
rocketmq-client = "5.3.2"
rocketmq-spring-boot-starter = "2.3.5"

# Security & Cache
redisson = "4.3.1"
jjwt = "0.13.0"

# Utils
zstd = "1.5.7-7"
lombok = "1.18.44"

[libraries]
# Spring Boot Starters
spring-boot-starter-webmvc = { group = "org.springframework.boot", name = "spring-boot-starter-webmvc" }
spring-boot-starter-security = { group = "org.springframework.boot", name = "spring-boot-starter-security" }
spring-boot-starter-aop = { group = "org.springframework.boot", name = "spring-boot-starter-aop", version.ref = "spring-aop" }
spring-boot-starter-data-redis = { group = "org.springframework.boot", name = "spring-boot-starter-data-redis" }
spring-boot-starter-flyway = { group = "org.springframework.boot", name = "spring-boot-starter-flyway" }
spring-boot-starter-test = { group = "org.springframework.boot", name = "spring-boot-starter-test" }
spring-security-test = { group = "org.springframework.security", name = "spring-security-test" }
spring-messaging = { group = "org.springframework", name = "spring-messaging" }

# Configuration Processor
spring-boot-configuration-processor = { group = "org.springframework.boot", name = "spring-boot-configuration-processor" }

# Database
mysql-connector-j = { group = "com.mysql", name = "mysql-connector-j", version.ref = "mysql" }
mybatis-plus-spring-boot4-starter = { group = "com.baomidou", name = "mybatis-plus-spring-boot4-starter", version.ref = "mybatis-plus" }
flyway-core = { group = "org.flywaydb", name = "flyway-core", version.ref = "flyway" }
flyway-mysql = { group = "org.flywaydb", name = "flyway-mysql", version.ref = "flyway" }

# Messaging
rocketmq-client = { group = "org.apache.rocketmq", name = "rocketmq-client", version.ref = "rocketmq-client" }
rocketmq-spring-boot-starter = { group = "org.apache.rocketmq", name = "rocketmq-spring-boot-starter", version.ref = "rocketmq-spring-boot-starter" }
rocketmq-tools = { group = "org.apache.rocketmq", name = "rocketmq-tools", version.ref = "rocketmq-client" }

# Security & Cache
redisson-spring-boot-starter = { group = "org.redisson", name = "redisson-spring-boot-starter", version.ref = "redisson" }

# JWT
jjwt-api = { group = "io.jsonwebtoken", name = "jjwt-api", version.ref = "jjwt" }
jjwt-impl = { group = "io.jsonwebtoken", name = "jjwt-impl", version.ref = "jjwt" }
jjwt-jackson = { group = "io.jsonwebtoken", name = "jjwt-jackson", version.ref = "jjwt" }

# Utils
zstd-jni = { group = "com.github.luben", name = "zstd-jni", version.ref = "zstd" }
lombok = { group = "org.projectlombok", name = "lombok", version.ref = "lombok" }

[plugins]
spring-boot = { id = "org.springframework.boot", version.ref = "spring-boot" }
dependency-management = { id = "io.spring.dependency-management", version = "1.1.7" }
flyway = { id = "org.flywaydb.flyway", version.ref = "flyway" }
```
</action>
<verify>
pwsh -Command "Test-Path springboot/libs.versions.toml"
</verify>
<acceptance_criteria>
- [ ] 文件存在: springboot/libs.versions.toml
- [ ] 包含 [versions] 段: java="26", spring-boot, flyway="12.4.0", mybatis-plus, rocketmq-client, redisson, jjwt, zstd, lombok
- [ ] 包含 [libraries] 段: 所有 Maven 依赖的 Gradle 版本
- [ ] 包含 [plugins] 段: spring-boot, dependency-management, flyway
</acceptance_criteria>
</task>

<task>
<id>3</id>
<type>execute</type>
<files>
  - springboot/gradle.properties
</files>
<objective>
创建 gradle.properties 文件，配置 JVM 参数
</objective>
<action>
创建 `springboot/gradle.properties` 文件:

```properties
# Gradle JVM args - 启用 preview features
org.gradle.jvmargs=--enable-preview

# Enable parallel build
org.gradle.parallel=true

# Enable build cache
org.gradle.caching=true

# Java installations auto detect
org.gradle.java.installations.auto-detect=true
```
</action>
<verify>
pwsh -Command "Get-Content springboot/gradle.properties"
</verify>
<acceptance_criteria>
- [ ] 文件存在: springboot/gradle.properties
- [ ] 包含 org.gradle.jvmargs=--enable-preview
- [ ] 包含 org.gradle.parallel=true
- [ ] 包含 org.gradle.caching=true
</acceptance_criteria>
</task>

<task>
<id>4</id>
<type>execute</type>
<files>
  - springboot/build.gradle.kts
</files>
<objective>
创建 build.gradle.kts 根构建脚本
</objective>
<action>
创建 `springboot/build.gradle.kts` 文件:

```kotlin
import org.jetbrains.kotlin.gradle.tasks.KotlinCompile

plugins {
    `java-library`
    id("org.springframework.boot") version "4.0.5"
    id("io.spring.dependency-management") version "1.1.7"
    id("org.flywaydb.flyway") version "12.4.0"
}

val javaVersion = JavaLanguageVersion.of(26)

java {
    toolchain {
        languageVersion.set(javaVersion)
    }
}

repositories {
    mavenCentral()
}

dependencyManagement {
    imports {
        mavenBom("org.springframework.boot:spring-boot-dependencies:4.0.5")
    }
}

dependencies {
    // Spring Boot Starters
    implementation(libs.springBootStarterWebmvc)
    implementation(libs.springBootStarterSecurity)
    implementation(libs.springBootStarterAop)
    implementation(libs.springBootStarterDataRedis)
    implementation(libs.springBootStarterFlyway)
    implementation(libs.springMessaging)

    // Database
    implementation(libs.mysqlConnectorJ)
    implementation(libs.mybatisPlusSpringBoot4Starter)
    implementation(libs.flywayCore)
    implementation(libs.flywayMysql)

    // Messaging
    implementation(libs.rocketmqClient)
    implementation(libs.rocketmqSpringBootStarter)
    implementation(libs.rocketmqTools)

    // Security & Cache
    implementation(libs.redissonSpringBootStarter)

    // JWT
    implementation(libs.jjwtApi)
    runtimeOnly(libs.jjwtImpl)
    runtimeOnly(libs.jjwtJackson)

    // Utils
    runtimeOnly(libs.zstdJni)

    // Annotation Processors
    annotationProcessor(libs.lombok)
    annotationProcessor(libs.springBootConfigurationProcessor)

    // Test
    testImplementation(libs.springBootStarterTest)
    testImplementation(libs.springSecurityTest)
}

tasks.withType<KotlinCompile> {
    compilerArgs.add("--enable-preview")
}

tasks.withType<Test> {
    useJUnitPlatform()
}

springBoot {
    buildInfo()
}

flyway {
    url = "jdbc:mysql://localhost:3306/admin_system?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false"
    user = System.getenv("DB_USER") ?: "root"
    password = System.getenv("DB_PASSWORD") ?: ""
    driver = "com.mysql.cj.jdbc.Driver"
    locations = listOf("classpath:db/migration")
    baselineOnMigrate = true
}
```

**注意:** Flyway 配置中的密码使用环境变量 `DB_PASSWORD`，不再硬编码。
</action>
<verify>
pwsh -Command "Test-Path springboot/build.gradle.kts"
</verify>
<acceptance_criteria>
- [ ] 文件存在: springboot/build.gradle.kts
- [ ] 包含 plugins 块: java-library, spring-boot, dependency-management, flyway
- [ ] 包含 java toolchain 配置: languageVersion.set(JavaLanguageVersion.of(26))
- [ ] 包含 dependencyManagement 配置
- [ ] 包含 dependencies 块: 所有 pom.xml 依赖已翻译
- [ ] 包含 annotationProcessor 配置: lombok 和 spring-boot-configuration-processor
- [ ] 包含 tasks.withType<KotlinCompile> 配置 --enable-preview
- [ ] 包含 flyway 配置块
- [ ] flyway 密码使用环境变量而非硬编码
</acceptance_criteria>
</task>

</tasks>

<verification>
1. 检查所有 4 个文件是否存在
2. 验证 build.gradle.kts 可被解析
3. 验证 libs.versions.toml 格式正确
</verification>

<success_criteria>
1. settings.gradle.kts, libs.versions.toml, gradle.properties, build.gradle.kts 全部创建
2. 所有 pom.xml 依赖已翻译为 Gradle 依赖
3. Java 26 toolchain 配置正确
4. annotationProcessor 配置正确 (Lombok + config processor)
5. --enable-preview 同时在 gradle.properties 和 KotlinCompile 任务中配置
6. Flyway 配置使用环境变量而非硬编码凭证
</success_criteria>

---

*Plan: 13-01*
*Requirements: MIGR-03, MIGR-04, MIGR-05*
*Created: 2026-04-30*
