---
phase: 28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - springboot/src/main/resources/config/datasource.yaml
  - springboot/src/main/resources/config/cache.yaml
  - springboot/src/main/resources/config/mq.yaml
  - springboot/src/main/resources/config/security.yaml
  - springboot/src/main/resources/config/business.yaml
  - springboot/src/main/resources/application.yaml
  - springboot/build.gradle.kts
  - springboot/gradle/libs.versions.toml
  - app-vue/eslint.config.js
  - app-vue/stylelint.config.js
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04
  - CFG-05
  - CFG-06
  - CFG-07
  - CFG-08

must_haves:
  truths:
    - "每个 YAML 配置文件包含段头注释（`# =====`），说明该段落配置的目的"
    - "关键属性（URL、密码、超时时间等）包含行内注释说明其用途和默认值"
    - "application.yaml 包含整体描述，说明配置文件拆分结构和应用名称"
    - "build.gradle.kts 每个构建块（plugins/dependencies/tasks/checkstyle/flyway）有章节注释"
    - "libs.versions.toml 每个 section（versions/libraries/plugins）有段头注释，关键依赖有行内说明"
    - "eslint.config.js 和 stylelint.config.js 有配置项注释说明规则用途"
    - "所有修改保持 Phase 27 规范：中文描述 + 英文技术术语保留"
  artifacts:
    - path: springboot/src/main/resources/config/datasource.yaml
      provides: "数据源 + Flyway 迁移配置注释"
    - path: springboot/src/main/resources/config/cache.yaml
      provides: "Redis + Redisson 分布式锁配置注释"
    - path: springboot/src/main/resources/config/mq.yaml
      provides: "RocketMQ NameServer + Producer 配置注释"
    - path: springboot/src/main/resources/config/security.yaml
      provides: "安全白名单 + JWT 配置注释"
    - path: springboot/src/main/resources/config/business.yaml
      provides: "MyBatis Plus + 秒杀参数 + 日志级别配置注释"
    - path: springboot/src/main/resources/application.yaml
      provides: "应用根配置，含拆分加载结构和虚拟线程/服务器说明"
    - path: springboot/build.gradle.kts
      provides: "构建脚本（plugins/dependencies/tasks/flyway）注释"
    - path: springboot/gradle/libs.versions.toml
      provides: "版本目录（versions/libraries/plugins）段头注释"
    - path: app-vue/eslint.config.js
      provides: "ESLint flat config 规则用途注释"
    - path: app-vue/stylelint.config.js
      provides: "Stylelint 配置项用途注释"
  key_links:
    - from: application.yaml
      to: config/*.yaml
      via: "spring.config.import 列表"
    - from: build.gradle.kts
      to: libs.versions.toml
      via: "libs.* 版本引用"
---

<objective>
为所有配置文件（YAML、Gradle 版本目录、ESLint/Stylelint）添加段头注释和关键属性说明。

Purpose: 让新开发者能快速理解每个配置项的作用和边界，降低上手成本。保留现有注释，增量补齐缺失部分。
Output: 10 个配置文件全部补充完毕，包含段头注释（段落目的说明）和关键属性行内说明。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@CLAUDE.md

接口/约定说明（从代码库提取）：
- YAML 段头格式（已有）：`# ===== 标题 =====` + `# 说明行` + `# -----`
- YAML 行内格式（已有）：`key: value  # 说明`
- build.gradle.kts：Kotlin DSL 格式，dependencies 使用 `libs.*` 版本目录引用
- libs.versions.toml：三段式（versions/libraries/plugins），library 命名用 kebab-case
- ESLint config：ESM 格式 `export default [...]`，flat config 方式
- Stylelint config：CommonJS 格式 `export default {...}`
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 5 个业务 YAML 配置补充段头和行内注释</name>
  <files>
    springboot/src/main/resources/config/datasource.yaml
    springboot/src/main/resources/config/cache.yaml
    springboot/src/main/resources/config/mq.yaml
    springboot/src/main/resources/config/security.yaml
    springboot/src/main/resources/config/business.yaml
  </files>
  <action>
    读取每个 YAML 文件，按 Phase 27 规范增量补充注释（保留现有注释，只加不减）。

    **datasource.yaml**：已有良好段头和行内注释，核实完整性后仅补充 Flyway 段落的标准格式（`# -----` 分隔线对齐）。

    **cache.yaml**：已有 `# ===== Redis 配置 =====` 和 `# ===== Redisson 分布式锁配置 =====`。补充：
    - Redis 连接池每个参数行内注释补充语义（max-active 改为 `# 最大活跃连接数（负值表示无限制）`）
    - 在文件顶部添加文件级说明：`# Redis 缓存和分布式锁配置`（现有已满足，跳过）

    **mq.yaml**：现有仅一个段头。保持段头，在文件顶部添加文件级说明行。补充 producer 段内的 `send-message-timeout` 行内注释。

    **security.yaml**：已有段头。补充：
    - 在 `security.whitelist` 值上方添加 `# 安全白名单路径（无需认证即可访问的 API 端点，多个路径用逗号分隔）`
    - 在 `jwt.secret` 行添加注释：`# Access Token 签名密钥（生产环境通过环境变量注入，禁止硬编码）`

    **business.yaml**：已有良好段头和行内注释。补充：
    - MyBatis Plus 段落添加 configuration 的子项注释（已有，跳过）
    - 秒杀段落 `sse.timeout` 添加连接说明（已有，跳过）
    - logging 段落补充 `cn.coderstory` 行内注释说明这是应用包日志级别

    说明（遵循 D-09/D-10 决策）：
    - 保留现有内容，只做增量补充
    - 段头格式统一：`# ===== 段落标题 =====` 上 + `# 段落说明` + `# -----`
    - 行内注释确保至少与值间隔 2 个空格
    - 密码/密钥所在行添加注释提醒"通过环境变量注入"
  </action>
  <verify>
    <automated>
      for f in springboot/src/main/resources/config/datasource.yaml springboot/src/main/resources/config/cache.yaml springboot/src/main/resources/config/mq.yaml springboot/src/main/resources/config/security.yaml springboot/src/main/resources/config/business.yaml; do
        echo "=== $f ===" && grep -c '# =====' "$f" 2>/dev/null || echo "0 section headers"
      done
    </automated>
  </verify>
  <done>5 个 YAML 配置文件全部包含段头注释和关键属性行内说明，执行 `grep -c '# ===' config/*.yaml` 返回每个文件至少 2 个段头。</done>
</task>

<task type="auto">
  <name>Task 2: 为 application.yaml + Gradle 构建文件补充注释</name>
  <files>
    springboot/src/main/resources/application.yaml
    springboot/build.gradle.kts
    springboot/gradle/libs.versions.toml
  </files>
  <action>
    读取每个文件，按 Phase 27 规范补充注释。

    **application.yaml**：
    - 保留现有文件头注释（大段头 + config.import 列表）
    - 在 `spring.application.name` 行内已有注释，保留
    - 在 `spring.config.import` 列表上方补充：`# 按关注点拆分的子配置文件，通过 classpath:config/ 加载`
    - `spring.threads.virtual.enabled` 的注释已存在，保留
    - `server.port` 上方添加段头 `# ===== 服务器配置 =====`（现有已存在，保留）

    **build.gradle.kts**：
    - add `// ===== 构建脚本配置 =====` 在 buildscript 上方
    - add `// ===== 项目插件 =====` 在 plugins 块上方
    - add `// ===== Java 工具链配置 =====` 在 java 块上方
    - add `// ===== 依赖管理 =====` 在 dependencies 块上方
    - add `// ===== Spring Boot 构建信息 =====` 在 springBoot 块上方
    - add `// ===== Flyway 数据库迁移配置 =====` 在 flyway 块上方
    - buildscript 内的 flyway-mysql 依赖已有行注释，保留
    - dependencies 块内按组加行注释（现有已有 `// Spring Boot Starters` 等，保留）

    **libs.versions.toml**：
    - add `# ===== 版本号定义 =====` 在 [versions] 上方
    - add `# ===== 依赖库声明 =====` 在 [libraries] 上方  
    - add `# ===== Gradle 插件声明 =====` 在 [plugins] 上方
    - 在每个 library 的组间补充分组说明注释：
      ```
      # BOM（Spring Boot 依赖管理）
      ```
      ```
      # Spring Boot Starters（由 BOM 管理版本，无需指定版本号）
      ```
      ```
      # Database 驱动和 ORM
      ```
      ```
      # RocketMQ 消息队列
      ```
      ```
      # JWT 认证
      ```

    说明（遵循 D-09/D-10 决策）：
    - build.gradle.kts 使用 `//` 行注释，遵循 Kotlin DSL 注释风格
    - libs.versions.toml 使用 `#` 行注释
    - 按阶段章节分组，让新开发者能快速找到对应类别的依赖
  </action>
  <verify>
    <automated>
      echo "application.yaml sections:" && grep -c '# =====' springboot/src/main/resources/application.yaml
      echo "build.gradle.kts sections:" && grep -c '// =====' springboot/build.gradle.kts
      echo "libs.versions.toml sections:" && grep -c '# =====' springboot/gradle/libs.versions.toml
    </automated>
  </verify>
  <done>application.yaml 包含文件头描述和段头配置，build.gradle.kts 每个构建块有章节注释，libs.versions.toml 三段结构清晰且有分组说明。</done>
</task>

<task type="auto">
  <name>Task 3: 为 ESLint 和 Stylelint 前端 lint 配置补充注释</name>
  <files>
    app-vue/eslint.config.js
    app-vue/stylelint.config.js
  </files>
  <action>
    读取每个文件，按 Phase 27 规范补充注释。

    **eslint.config.js**：
    - 文件顶部添加 JSDoc 块注释：
      ```
      /**
       * ESLint 10.x Flat Config 配置。
       * 使用 typescript-eslint + eslint-plugin-vue + @stylistic/eslint-plugin 进行 JS/TS/Vue 代码检查。
       */
      ```
    - 在 `import` 语句上方添加注释：`// 全局变量定义`
    - 在 `export default [` 之后第一个 ignores 对象上方加行注释：`// 忽略目录（构建产物和依赖）`
    - 在 vue 配置块内 `parserOptions` 前加：`// Vue 单文件组件解析，内部使用 TypeScript 解析器`
    - 在 rules 对象前加：`// Vue 推荐规则（使用 eslint-plugin-vue 默认规则集）`
    - 在 ts/js 配置块内 rules 对象前加注释：
      ```
      // TypeScript 推荐规则
      // no-unused-vars 委托给 @typescript-eslint 处理
      ```
    - 在 stylistic 配置块内 rules 对象前加：
      ```
      // Stylistic 代码风格规则
      // 当前为严控阶段，后续可调松
      ```
    - 对 `@stylistic/comma-dangle` 等每条规则加简单行内说明（可选，根据 Claude's Discretion 判断是否需要）

    **stylelint.config.js**：
    - 文件顶部添加：`// Stylelint 17.x 配置 — CSS 样式检查规则`
    - 在 `extends` 前加：`// 基础配置（继承 stylelint-config-standard）`
    - 在 rules 对象中，对每条 non-null 的规则加行内说明其目的。
      例如：
      - `'at-rule-no-unknown': true` → `// 禁用未知 at-rule（如拼写错误）`
      - `'block-no-empty': true` → `// 禁止空样式块`
      - `'color-no-invalid-hex': true` → `// 禁止无效十六进制颜色值`
      - 对 null 豁免规则添加简单说明：
        `'selector-class-pattern': null` → `// 允许自定义 class 命名风格（项目使用 kebab-case + BEM 混合）`
        `'no-descending-specificity': null` → `// 不强制选择器特异性降序排列`

    说明（遵循 D-09、Phase 27 YAML 注释规范类比 JS 配置）：
    - ESLint flat config 是 ESM 格式，注释用 `//` 风格
    - Stylelint 是 CommonJS 格式，注释用 `//` 风格
    - 仅补充缺失注释，保留现有注释内容
  </action>
  <verify>
    <automated>
      echo "eslint.config.js has file header:" && head -5 app-vue/eslint.config.js | grep -c '/\*\*'
      echo "stylelint.config.js has description:" && head -3 app-vue/stylelint.config.js | grep -c '// Stylelint'
      echo "stylelint rules have comments:" && grep -c '//' app-vue/stylelint.config.js
    </automated>
  </verify>
  <done>eslint.config.js 包含文件头注释、配置块说明和规则注释。stylelint.config.js 包含文件描述和每条规则的用途说明。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 配置文件→应用运行时 | 配置文件中的数据库密码、JWT 密钥等敏感信息注入到应用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information Disclosure | security.yaml, jwt.secret | mitigate | 通过行内注释提示"生产环境通过环境变量注入，禁止硬编码" |
| T-28-02 | Information Disclosure | datasource.yaml, password | mitigate | 现有注释已提示环境变量覆盖，保持不增删 |

本 Plan 仅修改注释内容，不修改运行时行为，无新增安全边界。威胁仅涉及注释内容本身是否引导用户安全配置。
</threat_model>

<verification>
1. 所有 10 个配置文件被修改
2. 段头注释格式统一（`# =====` 或 `// =====`）
3. YAML 文件通过 `yq` 或 Spring Boot 加载验证（`./gradlew.bat bootRun` 不因注释变更而失败）
4. ESLint/Stylelint 配置通过 `npm run lint` 验证（注释变更不影响 lint 结果）
</verification>

<success_criteria>
- CFG-01 至 CFG-08 全部满足：10 个配置文件包含符合 Phase 27 规范的注释
- YAML 配置文件每份至少包含每个段落 `# ===== 段落名 =====` 段头 + 每个非自解释属性行内注释
- build.gradle.kts 和 libs.versions.toml 包含分组段头注释
- ESLint/Stylelint 配置包含文件描述和关键规则用途注释
- `./gradlew.bat build` 和 `npm run lint` 不受影响（注释变更不改变运行时行为）
</success_criteria>

<output>
After completion, create `.planning/phases/28-配置层-Controller层注释/28-01-SUMMARY.md`
</output>
