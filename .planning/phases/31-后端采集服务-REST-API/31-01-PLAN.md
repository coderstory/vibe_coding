---
phase: 31
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - springboot/gradle/libs.versions.toml
  - springboot/build.gradle.kts
  - springboot/src/main/resources/config/business.yaml
  - springboot/src/main/java/cn/coderstory/springboot/config/MonitorHardwareProperties.java
  - springboot/src/main/java/cn/coderstory/springboot/config/MonitoringSchedulerConfig.java
  - springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/MetricValue.java
  - springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/CpuMetricsDTO.java
  - springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/MemoryMetricsDTO.java
  - springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/DiskMetricsDTO.java
  - springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/NetworkMetricsDTO.java
  - springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/SystemInfoDTO.java
  - springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/HardwareMetricsDTO.java
  - springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/TrendDataPoint.java
  - springboot/src/main/java/cn/coderstory/springboot/service/monitor/hardware/RingBuffer.java
  - springboot/src/main/java/cn/coderstory/springboot/service/monitor/hardware/HardwareMetricsService.java
autonomous: true
requirements:
  - HWM-01
  - HWM-08
  - TDD-01
must_haves:
  truths:
    - "OSHI 7.x FFM 依赖正确添加到项目，`./gradlew.bat build -x test` 编译通过"
    - "HardwareMetricsService 接口定义了 getCurrentMetrics()、getTrendHistory()、getSystemInfo() 三个方法"
    - "RingBuffer 支持 360 点环形缓冲，add/snapshot/size/capacity 方法完整"
    - "MonitorHardwareProperties 映射 business.yaml 中 monitor.hardware 配置段"
    - "MonitoringSchedulerConfig 创建 ScheduledExecutorService 单线程调度器"
    - "所有 DTO 包含符合 D-04~D-07 的字段结构"
  artifacts:
    - path: "springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/MetricValue.java"
      provides: "后端格式化 {value, format, unit} 结构（D-05）"
    - path: "springboot/src/main/java/cn/coderstory/springboot/config/MonitoringSchedulerConfig.java"
      provides: "ScheduledExecutorService 单线程调度 Bean（D-09, D-11）"
    - path: "springboot/src/main/java/cn/coderstory/springboot/service/monitor/hardware/HardwareMetricsService.java"
      provides: "采集服务接口定义（TDD-01, D-13）"
    - path: "springboot/src/main/java/cn/coderstory/springboot/service/monitor/hardware/RingBuffer.java"
      provides: "环形缓冲区，360 容量，线程安全（HWM-08）"
    - path: "springboot/src/main/java/cn/coderstory/springboot/config/MonitorHardwareProperties.java"
      provides: "硬件监控配置属性映射"
  key_links:
    - from: "build.gradle.kts"
      to: "libs.versions.toml"
      via: "implementation(libs.oshi.core.ffm)"
      pattern: "oshi-core-ffm"
    - from: "HardwareMetricsService"
      to: "HardwareMetricsDTO"
      via: "return type of getCurrentMetrics()"
      pattern: "HardwareMetricsDTO"
    - from: "MonitoringSchedulerConfig"
      to: "ScheduledExecutorService"
      via: "@Bean(destroyMethod = \"shutdown\")"
      pattern: "newSingleThreadScheduledExecutor"
---

<objective>
**目的:** 创建 Phase 31 基础层 — OSHI 依赖集成、配置类、DTO 数据结构、RingBuffer 和 Service 接口。

**输出:**
- OSHI 7.x FFM 依赖（`libs.versions.toml` + `build.gradle.kts`）
- 硬件监控配置段（`business.yaml`）和配置属性类
- 单线程调度器配置类
- 所有 DTO（含 MetricValue 格式化结构）
- RingBuffer 泛型环形缓冲区
- HardwareMetricsService 接口定义
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-后端采集服务-REST-API/31-CONTEXT.md
@.planning/phases/31-后端采集服务-REST-API/31-RESEARCH.md

<interfaces>
<!-- 本计划创建所有 DTO 接口定义和 Service 接口，
     下游 Plan 02 和 Plan 03 依赖这些接口。 -->

MonitorHardwareProperties 配置映射:
  prefix: "monitor.hardware"
  字段: samplingInterval(2000), diskNetInterval(3), trendBufferSize(360),
        trendDecimation(5), enabled(true)

HardwareMetricsService 接口:
  HardwareMetricsDTO getCurrentMetrics();     // 获取当前快照
  List<HardwareMetricsDTO> getTrendHistory(); // 获取全部趋势数据
  SystemInfoDTO getSystemInfo();              // 获取系统基本信息

RingBuffer<T>:
  void add(T item)
  List<T> snapshot()
  int size()
  int capacity()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>任务 1: OSHI 依赖 + 配置类 + business.yaml</name>
  <files>
    springboot/gradle/libs.versions.toml
    springboot/build.gradle.kts
    springboot/src/main/resources/config/business.yaml
    springboot/src/main/java/cn/coderstory/springboot/config/MonitorHardwareProperties.java
    springboot/src/main/java/cn/coderstory/springboot/config/MonitoringSchedulerConfig.java
  </files>
  <action>
    1. 修改 `libs.versions.toml`（遵循 D-01, D-02）:
       - 在 [versions] 添加 `oshi = "~7.1.0"`（Gradle `~` 范围约束，D-02）
       - 在 [libraries] 添加：
         `oshi-core-ffm = { module = "com.github.oshi:oshi-core-ffm", version.ref = "oshi" }`

    2. 修改 `build.gradle.kts`:
       - 在 dependencies 块末尾添加 `implementation(libs.oshi.core.ffm)`
       - 不要修改其他已有依赖

    3. 修改 `business.yaml`:
       - 在文件末尾添加 monitor.hardware 配置段（参考 SeckillProperties → Seckill 配置段模式）:
         ```yaml
         # ==================== 硬件监控配置 ====================
         # OSHI 采集服务的采样间隔、环形缓冲区大小和降级策略。
         # ---------------------------------------------------
         monitor:
           hardware:
             sampling-interval: 2000              # 主循环间隔（毫秒）
             disk-net-interval: 3                  # 磁盘/网络每 N 次采样更新一次
             trend-buffer-size: 360               # 环形缓冲区容量（360 点 = 1h at 10s 间隔）
             trend-decimation: 5                   # 每 N 次采样写入一次趋势缓冲区
             enabled: true                         # 是否启用采集
         ```

    4. 创建 `MonitorHardwareProperties.java`（遵循 SeckillProperties 模式）:
       - 包: `cn.coderstory.springboot.config`
       - `@Data @Component @ConfigurationProperties(prefix = "monitor.hardware")`
       - 字段（带默认值）：samplingInterval=2000, diskNetInterval=3, trendBufferSize=360, trendDecimation=5, enabled=true
       - 每个字段添加 `/** 中文注释 */` 说明用途
       - 类级 Javadoc 描述职责（L3 层强制）
       - 添加 `@since 1.8.0`

    5. 创建 `MonitoringSchedulerConfig.java`（遵循 D-09, D-11, RESEARCH Pattern 1）:
       - 包: `cn.coderstory.springboot.config`
       - `@Configuration @Slf4j`
       - 类级 Javadoc 描述职责（L3 层强制），说明创建独立调度线程池而非使用 @Scheduled
       - `@Bean(destroyMethod = "shutdown")` 返回 `ScheduledExecutorService`
       - 内部使用 `Executors.newSingleThreadScheduledExecutor(runnable -> {...})`
       - 线程名: `"monitor-hardware-collector"`，设置 daemon=true
       - 添加 `@since 1.8.0`

    验证: 所有文件编译通过
  </action>
  <verify>
    <automated>cd springboot && ./gradlew.bat build -x test -x check 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `libs.versions.toml` 包含 oshi 版本和 oshi-core-ffm library
    - `build.gradle.kts` 包含 implementation(libs.oshi.core.ffm)
    - `business.yaml` 包含 monitor.hardware 配置段
    - `MonitorHardwareProperties.java` 编译通过，所有字段有默认值
    - `MonitoringSchedulerConfig.java` 编译通过，创建单线程调度器
    - `./gradlew.bat build -x test -x check` 成功
  </done>
</task>

<task type="auto">
  <name>任务 2: DTO 定义 + RingBuffer + Service 接口</name>
  <files>
    springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/MetricValue.java
    springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/CpuMetricsDTO.java
    springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/MemoryMetricsDTO.java
    springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/DiskMetricsDTO.java
    springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/NetworkMetricsDTO.java
    springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/SystemInfoDTO.java
    springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/HardwareMetricsDTO.java
    springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/TrendDataPoint.java
    springboot/src/main/java/cn/coderstory/springboot/service/monitor/hardware/RingBuffer.java
    springboot/src/main/java/cn/coderstory/springboot/service/monitor/hardware/HardwareMetricsService.java
  </files>
  <action>
    创建以下文件（所有 DTO 使用 `@Data`，包路径 `cn.coderstory.springboot.dto.monitor.hardware`）:

    1. `MetricValue.java`（D-05 格式化结构）:
       - `@Data @AllArgsConstructor @NoArgsConstructor`
       - 泛型 `MetricValue<T>`，字段: `T value`, `String format`, `String unit`
       - 添加静态工厂: `MetricValue.of(T value, String format, String unit)`

    2. `CpuMetricsDTO.java`（D-07 各核数组）:
       - 字段: `double systemLoad`（总体 CPU 使用率，百分比 0-100）,
         `double[] perCoreLoad`（各核使用率数组）,
         `int logicalCores`, `int physicalCores`,
         `String processorName`, `long processorFrequency`, `boolean cpu64bit`
       - `static CpuMetricsDTO empty()` 返回全零实例（用于降级）

    3. `MemoryMetricsDTO.java`:
       - 字段: `MetricValue<Long> total`, `MetricValue<Long> available`,
         `MetricValue<Long> used`, `MetricValue<Double> usagePercent`
       - `static MemoryMetricsDTO empty()` 返回全空实例

    4. `DiskMetricsDTO.java`（HWM-04, HWM-05）:
       - 字段: `String name`, `String model`, `MetricValue<Long> size`, `String diskType`,
         `String mount`（挂载点）,
         `MetricValue<Long> totalSpace`, `MetricValue<Long> usedSpace`, `MetricValue<Long> usableSpace`,
         `MetricValue<Long> readBytesPerSec`, `MetricValue<Long> writeBytesPerSec`,
         `MetricValue<Long> readsPerSec`, `MetricValue<Long> writesPerSec`

    5. `NetworkMetricsDTO.java`（HWM-06）:
       - 字段: `String displayName`, `String macAddress`,
         `long speed`（连接速率 bps）, `boolean isKnownVm`,
         `MetricValue<Long> bytesSentPerSec`, `MetricValue<Long> bytesRecvPerSec`,
         `MetricValue<Long> packetsSentPerSec`, `MetricValue<Long> packetsRecvPerSec`

    6. `SystemInfoDTO.java`（HWM-09）:
       - 字段: `String osFamily`, `String osVersion`, `String osVersionInfo`,
         `long systemUptime`（秒）, `int processCount`, `String systemUptimeFormatted`

    7. `HardwareMetricsDTO.java`（D-04 单一顶层 DTO）:
       - 字段: `long timestamp`, `CpuMetricsDTO cpu`, `MemoryMetricsDTO memory`,
         `List<DiskMetricsDTO> disks`, `List<NetworkMetricsDTO> network`, `SystemInfoDTO system`
       - `static HardwareMetricsDTO empty()` 返回带有空子 DTO 的实例

    8. `TrendDataPoint.java`（D-06 趋势复用同一类型+时间戳）:
       - 字段: `long timestamp`, `double value`, `String metric`（指标名称如 "cpu"、"memory"）
       - `@Data @AllArgsConstructor @NoArgsConstructor`

    9. `RingBuffer.java`（RESEARCH Pattern 5，HWM-08）:
       - 包: `cn.coderstory.springboot.service.monitor.hardware`
       - 泛型 `RingBuffer<T>`，内部使用 `Object[]` 数组
       - 自带 `ReentrantReadWriteLock` 保证线程安全
       - 构造参数 `capacity`（int），分配 `new Object[capacity]`
       - 方法: `void add(T item)`, `List<T> snapshot()`, `int size()`, `int capacity()`
       - `add()` 使用写锁，`snapshot()` 使用读锁
       - `snapshot()` 按插入顺序返回（从头到尾遍历）
       - 添加四个包级别的注释（L2 鼓励）

    10. `HardwareMetricsService.java`（D-13 接口模式，TDD-01）:
        - 包: `cn.coderstory.springboot.service.monitor.hardware`
        - Interface，非 class
        - 三个方法（接口级 Javadoc 说明职责，方法级 Javadoc 含 @return）:
          - `HardwareMetricsDTO getCurrentMetrics();`
          - `List<HardwareMetricsDTO> getTrendHistory();`
          - `SystemInfoDTO getSystemInfo();`

    所有 DTO 属于 L0 层（不强制类级 Javadoc），但建议添加简短类职责注释。
    RingBuffer 属于 L2 层（鼓励类级 Javadoc 和方法说明）。
    HardwareMetricsService 属于 L2 层（鼓励接口级 Javadoc）。
  </action>
  <verify>
    <automated>cd springboot && ./gradlew.bat compileJava 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 所有 8 个 DTO 文件存在，`./gradlew.bat compileJava` 通过
    - `MetricValue` 包含泛型 `{value, format, unit}` 结构
    - `CpuMetricsDTO` 包含 `perCoreLoad: double[]` 数组字段
    - `HardwareMetricsDTO` 是顶层 DTO，嵌套所有子 DTO
    - `TrendDataPoint` 包含 `timestamp`, `value`, `metric` 字段
    - `RingBuffer` 编译通过，add/snapshot 方法使用 ReentrantReadWriteLock
    - `HardwareMetricsService` 接口定义 3 个方法，编译通过
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| HTTP 请求 → Controller | 客户端传入不可信请求参数 |

## STRIDE 威胁登记表

| 威胁 ID | 类别 | 组件 | 处置 | 缓解方案 |
|---------|------|------|------|----------|
| T-31-01 | 篡改 (Tampering) | libs.versions.toml / build.gradle.kts | 缓解 | OSHI artifact 来自 Maven Central，使用 checksum 验证（Gradle 自动） |
| T-31-02 | 拒绝服务 (DoS) | MonitoringSchedulerConfig | 缓解 | 单线程调度器，异常被 collect() 内部捕获，不会传播导致调度停止 |
</threat_model>

<verification>
- `./gradlew.bat compileJava` 全部通过
- `build.gradle.kts` 中 oshi-core-ffm 依赖已添加（`grep -n "oshi" build.gradle.kts`）
- `libs.versions.toml` 中 `oshi` 版本和 `oshi-core-ffm` library 存在
</verification>

<success_criteria>
1. OSHI 依赖引入，编译通过
2. 所有 8 个 DTO 定义了完整的硬件指标数据结构
3. RingBuffer 线程安全，360 容量
4. HardwareMetricsService 接口定义了 3 个方法的契约
5. MonitorHardwareProperties 可配置所有采集参数
6. MonitoringSchedulerConfig 提供独立调度线程池
</success_criteria>

<output>
创建 `./planning/phases/31-后端采集服务-REST-API/31-01-SUMMARY.md`（记录创建的文件、决策实现、编译验证结果）
</output>
