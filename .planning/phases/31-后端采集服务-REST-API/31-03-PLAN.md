---
phase: 31
plan: 03
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - springboot/src/main/java/cn/coderstory/springboot/controller/monitor/hardware/HardwareMonitorController.java
  - springboot/src/test/java/cn/coderstory/springboot/controller/monitor/hardware/HardwareMonitorControllerTest.java
  - springboot/src/test/java/cn/coderstory/springboot/service/monitor/hardware/HardwareMetricsServiceIT.java
autonomous: true
requirements:
  - HWM-07
  - HWM-08
  - HWM-09
must_haves:
  truths:
    - "GET /api/monitor/hardware/current 返回 HardwareMetricsDTO 完整快照 JSON"
    - "GET /api/monitor/hardware/trend?metric=cpu&range=360 返回趋势数据点列表"
    - "GET /api/monitor/hardware/system 返回 SystemInfoDTO 系统信息 JSON"
    - "非法 metric 参数返回 400 错误"
    - "range 参数超过 360 时截断到 360（RingBuffer 最大容量）"
    - "Controller 使用 @RequiredArgsConstructor，注入 HardwareMetricsService 接口（非实现类）"
    - "单元测试模拟 HardwareMetricsService，验证 3 个端点 HTTP 响应正确"
    - "集成测试在 Windows 环境加载真实 OSHI 验证全链路"
  artifacts:
    - path: "springboot/src/main/java/cn/coderstory/springboot/controller/monitor/hardware/HardwareMonitorController.java"
      provides: "3 个 REST 端点定义和趋势提取逻辑"
      min_lines: 80
    - path: "springboot/src/test/java/cn/coderstory/springboot/controller/monitor/hardware/HardwareMonitorControllerTest.java"
      provides: "Controller 单元测试（MockMvc + mock service）"
      min_lines: 100
    - path: "springboot/src/test/java/cn/coderstory/springboot/service/monitor/hardware/HardwareMetricsServiceIT.java"
      provides: "集成测试（@SpringBootTest + 真实 OSHI）"
      min_lines: 60
  key_links:
    - from: "HardwareMonitorController"
      to: "HardwareMetricsService"
      via: "@RequiredArgsConstructor 注入"
      pattern: "private final HardwareMetricsService"
    - from: "GET /current"
      to: "currentMetrics 缓存"
      via: "metricsService.getCurrentMetrics()"
      pattern: "getCurrentMetrics"
    - from: "GET /trend"
      to: "historyBuffer"
      via: "metricsService.getTrendHistory() → extractTrend()"
      pattern: "extractTrend"
---

<objective>
**目的:** 创建 REST API 端点，暴露硬件指标数据供前端和调试工具使用。实现趋势数据提取逻辑（按 metric 参数提取对应时间序列）。编写 Controller 单元测试和集成测试。

**输出:**
- `HardwareMonitorController.java` — 3 个 REST 端点
- `HardwareMonitorControllerTest.java` — MockMvc 单元测试
- `HardwareMetricsServiceIT.java` — 真实 OSHI 集成测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-后端采集服务-REST-API/31-RESEARCH.md (Controller 代码示例、集成测试模式)
@.planning/phases/31-后端采集服务-REST-API/31-CONTEXT.md (D-07, D-16 API 端点定义)
@springboot/src/main/java/cn/coderstory/springboot/controller/monitor/MonitorController.java (现有控制器模式)
@springboot/src/main/java/cn/coderstory/springboot/dto/ApiResponse.java (统一响应封装)
@springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/HardwareMetricsDTO.java
@springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/SystemInfoDTO.java
@springboot/src/main/java/cn/coderstory/springboot/dto/monitor/hardware/TrendDataPoint.java
@springboot/src/main/java/cn/coderstory/springboot/service/monitor/hardware/HardwareMetricsService.java

<interfaces>
<!-- HardwareMetricsService interface (from Plan 01) -->
HardwareMetricsDTO getCurrentMetrics();
List<HardwareMetricsDTO> getTrendHistory();
SystemInfoDTO getSystemInfo();

<!-- ApiResponse<T> -->
static <T> ApiResponse<T> success(T data)     // {code: 200, message: "success", data: ...}
static <T> ApiResponse<T> badRequest(String message)  // {code: 400, message: ..., data: null}

<!-- MonitorController 现有端点模式 (参考) -->
@RestController @RequestMapping("/api/monitor")
@RequiredArgsConstructor
ApiResponse<Map<String, Object>> getMetrics() → ApiResponse.success(data)

<!-- TrendDataPoint -->
{timestamp: long, value: double, metric: String}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>任务 1: 创建 HardwareMonitorController</name>
  <files>
    springboot/src/main/java/cn/coderstory/springboot/controller/monitor/hardware/HardwareMonitorController.java
  </files>
  <action>
    创建 `HardwareMonitorController.java`（D-07, D-16，参考 MonitorController 和 RESEARCH Controller 示例）:

    **包和修饰:**
    - 包: `cn.coderstory.springboot.controller.monitor.hardware`
    - `@RestController @RequestMapping("/api/monitor/hardware") @RequiredArgsConstructor @Slf4j`
    - 类级 Javadoc（L3 层强制）: 说明控制器的三个端点、职责和 @since 1.8.0
    - 注入 `private final HardwareMetricsService metricsService`（接口类型，非实现类）

    **端点 1: GET /api/monitor/hardware/current（HWM-07）**
    ```java
    @GetMapping("/current")
    public ApiResponse<HardwareMetricsDTO> getCurrent() {
        return ApiResponse.success(metricsService.getCurrentMetrics());
    }
    ```
    - 方法级 Javadoc（L3 层强制）: 说明返回当前硬件指标快照
    - 包含 @return 标签

    **端点 2: GET /api/monitor/hardware/trend（HWM-08）**
    ```java
    @GetMapping("/trend")
    public ApiResponse<List<TrendDataPoint>> getTrend(
            @RequestParam(defaultValue = "cpu") String metric,
            @RequestParam(defaultValue = "360") @Min(1) @Max(360) int range) {
        // 验证 metric 参数
        if (!SUPPORTED_METRICS.contains(metric)) {
            return ApiResponse.badRequest("不支持的指标: " + metric + "，支持: " + SUPPORTED_METRICS);
        }
        List<HardwareMetricsDTO> history = metricsService.getTrendHistory();
        List<TrendDataPoint> trend = extractTrend(history, metric, range);
        return ApiResponse.success(trend);
    }
    ```
    - 方法级 Javadoc（L3 层强制）: 说明按指标和时间范围查询趋势
    - 包含 @param metric 说明（cpu/memory/disk_read/disk_write/net_sent/net_recv）
    - 包含 @param range 说明（点数，最大 360）
    - 包含 @return 标签
    - 使用 `@Validated` 或手动校验（Spring Boot 4 兼容，项目无 @Validated 依赖则用手动校验）
    - 手动校验方式更安全（不引入新依赖）:
      ```java
      private static final Set<String> SUPPORTED_METRICS = Set.of(
          "cpu", "memory", "disk_read", "disk_write", "net_sent", "net_recv"
      );
      ```
    - range 限制: `int actualRange = Math.min(Math.max(range, 1), 360);`

    **端点 3: GET /api/monitor/hardware/system（HWM-09）**
    ```java
    @GetMapping("/system")
    public ApiResponse<SystemInfoDTO> getSystem() {
        return ApiResponse.success(metricsService.getSystemInfo());
    }
    ```
    - 方法级 Javadoc（L3 层强制）: 说明返回系统基本信息
    - 包含 @return 标签

    **extractTrend() 私有方法:**
    ```java
    private List<TrendDataPoint> extractTrend(List<HardwareMetricsDTO> history, String metric, int maxPoints) {
        List<TrendDataPoint> result = new ArrayList<>();
        // 按 range 截取最近的 N 个点
        int start = Math.max(0, history.size() - maxPoints);
        List<HardwareMetricsDTO> slice = history.subList(start, history.size());

        for (HardwareMetricsDTO dto : slice) {
            double value = switch (metric) {
                case "cpu" -> dto.getCpu() != null ? dto.getCpu().getSystemLoad() : 0.0;
                case "memory" -> dto.getMemory() != null ? dto.getMemory().getUsagePercent().getValue() : 0.0;
                case "disk_read" -> sumDiskReadBytes(dto.getDisks());
                case "disk_write" -> sumDiskWriteBytes(dto.getDisks());
                case "net_sent" -> sumNetSentBytes(dto.getNetwork());
                case "net_recv" -> sumNetRecvBytes(dto.getNetwork());
                default -> 0.0;
            };
            result.add(new TrendDataPoint(dto.getTimestamp(), value, metric));
        }
        return result;
    }
    ```

    辅助方法（所有 private static，提取到类底部，方法级 Javadoc L3 层强制）:
    - `sumDiskReadBytes(List<DiskMetricsDTO> disks)` — 对所有 disk 的 readBytesPerSec.value 求和
    - `sumDiskWriteBytes(List<DiskMetricsDTO> disks)` — 对所有 disk 的 writeBytesPerSec.value 求和
    - `sumNetSentBytes(List<NetworkMetricsDTO> nets)` — 对所有 net 的 bytesSentPerSec.value 求和
    - `sumNetRecvBytes(List<NetworkMetricsDTO> nets)` — 对所有 net 的 bytesRecvPerSec.value 求和
    - 每个方法判空，null 时返回 0.0

    **关键设计决策:**
    - 不引入 SSE 端点（属于 Phase 32，D-16 限制）
    - 端点路径使用 `/current`、`/trend`、`/system`（与 MonitorController `/metrics` 区分，避免路由冲突，D-16）
    - Controller 依赖 interface 而非 impl（TDD-01, D-13）
    - 所有响应使用 `ApiResponse.success()`（项目规范）
    - 错误处理返回 `ApiResponse.badRequest()` 而非抛异常（更明确）
    - 不添加安全注解（Phase 34 处理角色校验，D-16）
  </action>
  <verify>
    <automated>cd springboot && ./gradlew.bat compileJava 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `HardwareMonitorController.java` 编译通过
    - 3 个端点定义完整，方法级 Javadoc 完整
    - extractTrend 支持 6 种指标提取
    - metric 参数校验（非法参数返回 400）
    - range 参数截断（不超过 360）
    - 不引用具体实现类，只依赖 HardwareMetricsService 接口
  </done>
</task>

<task type="auto">
  <name>任务 2: Controller 单元测试 + 集成测试</name>
  <files>
    springboot/src/test/java/cn/coderstory/springboot/controller/monitor/hardware/HardwareMonitorControllerTest.java
    springboot/src/test/java/cn/coderstory/springboot/service/monitor/hardware/HardwareMetricsServiceIT.java
  </files>
  <action>
    1. 创建 `HardwareMonitorControllerTest.java`（MockMvc + @WebMvcTest）:

       - 包: `cn.coderstory.springboot.controller.monitor.hardware`
       - `@WebMvcTest(HardwareMonitorController.class)` — 仅加载 Controller 层
       - `@ExtendWith(MockitoExtension.class)`（@WebMvcTest 已包含，但需要显式导入）
       - `@MockBean private HardwareMetricsService metricsService;`
       - `@Autowired private MockMvc mockMvc;`
       - 类级 `@DisplayName("HardwareMonitorController 单元测试")`
       - 类级 Javadoc（L3 层强制）

       测试方法（每个端点一个 @Nested 类）:

       **GET /current 测试 (Nested):**
       - `正常返回HardwareMetricsDTO`:
         - when(metricsService.getCurrentMetrics()).thenReturn(mockHardwareMetricsDTO())
         - mockMvc.perform(get("/api/monitor/hardware/current"))
           .andExpect(status().isOk())
           .andExpect(jsonPath("$.code").value(200))
           .andExpect(jsonPath("$.data.cpu").exists())
       - `首次采集前返回empty DTO`:
         - when(metricsService.getCurrentMetrics()).thenReturn(HardwareMetricsDTO.empty())
         - mockMvc.perform(get("/api/monitor/hardware/current"))
           .andExpect(status().isOk())
           .andExpect(jsonPath("$.data.cpu.systemLoad").doesNotExist())

       **GET /trend 测试 (Nested):**
       - `cpu指标趋势返回趋势点列表`:
         - when(metricsService.getTrendHistory()).thenReturn(mockHistory())
         - mockMvc.perform(get("/api/monitor/hardware/trend")
           .param("metric", "cpu").param("range", "10"))
           .andExpect(status().isOk())
           .andExpect(jsonPath("$.data.length()").isNumber())
       - `非法metric返回400`:
         - mockMvc.perform(get("/api/monitor/hardware/trend")
           .param("metric", "invalid"))
           .andExpect(status().isOk())  // Controller 不抛异常，返回 400 业务码
           .andExpect(jsonPath("$.code").value(400))
       - `range超过360截断到360`（依赖 getTrendHistory 返回 360 条 mock 数据）
       - `memory指标提取usagePercent`

       **GET /system 测试 (Nested):**
       - `正常返回系统信息`:
         - when(metricsService.getSystemInfo()).thenReturn(mockSystemInfo())
         - mockMvc.perform(get("/api/monitor/hardware/system"))
           .andExpect(status().isOk())
           .andExpect(jsonPath("$.data.osFamily").value("Windows"))
       - `无数据时返回空字段`

       辅助方法:
       - `private HardwareMetricsDTO mockHardwareMetricsDTO()` — 构造完整 mock DTO
       - `private List<HardwareMetricsDTO> mockHistory()` — 构造 360 个模拟趋势点
       - `private SystemInfoDTO mockSystemInfo()` — 构造模拟系统信息

    2. 创建 `HardwareMetricsServiceIT.java`（@SpringBootTest，集成测试）:

       - 包: `cn.coderstory.springboot.service.monitor.hardware`
       - `@SpringBootTest` — 加载完整 Spring 上下文（含真实 OSHI）
       - `@Autowired private HardwareMetricsService metricsService;`
       - 类级 Javadoc（L3 层强制）
       - `@EnabledOnOs(OS.WINDOWS)` — 仅在 Windows 上执行（OSHI 采集需要 Windows API）
       - `@DisplayName("HardwareMetricsService 集成测试")`

       测试方法:
       - `@Test @DisplayName("Spring 上下文加载正常")`:
         - 验证 metricsService bean 不为 null
       - `@Test @DisplayName("getCurrentMetrics 返回完整指标快照")`:
         - 等待 3 秒（等待首次采集完成）
         - Thread.sleep(3000)
         - 验证返回的 DTO 中 cpu、memory、system 不为 null
         - 验证 disks 和 network 为 List
       - `@Test @DisplayName("getSystemInfo 返回操作系统信息")`:
         - SystemInfoDTO info = metricsService.getSystemInfo()
         - assertNotNull(info.getOsFamily())
         - assertTrue(info.getSystemUptime() > 0)
       - `@Test @DisplayName("getTrendHistory 返回趋势数据")`:
         - 等待 12 秒（确保至少 1 个趋势点写入）
         - 验证 history.size() >= 0（不强制 >0 以防超时）

       注意: 集成测试需要较长时间（等待 OSHI 采集），标记 `@Tag("integration")`，
       默认 `./gradlew.bat test` 排除集成测试（通过在 build.gradle.kts 中添加 filter，
       或简单地让集成测试类名以 `IT` 结尾而不是 `Test`）。

       **重要:** 由于集成测试需要真实 Windows OSHI 环境且耗时较长，
       在 build.gradle.kts 的 test 任务中排除 `*IT.java`:
       ```kotlin
       tasks.withType<Test> {
           useJUnitPlatform()
           exclude("**/*IT.class")
       }
       ```
       如果 build.gradle.kts 已存在 tasks.withType<Test> 块，追加 exclude 配置。
       集成测试通过 `./gradlew.bat test --tests "*HardwareMetricsServiceIT"` 单独运行。

    **关键设计决策:**
    - Controller 测试用 `@WebMvcTest` + `@MockBean`，不加载整个 Spring 上下文
    - 集成测试用 `@SpringBootTest`，仅 Windows 执行（`@EnabledOnOs(OS.WINDOWS)`）
    - IT 类名后缀 `IT` 而非 `Test`，自动被 exclude 排除（已配置）
    - 验证 JSON 响应路径使用 `jsonPath`，验证结构而非具体值
  </action>
  <verify>
    <automated>cd springboot && ./gradlew.bat test --tests "*HardwareMonitorControllerTest" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `HardwareMonitorControllerTest` 编译通过，MockMvc 测试绿色
    - GET /current 返回完整 DTO JSON，路径验证通过
    - GET /trend 对 6 种指标提取正确，非法参数返回 400
    - GET /system 返回系统信息
    - `HardwareMetricsServiceIT` 编译通过（仅 Windows 执行）
    - `./gradlew.bat test --tests "*HardwareMonitorControllerTest"` 全部通过
    - 所有测试覆盖 3 个端点正常路径和异常路径
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| HTTP 请求 → Controller | 客户端传入 metric 和 range 参数（不可信） |

## STRIDE 威胁登记表

| 威胁 ID | 类别 | 组件 | 处置 | 缓解方案 |
|---------|------|------|------|----------|
| T-31-06 | 篡改 (Tampering) | GET /trend | 缓解 | metric 参数白名单校验（仅允许 6 种已知指标名）；range 参数截断到 [1, 360] |
| T-31-07 | 信息泄露 (Information Disclosure) | 所有 3 个端点 | 接受 | 硬件指标非敏感 PII 数据，Phase 34 补充 ADMIN 角色校验。当前依赖已有 JWT 认证保护 |
</threat_model>

<verification>
- `./gradlew.bat compileJava` 编译通过
- `./gradlew.bat test --tests "*HardwareMonitorControllerTest"` 绿色
- 集成测试（可选 Windows 环境）: `./gradlew.bat test --tests "*HardwareMetricsServiceIT"`
</verification>

<success_criteria>
1. 3 个 REST 端点编译通过，路由路径正确
2. `/current` 返回 HardwareMetricsDTO 完整 JSON
3. `/trend` 支持 6 种指标（cpu/memory/disk_read/disk_write/net_sent/net_recv）
4. `/trend` 对非法 metric 返回 400 错误码
5. `/system` 返回 OS 版本、运行时间、进程数
6. extractTrend 对 null 字段安全处理（NPE 防护）
7. Controller 只依赖接口（不依赖具体实现类）
8. MockMvc 单元测试绿色，覆盖正常+异常路径
</success_criteria>

<output>
创建 `./planning/phases/31-后端采集服务-REST-API/31-03-SUMMARY.md`（记录端点规格、测试结果、验证截图）
</output>
