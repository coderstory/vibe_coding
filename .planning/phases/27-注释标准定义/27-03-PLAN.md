---
phase: 27-注释标准定义
plan: 03
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [STD-05]
must_haves:
  truths:
    - "CLAUDE.md 中 PR 检查清单的每个检查项定义了明确的通过/未通过判定标准"
    - "通过标准可直接用于 PR 评审，评审人无需额外判断"
    - "检查清单已通过真实代码抽样验证确认可执行"
  artifacts:
    - path: CLAUDE.md
      provides: "PR 检查清单的详细判定标准"
      contains: "通过标准 / 未通过标准"
  key_links:
    - from: "PR 检查清单判定标准"
      to: "Phase 28-30 注释评审"
      via: "评审人逐项勾选并通过/未通过判定"
---

<objective>

为 CLAUDE.md `## 注释规范 → ### 7. PR Review 检查清单` 补充每条检查项的详细通过/未通过判定标准，并通过抽样真实代码验证清单的可执行性。

Purpose: 检查清单仅有检查项名称不足以让评审人一致判定。明确的通过/未通过标准确保不同评审人对同一代码做出相同判断。
Output: CLAUDE.md 中 PR 检查清单扩展为包含判定标准的完整表格；抽样验证结果作为示例嵌入规范。

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-注释标准定义/27-CONTEXT.md（D-17, D-18, D-19）
@.planning/phases/27-注释标准定义/27-RESEARCH.md（§PR Review 注释检查清单）
@CLAUDE.md（查看 `## 注释规范 → ### 7. PR Review 检查清单` 当前内容）

<interfaces>
<!-- 当前 PR 检查清单结构（来自 27-01-PLAN.md 输出）：
共有 9 项检查，每项有 #、检查项、层级、严重度 四个字段。
本计划将增加"通过标准"和"未通过标准"两个字段。
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 补充每条检查项的判定标准并写入 CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
  - CLAUDE.md（确认 `### 7. PR Review 检查清单` 所在位置和当前表格结构）
  - 27-CONTEXT.md（D-17~D-19）
  - 27-RESEARCH.md（§PR Review — 检查项的判定逻辑）
  </read_first>
  <action>
  在 CLAUDE.md 中找到 `### 7. PR Review 检查清单` 子章节。当前已有一个 9 行检查表，需在表格后方追加"判定标准说明"段落，为每条检查项定义详细的通过/未通过判定标准。

  追加内容格式如下（在当前检查表之后）：

  ```
  #### 判定标准

  每条检查项的通过/未通过判定标准：

  **1. L3 层文件是否有类级职责 Javadoc？**
  - 通过：类声明正上方存在 `/** ... */` 块注释，包含至少 10 个中文字符的职责描述，且包含 `@since x.x.x` 版本号
  - 未通过：类声明上方无 Javadoc；或 Javadoc 仅含 `@since` 无职责描述；或描述不足 10 个中文字符

  **2. L3 层方法级是否有 @param/@return（有返回值时）？**
  - 通过：方法声明上方存在 `/** ... */`，每个参数有 `@param` 标签且描述非空；有返回值时包含 `@return` 标签且描述非空
  - 未通过：缺少 `@param`（参数 > 0 时）；有返回值但缺少 `@return`；标签后描述为空（空骨架）

  **3. 是否存在空骨架注释（有标签无内容）？**
  - 通过：所有 Javadoc 标签（@param/@return/@throws）的描述字段均有实际内容（至少 2 个中文字符或英文术语）
  - 未通过：存在 `@param name` 后直接换行或 `@param name */` 的标签；存在 `@return` 后直接换行或 `@return */` 的标签
  - 判定方法：执行 `grep -rn '@param\s\+\w\+\s*\*/'` 和 `grep -rn '@return\s*\*/'` 扫描目标文件

  **4. 是否存在被注释掉的代码块？**
  - 通过：源文件中无被注释的 Java/TS 代码行（以 `//` 开头但内容是代码而非说明的行；或以 `/* ... */` 包裹的代码块）
  - 未通过：存在被注释掉的方法调用、变量声明、逻辑代码（判断标准：注释内容包含 Java/TS 关键字如 `public`、`private`、`function`、`if`、`for`、`import`、`const`、`let`、`var`）
  - 例外：TODO 注释中包含的代码片段引用不属于注释掉的代码块
  - 判定方法：执行 `grep -rn '^\s*//\s*\(public\|private\|protected\|import\|const\|let\|var\|function\)'` 扫描目标文件

  **5. TODO 是否都关联了 Issue 编号？**
  - 通过：每个 `TODO` 后紧跟 `(#数字):` 或 `(#TODO-数字):` 格式
  - 未通过：存在 `TODO:` 或 `TODO(描述)` 而不包含 `(#数字)`
  - 判定方法：执行 `grep -rn 'TODO[^#]'` 或 `grep -rn 'TODO'` 并手动检查不符合 `#N` 格式的项

  **6. 中英文混用？**
  - 通过：Javadoc 描述为纯中文（仅技术术语保留英文，如 JWT、RocketMQ、token、cache、DTO、VO、API）；或纯英文技术描述
  - 未通过：中英文混写，如"获取 user by id"、"根据 Id 查询"
  - 判定方法：目视检查 Javadoc 描述段落

  **7. 是否存在 @author 标签？**
  - 通过：无 `@author` 标签出现在任何文件中
  - 未通过：任何文件包含 `@author` 标签
  - 判定方法：执行 `grep -rn '@author'` 扫描

  **8. L2 层 Vue 组件是否有职责说明？**
  - 通过：组件文件包含 `<script lang="ts">` 块（非 setup）并在其 `/** */` 中描述组件职责；或文件顶部有 `//` 行注释说明职责
  - 未通过：组件文件无任何形式的职责说明
  - 例外：极简组件（如仅 1 个标签的包装组件）可通过 `/^<template>/` 规则豁免

  **9. L2 层 Service 接口是否有类级 Javadoc？**
  - 通过：接口声明上方存在 `/** ... */` 块注释，包含职责描述
  - 未通过：接口声明上方无 Javadoc，或仅有单行 `//` 注释
  ```

  将上述判定标准内容追加在现有检查表之后（保持表格不动，在其后新增 `#### 判定标准` 子标题和逐项标准说明）。确保每条标准包含具体的判定方法（grep 命令或目视检查规则）。
  </action>
  <acceptance_criteria>
  - [ ] `grep -c "#### 判定标准" CLAUDE.md` 返回 1
  - [ ] `grep -c "**1\." CLAUDE.md` 在 `### 7. PR Review 检查清单` 区域内 >= 1
  - [ ] `grep -c "**9\." CLAUDE.md` 在 `### 7. PR Review 检查清单` 区域内 >= 1
  - [ ] 每条判定标准包含"通过"和"未通过"两条判定条件
  - [ ] 至少 3 条判定标准包含具体的 grep 判定方法
  - [ ] `grep -c "grep -rn" CLAUDE.md` 返回 >= 3（判定方法中的命令示例）
  </acceptance_criteria>
  <verify>
  <automated>grep -c "#### 判定标准" CLAUDE.md && grep -c "grep -rn '@author'" CLAUDE.md</automated>
  </verify>
  <done>
  - PR 检查清单的 9 项检查各有明确通过/未通过标准
  - 至少 5 项标准包含自动化判定方法（grep 命令）
  - 判定标准已写入 CLAUDE.md 的 `### 7. PR Review 检查清单` 子章节
  </done>
</task>

<task type="auto">
  <name>Task 2: 抽样验证检查清单在现有代码上的可执行性</name>
  <files>CLAUDE.md</files>
  <read_first>
  - CLAUDE.md（确认 `### 7. PR Review 检查清单 → #### 判定标准` 已存在）
  - 以下 L3 层代表性文件的当前内容（用于抽样验证）：
    - springboot/src/main/java/cn/coderstory/springboot/shared/config/
    - springboot/src/main/java/cn/coderstory/springboot/shared/security/
    - springboot/src/main/java/cn/coderstory/springboot/shared/exception/BusinessException.java
    - springboot/src/main/java/cn/coderstory/springboot/shared/aspect/AuditAspect.java
  </read_first>
  <action>
  1. 选择 3 个 L3 层文件（Config 类、Controller 类、JWT 相关类各一个）作为抽样样本。使用以下命令确定具体文件路径（选择最典型的文件）：

  ```bash
  # 找出 config 包中的配置类
  ls springboot/src/main/java/cn/coderstory/springboot/shared/config/
  
  # 找出任意一个 Controller
  find springboot/src/main/java/cn/coderstory/springboot -name "*Controller.java" | head -3
  
  # 找出 JWT 相关类
  find springboot/src/main/java/cn/coderstory/springboot/shared/security -name "*.java"
  ```

  2. 对每个样本文件，逐条运行 1-5 号检查项（L3 blocking 项），记录判定结果到临时缓冲区：

  ```bash
  # 检查项 1：类级 Javadoc
  head -20 $FILE | grep -c '/\*\*'
  
  # 检查项 3：空骨架注释
  grep -n '@param\s\+\w\+\s*\*/' $FILE
  
  # 检查项 4：注释代码块
  grep -n '^\s*//\s*\(public\|private\|import\|const\)' $FILE
  
  # 检查项 5：TODO Issue 引用
  grep -n 'TODO' $FILE
  
  # 检查项 7：@author
  grep -n '@author' $FILE
  ```

  3. 将验证结果和典型样例追加到 CLAUDE.md `### 7. PR Review 检查清单` 子章节末尾，格式：

  ```
  #### 验证示例

  以下通过对真实代码的抽样验证展示检查清单的判定过程：

  **样本 1：`{文件路径}`**
  - 检查项 1（类级 Javadoc）：{通过/未通过} — {说明，如"存在类级 Javadoc 包含职责说明和 @since 1.4.0"}
  - 检查项 3（空骨架注释）：{通过/未通过} — {说明}
  - ...

  **样本 2：`{文件路径}`**
  - 检查项 1（类级 Javadoc）：{通过/未通过} — {说明}
  - ...

  **样本 3：`{文件路径}`**
  - 检查项 1（类级 Javadoc）：{通过/未通过} — {说明}
  - ...
  ```

  使用实际验证结果填充，让 Phase 28-30 的评审人可以看到真实判定示例。
  </action>
  <acceptance_criteria>
  - [ ] 已选择 3 个代表性 L3 文件并读取其内容
  - [ ] 对每个文件运行了 1-5 号检查项的判定命令
  - [ ] 验证结果已写入 CLAUDE.md `### 7. PR Review 检查清单` 子章节
  - [ ] 验证示例使用真实文件路径和真实判定结果
  - [ ] CLAUDE.md 中 `#### 验证示例` 标题存在且包含至少 3 个样本
  </acceptance_criteria>
  <verify>
  <automated>grep -c "#### 验证示例" CLAUDE.md && grep -c "样本 [1-3]" CLAUDE.md</automated>
  </verify>
  <done>
  - 3 个 L3 层样本文件完成检查清单验证
  - 验证结果作为可执行性证明写入 CLAUDE.md
  - 检查清单的判定标准的可操作性和一致性得到验证
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
本阶段仅编辑 CLAUDE.md 中的注释规范文档，不修改任何源代码，不改变系统行为。

## STRIDE Threat Register
N/A — 纯文档编辑，无可攻击面。
</threat_model>

<verification>

### must_haves
1. `grep -c "#### 判定标准" CLAUDE.md` 返回 1
2. `grep -c "#### 验证示例" CLAUDE.md` 返回 1
3. 所有 9 项检查都有通过/未通过标准
4. 至少 3 条标准包含 grep 判定命令
5. 验证示例使用真实代码路径

### truths
- 检查清单可执行：每条检查项有明确的客观判定标准
- 验证示例证明清单可在真实代码上运行
- Phase 28-30 的评审人可一致地应用该清单
</verification>

<success_criteria>
1. CLAUDE.md `### 7. PR Review 检查清单` 包含三项内容：检查表、判定标准、验证示例
2. 判定标准可直接指导 PR 评审人做出客观判定
3. 验证示例证明判定标准可在真实代码上一致应用
</success_criteria>

<output>
After completion, create `.planning/phases/27-注释标准定义/27-03-SUMMARY.md`
</output>
