---
phase: 02-user-permissions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - springboot/src/main/resources/schema.sql
  - springboot/src/main/java/cn/coderstory/springboot/entity/User.java
  - springboot/src/main/java/cn/coderstory/springboot/entity/Role.java
  - springboot/src/main/java/cn/coderstory/springboot/entity/Menu.java
  - springboot/src/main/java/cn/coderstory/springboot/entity/RoleMenuPermission.java
  - springboot/src/main/java/cn/coderstory/springboot/mapper/RoleMapper.java
  - springboot/src/main/java/cn/coderstory/springboot/mapper/MenuMapper.java
  - springboot/src/main/java/cn/coderstory/springboot/service/RoleService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/MenuService.java
  - springboot/src/main/java/cn/coderstory/springboot/controller/RoleController.java
  - springboot/src/main/java/cn/coderstory/springboot/controller/MenuController.java
autonomous: true
requirements:
  - ROLE-01
  - ROLE-02
  - ROLE-03
  - ROLE-04
  - MENU-02
must_haves:
  truths:
    - "Admin can view role list with pagination"
    - "Admin can create role with name, code, description"
    - "Admin can edit existing role"
    - "Admin can delete role (soft delete)"
    - "Admin can assign menu permissions to role via tree checkbox"
    - "User sees only menus they have permission to access"
  artifacts:
    - path: springboot/src/main/resources/schema.sql
      provides: "Database schema for sys_role, sys_menu, sys_role_menu"
      contains: "CREATE TABLE sys_role"
    - path: springboot/src/main/java/cn/coderstory/springboot/entity/Role.java
      provides: "Role entity with MyBatis-Plus annotations"
      exports: Role
    - path: springboot/src/main/java/cn/coderstory/springboot/entity/Menu.java
      provides: "Menu entity with hierarchical structure"
      exports: Menu
    - path: springboot/src/main/java/cn/coderstory/springboot/entity/RoleMenuPermission.java
      provides: "Role-Menu many-to-many association"
      exports: RoleMenuPermission
    - path: springboot/src/main/java/cn/coderstory/springboot/controller/RoleController.java
      provides: "Role CRUD API endpoints"
      exports: "GET /api/roles, POST /api/roles, PUT /api/roles/{id}, DELETE /api/roles/{id}"
    - path: springboot/src/main/java/cn/coderstory/springboot/controller/MenuController.java
      provides: "Menu tree API for permission filtering"
      exports: "GET /api/menus/tree, GET /api/menus/tree/{roleId}"
  key_links:
    - from: springboot/src/main/java/cn/coderstory/springboot/entity/RoleMenuPermission.java
      to: springboot/src/main/java/cn/coderstory/springboot/entity/Role.java
      via: role_id foreign key
    - from: springboot/src/main/java/cn/coderstory/springboot/entity/RoleMenuPermission.java
      to: springboot/src/main/java/cn/coderstory/springboot/entity/Menu.java
      via: menu_id foreign key
---

<objective>
创建角色管理与菜单权限后端基础设施。包括数据库Schema扩展、实体类、Mapper、Service、Controller。

Purpose: 为前端提供角色CRUD API和菜单权限API，建立RBAC权限模型的数据基础
Output: 后端角色管理API、菜单权限API
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-user-permissions/02-RESEARCH.md
@.planning/phases/02-user-permissions/02-CONTEXT.md
@springboot/src/main/java/cn/coderstory/springboot/entity/User.java
</context>

<interfaces>
<!-- From existing codebase -->
From springboot/src/main/java/cn/coderstory/springboot/entity/User.java:
```java
@Data
@TableName("sys_user")
public class User {
    @TableId(type = IdType.AUTO)
    private Long id;
    private String username;
    private String password;
    private String name;
    private Long roleId;
    @TableLogic
    private Integer deleted;
    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;
    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: 更新数据库Schema - 添加角色和菜单表</name>
  <files>springboot/src/main/resources/schema.sql</files>
  <read_first>springboot/src/main/resources/schema.sql</read_first>
  <action>
    在schema.sql末尾添加以下表创建语句（注意：不要修改现有sys_user表的定义，只添加ALTER语句扩展字段）：

    ```sql
    -- 角色表
    CREATE TABLE sys_role (
        id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键',
        role_name VARCHAR(50) NOT NULL COMMENT '角色名称',
        role_code VARCHAR(50) NOT NULL COMMENT '角色编码',
        description VARCHAR(255) COMMENT '描述',
        deleted TINYINT DEFAULT 0 COMMENT '删除标记',
        create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
        update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
        UNIQUE KEY uk_role_code (role_code)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';

    -- 菜单表（支持层级结构）
    CREATE TABLE sys_menu (
        id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键',
        parent_id BIGINT DEFAULT 0 COMMENT '父菜单ID',
        name VARCHAR(50) NOT NULL COMMENT '菜单名称',
        path VARCHAR(255) COMMENT '路由路径',
        icon VARCHAR(50) COMMENT '图标',
        sort_order INT DEFAULT 0 COMMENT '排序',
        deleted TINYINT DEFAULT 0 COMMENT '删除标记',
        create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜单表';

    -- 角色-菜单权限关联表
    CREATE TABLE sys_role_menu (
        id BIGINT PRIMARY KEY AUTO_INCREMENT,
        role_id BIGINT NOT NULL COMMENT '角色ID',
        menu_id BIGINT NOT NULL COMMENT '菜单ID',
        UNIQUE KEY uk_role_menu (role_id, menu_id)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色菜单关联表';

    -- 修改sys_user表（添加D-01要求的字段）
    ALTER TABLE sys_user 
    ADD COLUMN gender TINYINT COMMENT '性别(0女1男)' AFTER name,
    ADD COLUMN avatar VARCHAR(255) COMMENT '头像URL' AFTER gender,
    ADD COLUMN email VARCHAR(100) COMMENT '邮箱' AFTER role_id,
    ADD COLUMN department VARCHAR(100) COMMENT '部门' AFTER email,
    ADD COLUMN position VARCHAR(100) COMMENT '岗位' AFTER department,
    ADD COLUMN enabled TINYINT DEFAULT 1 COMMENT '是否启用(0禁用1启用)' AFTER position;

    -- 插入初始管理员角色（假设admin用户role_id=1）
    INSERT INTO sys_role (id, role_name, role_code, description) VALUES (1, '超级管理员', 'ADMIN', '拥有所有权限');

    -- 插入初始菜单数据（系统管理菜单）
    INSERT INTO sys_menu (id, parent_id, name, path, icon, sort_order) VALUES
    (1, 0, '系统管理', '', 'Setting', 1),
    (2, 1, '用户管理', '/system/user', 'User', 1),
    (3, 1, '角色管理', '/system/role', 'Key', 2),
    (4, 1, '审计日志', '/system/audit', 'Document', 3),
    (5, 0, '业务数据', '', 'Goods', 2),
    (6, 5, '数据列表', '/business/data', 'List', 1),
    (7, 0, '首页', '/dashboard', 'HomeFilled', 0);

    -- 初始角色分配所有菜单权限
    INSERT INTO sys_role_menu (role_id, menu_id) VALUES
    (1, 1), (1, 2), (1, 3), (1, 4), (1, 5), (1, 6), (1, 7);
    ```

    注意：ALTER TABLE语句会在已有sys_user表上添加新字段，保持原有数据不变。
  </action>
  <verify>
    <automated>mysql -u root -p -e "USE your_database; DESCRIBE sys_role; DESCRIBE sys_menu; DESCRIBE sys_role_menu;"</automated>
  </verify>
  <done>schema.sql包含完整的角色、菜单、角色菜单关联表定义，并包含ALTER语句扩展sys_user表</done>
</task>

<task type="auto">
  <name>task 2: 创建实体类 - Role, Menu, RoleMenuPermission</name>
  <files>
    springboot/src/main/java/cn/coderstory/springboot/entity/Role.java
    springboot/src/main/java/cn/coderstory/springboot/entity/Menu.java
    springboot/src/main/java/cn/coderstory/springboot/entity/RoleMenuPermission.java
  </files>
  <action>
    创建以下三个实体类（参考02-RESEARCH.md中的代码示例）：

    1. Role.java - 角色实体，包含：id, roleName, roleCode, description, deleted, createTime, updateTime
       - 使用 @TableName("sys_role")
       - 使用 @TableLogic 注解标记deleted字段
       - 使用 @TableField(fill = FieldFill.INSERT) 标记时间字段

    2. Menu.java - 菜单实体，包含：id, parentId, name, path, icon, sortOrder, deleted, createTime
       - 使用 @TableName("sys_menu")
       - parentId默认0表示顶级菜单
       - 使用 @TableLogic 注解标记deleted字段

    3. RoleMenuPermission.java - 角色菜单关联实体，包含：id, roleId, menuId
       - 使用 @TableName("sys_role_menu")
       - 无软删除字段（关联表）
       - roleId和menuId不要用驼峰映射，保持数据库下划线命名

    同时更新 User.java：
    - 添加字段：gender (Integer), avatar (String), email (String), department (String), position (String), enabled (Integer)
    - 这些字段需要添加在 roleId 字段之后（参考schema.sql的ALTER语句顺序）
  </action>
  <verify>
    <automated>cd springboot && mvn compile -q 2>&1 | head -30</automated>
  </verify>
  <done>Role.java, Menu.java, RoleMenuPermission.java三个实体类创建完成，User.java字段扩展完成</done>
</task>

<task type="auto">
  <name>task 3: 创建Role和Menu的Mapper接口</name>
  <files>
    springboot/src/main/java/cn/coderstory/springboot/mapper/RoleMapper.java
    springboot/src/main/java/cn/coderstory/springboot/mapper/MenuMapper.java
  </files>
  <action>
    创建RoleMapper.java和MenuMapper.java：

    1. RoleMapper.java：
       - 继承 BaseMapper<Role>
       - 使用 @Mapper 注解
       - 可选：添加分页查询方法 selectRolePage(IPage, roleName)

    2. MenuMapper.java：
       - 继承 BaseMapper<Menu>
       - 使用 @Mapper 注解
       - 添加方法：List<Menu> selectMenuTree() - 查询所有菜单用于构建树
       - 添加方法：List<Menu> selectByRoleId(Long roleId) - 根据角色ID查询菜单权限
  </action>
  <verify>
    <automated>cd springboot && mvn compile -q 2>&1 | head -30</automated>
  </verify>
  <done>RoleMapper.java和MenuMapper.java创建完成，编译通过</done>
</task>

<task type="auto">
  <name>task 4: 创建RoleService和MenuService</name>
  <files>
    springboot/src/main/java/cn/coderstory/springboot/service/RoleService.java
    springboot/src/main/java/cn/coderstory/springboot/service/RoleServiceImpl.java
    springboot/src/main/java/cn/coderstory/springboot/service/MenuService.java
    springboot/src/main/java/cn/coderstory/springboot/service/MenuServiceImpl.java
  </files>
  <action>
    创建RoleService和MenuService及其实现类：

    RoleService接口定义：
    - IPage<Role> getRolePage(Page, roleName) - 分页查询
    - boolean saveRole(Role) - 新增角色
    - boolean updateRole(Role) - 更新角色
    - boolean deleteRole(Long id) - 删除角色（软删除）
    - List<Menu> getMenusByRoleId(Long roleId) - 获取角色菜单权限
    - boolean assignMenus(Long roleId, List<Long> menuIds) - 分配菜单权限

    RoleServiceImpl实现：
    - 使用 @Service
    - 使用 @RequiredArgsConstructor 注入 RoleMapper, MenuMapper
    - assignMenus方法：先删除该角色所有菜单权限，再插入新的权限

    MenuService接口定义：
    - List<Menu> getMenuTree() - 获取完整菜单树
    - List<Menu> getMenuTreeByRoleId(Long roleId) - 获取角色菜单权限树

    MenuServiceImpl实现：
    - 使用 @Service
    - 使用 @RequiredArgsConstructor 注入 MenuMapper
    - getMenuTree：查询所有菜单后，在Java中组装成树结构返回

    注意：MenuMapper中需要添加：
    - List<Menu> selectAllOrderBySortOrder() - 查询所有菜单按sortOrder排序
  </action>
  <verify>
    <automated>cd springboot && mvn compile -q 2>&1 | head -30</automated>
  </verify>
  <done>RoleService和MenuService及其实现类创建完成</done>
</task>

<task type="auto">
  <name>task 5: 创建RoleController和MenuController</name>
  <files>
    springboot/src/main/java/cn/coderstory/springboot/controller/RoleController.java
    springboot/src/main/java/cn/coderstory/springboot/controller/MenuController.java
  </files>
  <action>
    创建RoleController.java：
    - 使用 @RestController
    - 使用 @RequestMapping("/api/roles")
    - 使用 @RequiredArgsConstructor 注入 RoleService

    Endpoints:
    - GET /api/roles?roleName=&page=1&size=20 - 分页查询角色列表
    - GET /api/roles/{id} - 获取单个角色详情
    - POST /api/roles - 新增角色
    - PUT /api/roles/{id} - 更新角色
    - DELETE /api/roles/{id} - 删除角色（软删除）
    - GET /api/roles/{id}/menus - 获取角色菜单权限
    - PUT /api/roles/{id}/menus - 分配菜单权限

    创建MenuController.java：
    - 使用 @RestController
    - 使用 @RequestMapping("/api/menus")
    - 使用 @RequiredArgsConstructor 注入 MenuService

    Endpoints:
    - GET /api/menus/tree - 获取完整菜单树（用于权限分配）
    - GET /api/menus/tree/{roleId} - 获取角色菜单权限树
    - GET /api/menus/user/{userId} - 获取用户实际可见菜单（根据用户角色获取）
      此接口用于MENU-02：用户只能看到有权限访问的菜单项

    统一响应格式：{code: 200, message: "success", data: {}}
    使用 @Slf4j 记录日志
  </action>
  <verify>
    <automated>cd springboot && mvn compile -q 2>&1 | head -30</automated>
  </verify>
  <done>RoleController和MenuController创建完成，编译通过</done>
</task>

</tasks>

<verification>
- [ ] schema.sql包含完整的sys_role, sys_menu, sys_role_menu表定义
- [ ] schema.sql包含ALTER TABLE扩展sys_user的语句
- [ ] 实体类Role.java, Menu.java, RoleMenuPermission.java编译通过
- [ ] User.java已扩展D-01要求的字段
- [ ] RoleService和MenuService接口和实现类完整
- [ ] RoleController和MenuController提供完整的REST API
- [ ] MenuController提供GET /api/menus/user/{userId}用于MENU-02权限过滤
</verification>

<success_criteria>
- 数据库Schema包含完整的角色、菜单、权限表结构
- 后端提供角色CRUD的REST API
- 后端提供菜单树API，支持按角色获取菜单权限
- 后端提供用户可见菜单API（用于前端动态菜单过滤）
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-permissions/02-01-SUMMARY.md`
</output>
