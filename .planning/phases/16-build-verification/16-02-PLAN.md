---
phase: 16-build-verification
plan: "02"
type: execute
wave: 1
depends_on: ["16-01"]
files_modified: []
autonomous: true
requirements:
  - BUILD-04
  - BUILD-05
  - BUILD-06
  - BUILD-07

must_haves:
  truths:
    - "Flyway 数据库迁移正常工作"
    - "Redis 连接正常"
    - "RocketMQ 生产者/消费者正常"
    - "JWT 认证流程正常"
  artifacts:
    - path: "springboot/src/main/resources/application.yaml"
      provides: "基础设施配置"

objectives:
  - 验证 Flyway 数据库迁移
  - 验证 Redis 连接
  - 验证 RocketMQ 连接
  - 验证 JWT 认证
---

<objective>
验证基础设施组件正常工作：Flyway 数据库迁移、Redis 连接、RocketMQ 消息队列、JWT 认证。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 验证 Flyway 数据库迁移</name>
  <files>springboot/src/main/resources/application.yaml</files>
  <action>
验证 Flyway 迁移正常工作:

1. 启动应用 (./gradlew bootRun)
2. 检查日志中 Flyway 迁移信息
3. 验证数据库表已创建:
   - 检查 auth_user, auth_role 等表是否存在
4. 停止应用

注意: 需要 MySQL 运行在 localhost:3306
</action>
  <read_first>
springboot/src/main/resources/application.yaml
springboot/src/main/resources/db/migration/*.sql
</read_first>
  <acceptance_criteria>
1. bootRun 日志显示 "Flyway Migration" 相关成功信息
2. 数据库中存在应用表 (可通过日志或连接验证)
</acceptance_criteria>
  <done>
Flyway 数据库迁移验证完成
</done>
</task>

<task type="auto">
  <name>Task 2: 验证 Redis 连接</name>
  <files>springboot/src/main/resources/application.yaml</files>
  <action>
验证 Redis 连接正常:

1. 确认 Redis 运行在 localhost:6379
2. 启动应用
3. 检查日志中 Redis 连接成功信息
4. 测试分布式锁功能 (如果可用)
5. 停止应用

日志关键字: "Redis connection successful" 或 "Connected to Redis"
</action>
  <read_first>
springboot/src/main/resources/application.yaml
</read_first>
  <acceptance_criteria>
1. 应用启动时 Redis 连接成功
2. 日志无 Redis 连接错误
</acceptance_criteria>
  <done>
Redis 连接验证完成
</done>
</task>

<task type="auto">
  <name>Task 3: 验证 RocketMQ 连接</name>
  <files>springboot/src/main/resources/application.yaml</files>
  <action>
验证 RocketMQ 生产者/消费者正常:

1. 确认 RocketMQ NameServer 运行在 localhost:9876
2. 启动应用
3. 检查日志中 RocketMQ 连接信息
4. 验证生产者/消费者注册成功
5. 停止应用

日志关键字: "RocketMQ" + "start" 或 "register"
</action>
  <read_first>
springboot/src/main/resources/application.yaml
</read_first>
  <acceptance_criteria>
1. 应用启动时 RocketMQ 连接成功
2. 日志无 RocketMQ 连接错误
</acceptance_criteria>
  <done>
RocketMQ 连接验证完成
</done>
</task>

<task type="auto">
  <name>Task 4: 验证 JWT 认证流程</name>
  <files>springboot/src/main/java</files>
  <action>
验证 JWT 认证流程:

1. 启动应用
2. 调用登录接口获取 token:
   POST http://localhost:8080/api/auth/login
   Body: {"username": "admin", "password": "admin123"}
3. 使用 token 访问需要认证的接口
4. 验证 token 刷新功能
5. 停止应用

注意: 需要有初始管理员账户
</action>
  <read_first>
springboot/src/main/java/cn/coderstory/springboot/security/JwtAuthenticationFilter.java
springboot/src/main/java/cn/coderstory/springboot/controller/AuthController.java
</read_first>
  <acceptance_criteria>
1. 登录接口返回 token
2. 带 token 的请求可以访问受保护资源
3. token 过期后可以刷新
</acceptance_criteria>
  <done>
JWT 认证流程验证完成
</done>
</task>

</tasks>

<verification>
## Phase 16 Plan 02 Verification

1. BUILD-04: Flyway 迁移成功
2. BUILD-05: Redis 连接正常
3. BUILD-06: RocketMQ 连接正常
4. BUILD-07: JWT 认证正常
</verification>

<success_criteria>
1. Flyway 数据库迁移正常工作
2. Redis 连接成功
3. RocketMQ 生产者/消费者注册成功
4. JWT 认证流程正常
</success_criteria>
