---
phase: 16-build-verification
plan: "03"
type: execute
wave: 1
depends_on: ["16-02"]
files_modified: []
autonomous: true
requirements:
  - REGR-01
  - REGR-02
  - REGR-03
  - REGR-04
  - REGR-05
  - REGR-06
  - REGR-07

must_haves:
  truths:
    - "用户登录/登出正常"
    - "用户管理 CRUD 正常"
    - "角色管理正常"
    - "菜单管理正常"
    - "审计日志正常"
    - "知识库正常"
    - "秒杀系统正常"
  artifacts: []

objectives:
  - 验证所有功能模块回归测试通过
---

<objective>
验证所有功能模块回归测试：用户管理、角色管理、菜单管理、审计日志、知识库、秒杀系统。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 用户登录/登出验证 (REGR-01)</name>
  <files>springboot/src/main/java</files>
  <action>
验证用户登录/登出功能:

1. 启动应用
2. POST /api/auth/login 测试登录
3. POST /api/auth/logout 测试登出
4. 验证错误密码时的错误处理
5. 停止应用
</action>
  <read_first>
springboot/src/main/java/cn/coderstory/springboot/controller/AuthController.java
</read_first>
  <acceptance_criteria>
1. 正确凭据登录成功，返回 token
2. 错误凭据登录返回 401
3. 登出成功
</acceptance_criteria>
  <done>
用户登录/登出验证完成
</done>
</task>

<task type="auto">
  <name>Task 2: 用户管理 CRUD 验证 (REGR-02)</name>
  <files>springboot/src/main/java</files>
  <action>
验证用户管理 CRUD 功能:

1. 启动应用，登录获取 token
2. GET /api/users 列表用户
3. POST /api/users 创建用户
4. PUT /api/users/{id} 更新用户
5. DELETE /api/users/{id} 删除用户
6. 停止应用
</action>
  <read_first>
springboot/src/main/java/cn/coderstory/springboot/controller/UserController.java
</read_first>
  <acceptance_criteria>
1. 用户列表 API 返回 200
2. 创建用户成功
3. 更新用户成功
4. 删除用户成功
</acceptance_criteria>
  <done>
用户管理 CRUD 验证完成
</done>
</task>

<task type="auto">
  <name>Task 3: 角色管理验证 (REGR-03)</name>
  <files>springboot/src/main/java</files>
  <action>
验证角色管理功能:

1. 启动应用，登录获取 token
2. GET /api/roles 列表角色
3. POST /api/roles 创建角色
4. PUT /api/roles/{id} 更新角色
5. DELETE /api/roles/{id} 删除角色
6. 停止应用
</action>
  <read_first>
springboot/src/main/java/cn/coderstory/springboot/controller/RoleController.java
</read_first>
  <acceptance_criteria>
1. 角色列表 API 返回 200
2. 创建角色成功
3. 更新角色成功
4. 删除角色成功
</acceptance_criteria>
  <done>
角色管理验证完成
</done>
</task>

<task type="auto">
  <name>Task 4: 菜单管理验证 (REGR-04)</name>
  <files>springboot/src/main/java</files>
  <action>
验证菜单管理功能:

1. 启动应用，登录获取 token
2. GET /api/menus 列表菜单
3. POST /api/menus 创建菜单
4. PUT /api/menus/{id} 更新菜单
5. DELETE /api/menus/{id} 删除菜单
6. 停止应用
</action>
  <read_first>
springboot/src/main/java/cn/coderstory/springboot/controller/MenuController.java
</read_first>
  <acceptance_criteria>
1. 菜单列表 API 返回 200
2. 创建菜单成功
3. 更新菜单成功
4. 删除菜单成功
</acceptance_criteria>
  <done>
菜单管理验证完成
</done>
</task>

<task type="auto">
  <name>Task 5: 审计日志验证 (REGR-05)</name>
  <files>springboot/src/main/java</files>
  <action>
验证审计日志功能:

1. 启动应用，登录获取 token
2. 执行一些操作产生审计日志
3. GET /api/audit/logs 查看审计日志
4. 验证日志内容包含操作信息
5. 停止应用
</action>
  <read_first>
springboot/src/main/java/cn/coderstory/springboot/controller/AuditController.java
</read_first>
  <acceptance_criteria>
1. 审计日志 API 返回 200
2. 日志包含操作记录
</acceptance_criteria>
  <done>
审计日志验证完成
</done>
</task>

<task type="auto">
  <name>Task 6: 知识库验证 (REGR-06)</name>
  <files>springboot/src/main/java</files>
  <action>
验证知识库功能:

1. 启动应用，登录获取 token
2. GET /api/knowledge/articles 列表文章
3. POST /api/knowledge/articles 创建文章
4. GET /api/knowledge/articles/{id} 查看文章
5. PUT /api/knowledge/articles/{id} 更新文章
6. DELETE /api/knowledge/articles/{id} 删除文章
7. 停止应用
</action>
  <read_first>
springboot/src/main/java/cn/coderstory/springboot/controller/KnowledgeController.java
</read_first>
  <acceptance_criteria>
1. 知识库 API 返回 200
2. 文章 CRUD 操作正常
</acceptance_criteria>
  <done>
知识库验证完成
</done>
</task>

<task type="auto">
  <name>Task 7: 秒杀系统验证 (REGR-07)</name>
  <files>springboot/src/main/java</files>
  <action>
验证秒杀系统功能:

1. 启动应用，登录获取 token
2. GET /api/seckill/products 列表秒杀商品
3. POST /api/seckill/orders 创建秒杀订单
4. GET /api/seckill/orders/{id} 查看订单状态
5. 验证 Redis 扣减库存正常
6. 停止应用
</action>
  <read_first>
springboot/src/main/java/cn/coderstory/springboot/controller/SeckillController.java
</read_first>
  <acceptance_criteria>
1. 秒杀商品列表 API 返回 200
2. 秒杀下单成功
3. Redis 库存扣减正常
</acceptance_criteria>
  <done>
秒杀系统验证完成
</done>
</task>

</tasks>

<verification>
## Phase 16 Plan 03 Verification

1. REGR-01: 用户登录/登出正常
2. REGR-02: 用户管理 CRUD 正常
3. REGR-03: 角色管理正常
4. REGR-04: 菜单管理正常
5. REGR-05: 审计日志正常
6. REGR-06: 知识库正常
7. REGR-07: 秒杀系统正常
</verification>

<success_criteria>
1. 所有功能模块 API 测试通过
2. 秒杀系统 Redis 扣减正常
</success_criteria>
