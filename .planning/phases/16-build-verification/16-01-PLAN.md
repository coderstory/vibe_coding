---
phase: 16-build-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03

must_haves:
  truths:
    - "gradlew bootRun 成功启动应用"
    - "gradlew build 成功打包"
    - "gradlew test 所有测试通过"
  artifacts:
    - path: "springboot/build.gradle.kts"
      provides: "Gradle 构建配置"
    - path: "springboot/src/test"
      provides: "测试代码"

objectives:
  - 验证 Gradle 构建成功
  - 验证 bootRun 启动成功
  - 验证 test 通过
---

<objective>
验证 Spring Boot 4.1 + Gradle 项目构建成功。执行 gradlew build 和 gradlew bootRun，验证所有测试通过。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: gradlew build (跳过测试)</name>
  <files>springboot/build.gradle.kts</files>
  <action>
在 springboot 目录执行 Gradle 构建:

1. 执行: cd springboot && ./gradlew.bat build -x test
2. 验证构建成功 (BUILDUP SUCCESSFUL)
3. 验证 build/libs 目录生成了 jar 文件

注意: BUILD-01 要求 bootRun 成功启动，BUILD-02 要求 build 成功打包
</action>
  <read_first>
springboot/build.gradle.kts
</read_first>
  <acceptance_criteria>
1. ./gradlew.bat build -x test 返回 BUILD SUCCESSFUL
2. ls springboot/build/libs/*.jar 存在
</acceptance_criteria>
  <done>
gradlew build 成功，jar 文件已生成
</done>
</task>

<task type="auto">
  <name>Task 2: gradlew bootRun 启动验证</name>
  <files>springboot/build.gradle.kts</files>
  <action>
验证应用启动成功:

1. 在后台启动: cd springboot && ./gradlew.bat bootRun &
2. 等待启动完成 (检查端口 8080 是否监听)
3. 访问 http://localhost:8080 验证响应
4. 停止应用

注意: 使用 timeout 或后台运行方式，避免阻塞
</action>
  <read_first>
springboot/src/main/resources/application.yaml
</read_first>
  <acceptance_criteria>
1. bootRun 启动无异常
2. 端口 8080 开始监听
3. curl http://localhost:8080 返回响应 (可以是 404 但不能是连接拒绝)
</acceptance_criteria>
  <done>
bootRun 启动成功，应用响应正常
</done>
</task>

<task type="auto">
  <name>Task 3: gradlew test 测试验证</name>
  <files>springboot/src/test</files>
  <action>
执行所有测试:

1. 执行: cd springboot && ./gradlew.bat test
2. 等待测试完成
3. 检查测试报告 (build/reports/tests/test/index.html)
4. 验证所有测试通过

注意: 如果测试失败，需要分析是测试问题还是代码问题
</action>
  <read_first>
springboot/src/test
</read_first>
  <acceptance_criteria>
1. ./gradlew.bat test 返回 BUILD SUCCESSFUL
2. 测试报告显示所有测试通过
</acceptance_criteria>
  <done>
所有测试通过
</done>
</task>

</tasks>

<verification>
## Phase 16 Plan 01 Verification

1. BUILD-01: gradlew bootRun 成功
2. BUILD-02: gradlew build 成功
3. BUILD-03: gradlew test 通过
</verification>

<success_criteria>
1. gradlew build -x test 成功
2. bootRun 能启动并响应
3. 所有测试通过
</success_criteria>
