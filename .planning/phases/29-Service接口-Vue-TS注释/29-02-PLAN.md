---
phase: 29
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - app-vue/src/App.vue
  - app-vue/src/components/layout/AppHeader.vue
  - app-vue/src/components/layout/AppMenu.vue
  - app-vue/src/components/layout/AppTabs.vue
  - app-vue/src/components/business/knowledge/ArticleEditor.vue
  - app-vue/src/components/business/knowledge/CategoryTree.vue
  - app-vue/src/views/auth/Login.vue
  - app-vue/src/views/dashboard/DashboardIndex.vue
  - app-vue/src/views/error/NotFound.vue
  - app-vue/src/views/layout/Layout.vue
  - app-vue/src/views/business/BusinessData.vue
  - app-vue/src/views/seckill/SeckillIndex.vue
  - app-vue/src/views/seckill/SeckillDetail.vue
  - app-vue/src/views/seckill/SeckillRecord.vue
  - app-vue/src/views/seckill/MyReservations.vue
  - app-vue/src/views/seckill/SeckillCart.vue
  - app-vue/src/views/seckill/activity/ActivityForm.vue
  - app-vue/src/views/seckill/activity/ActivityList.vue
  - app-vue/src/views/seckill/goods/GoodsForm.vue
  - app-vue/src/views/seckill/goods/GoodsList.vue
  - app-vue/src/views/order/OrderList.vue
  - app-vue/src/views/order/OrderConfirm.vue
  - app-vue/src/views/system/UserManagement.vue
  - app-vue/src/views/system/UserDetail.vue
  - app-vue/src/views/system/RoleManage.vue
  - app-vue/src/views/system/MenuManage.vue
  - app-vue/src/views/audit/AuditLog.vue
  - app-vue/src/views/monitor/MonitorDashboard.vue
  - app-vue/src/views/rocketmq/Dashboard.vue
  - app-vue/src/views/rocketmq/TopicList.vue
  - app-vue/src/views/rocketmq/ConsumerGroupList.vue
  - app-vue/src/views/rocketmq/ConsumerGroupDetail.vue
  - app-vue/src/views/rocketmq/MessageList.vue
  - app-vue/src/views/rocketmq/ResetOffsetDialog.vue
  - app-vue/src/views/rocketmq/BrokerStatusTable.vue
  - app-vue/src/views/rocketmq/TopicBacklogTable.vue
  - app-vue/src/views/rocketmq/OverviewCard.vue
  - app-vue/src/views/rocketmq/QpsChart.vue
autonomous: true
requirements:
  - FRNT-01

must_haves:
  truths:
    - "每个 Vue 组件包含一个单独的 `<script lang=\"ts\">` 块（非 setup），内含组件职责的 JSDoc 注释"
    - "每个组件的 defineProps/defineEmits 包含行内 JSDoc，说明每个 prop 和 emit 的用途和类型"
    - "简单组件（仅 1 个根标签的包装组件）可通过组件名自解释豁免，但 `<script lang=\"ts\">` 块仍应存在"
  artifacts:
    - path: "所有 38 个 Vue 文件"
      provides: "组件职责 JSDoc 块 + Props/Emits 行内 JSDoc"
  key_links:
    - from: "<script lang=\"ts\"> 块"
      to: "组件职责说明"
      via: "JSDoc 块注释"
    - from: "defineProps"
      to: "行内 JSDoc"
      via: "行内 /** */ 注释"
---

<objective>
为所有 Vue 组件添加符合 Phase 27 TSDoc 规范的组件职责注释和 Props/Emits JSDoc。

Purpose: 让新开发者通过阅读组件头部的 JSDoc 快速了解每个组件的功能职责和 Props/Emits 含义，无需逐行阅读 template 和 script 逻辑。

Output: 38 个 Vue 文件全部包含 `<script lang="ts">` 组件职责注释块和 defineProps/defineEmits 行内 JSDoc。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md

## 注释规范（Phase 27 TSDoc 模板）

Vue 组件注释使用单独的 `<script lang="ts">` 块（非 setup）放置组件级文档，defineProps/defineEmits 使用行内 JSDoc：

```vue
<script lang="ts">
/**
 * 用户编辑弹窗组件。
 *
 * 支持新增和编辑两种模式，通过 userId prop 区分：
 * - userId 为空 → 新增模式
 * - userId 有值 → 编辑模式（自动回填数据）
 */
</script>

<script lang="ts" setup>
const props = defineProps<{
  /** 用户 ID（编辑模式时必填） */
  userId?: number
  /** 弹窗可见性 */
  visible: boolean
}>()

const emit = defineEmits<{
  /**
   * 保存成功后触发。
   * @param userId 已保存的用户 ID
   */
  saved: [userId: number]
  /** 取消操作时触发 */
  cancel: []
}>()
</script>
```

## 现有注释状态

- AppHeader.vue：已有 `<script lang="ts">` 组件注释块和 handleCommand 的方法注释
- Login.vue：无 `<script lang="ts">` 块，无组件级注释，defineProps/defineEmits 不存在（页面组件）
- 其余组件大部分无 `<script lang="ts">` 组件注释块
</context>

<tasks>

<task type="auto">
  <name>Task 1：为 layout、business、dashboard、error 等基础组件添加组件注释</name>
  <files>
    app-vue/src/App.vue
    app-vue/src/components/layout/AppHeader.vue
    app-vue/src/components/layout/AppMenu.vue
    app-vue/src/components/layout/AppTabs.vue
    app-vue/src/components/business/knowledge/ArticleEditor.vue
    app-vue/src/components/business/knowledge/CategoryTree.vue
    app-vue/src/views/auth/Login.vue
    app-vue/src/views/dashboard/DashboardIndex.vue
    app-vue/src/views/error/NotFound.vue
    app-vue/src/views/layout/Layout.vue
    app-vue/src/views/business/BusinessData.vue
  </files>
  <action>
    读取每个 Vue 文件，按 Phase 27 TSDoc 规范补充注释。

    **统一处理规则：**
    - 如果文件已有 `<script lang="ts">` 块（非 setup）且包含 JSDoc 组件注释，保留
    - 如果文件无 `<script lang="ts">` 块，在 `<script setup>` 前添加一个 `<script lang="ts">` 块，内含 `/** 组件职责说明 */`
    - 如果组件使用了 `defineProps`/`defineEmits`，为每个 prop/emit 添加行内 JSDoc
    - 行内 JSDoc 格式：`/** prop/emit 说明 */` 放在 prop/emit 声明同一行上方

    **各文件具体处理：**

    **App.vue**（根组件）：
    - 添加 `<script lang="ts">` 块：`/** 应用根组件。\n *\n * 包含全局路由出口 `<router-view />`，提供应用级布局容器。\n */`

    **AppHeader.vue**（已有部分注释）：
    - 已有 `<script lang="ts">` 和 handleCommand @returns，保留
    - 确认 defineProps（如有）已添加行内 JSDoc

    **AppMenu.vue、AppTabs.vue**（布局组件）：
    - AppMenu：`/** 侧边栏导航菜单组件。\n *\n * 根据路由配置自动生成菜单树，支持多级展开和路由跳转。\n */`
    - AppTabs：`/** 多标签页导航组件。\n *\n * 管理已打开页面的标签页，支持标签切换、关闭和刷新。\n */`
    - 如有 defineProps/defineEmits，添加行内 JSDoc

    **ArticleEditor.vue、CategoryTree.vue**（知识库业务组件）：
    - ArticleEditor：`/** 知识库文章编辑器组件。\n *\n * 提供富文本编辑、Markdown 编辑和文章元信息管理功能。\n */`
    - CategoryTree：`/** 知识库分类树组件。\n *\n * 以树形结构展示和管理知识库分类，支持拖拽排序。\n */`
    - defineProps/defineEmits 添加行内 JSDoc

    **Login.vue**（登录页面）：
    - 添加 `<script lang="ts">`：`/** 登录页面组件。\n *\n * 提供用户名/密码登录表单，包含粒子动画背景和安全白名单跳转。\n */`
    - 无 defineProps/defineEmits（页面组件），跳过行内 JSDoc

    **DashboardIndex.vue、NotFound.vue、Layout.vue、BusinessData.vue**（页面组件）：
    - DashboardIndex：`/** 仪表盘首页组件。\n *\n * 展示系统概览统计信息和核心业务指标。\n */`
    - NotFound：`/** 404 错误页面组件。\n *\n * 路由不匹配时展示的友好提示页面。\n */`
    - Layout：`/** 主布局容器组件。\n *\n * 包含侧边栏菜单、顶部导航栏、标签页和主内容区。\n */`
    - BusinessData：`/** 业务数据页面组件。\n *\n * 展示和管理核心业务数据。\n */`

    说明（遵循 D-06 决策）：
    - `<script lang="ts">` 块必须是**非 setup 块**，放在文件顶部或 `<script setup>` 之前
    - 描述使用中文陈述句，说明组件做什么而非如何实现
    - Props/Emits 的行内 JSDoc 简短说明用途即可
  </action>
  <verify>
    <automated>
      cd app-vue && for f in \
        src/App.vue \
        src/components/layout/AppMenu.vue \
        src/views/auth/Login.vue; do \
        echo "=== $f ===" && grep -c '<script lang="ts">' "$f" || echo "NO DOC BLOCK"; \
      done
    </automated>
  </verify>
  <done>11 个基础组件文件全部包含 `<script lang="ts">` 组件注释块，Props/Emits 包含行内 JSDoc。</done>
</task>

<task type="auto">
  <name>Task 2：为 seckill 和 order 域的页面组件添加组件注释</name>
  <files>
    app-vue/src/views/seckill/SeckillIndex.vue
    app-vue/src/views/seckill/SeckillDetail.vue
    app-vue/src/views/seckill/SeckillRecord.vue
    app-vue/src/views/seckill/MyReservations.vue
    app-vue/src/views/seckill/SeckillCart.vue
    app-vue/src/views/seckill/activity/ActivityForm.vue
    app-vue/src/views/seckill/activity/ActivityList.vue
    app-vue/src/views/seckill/goods/GoodsForm.vue
    app-vue/src/views/seckill/goods/GoodsList.vue
    app-vue/src/views/order/OrderList.vue
    app-vue/src/views/order/OrderConfirm.vue
  </files>
  <action>
    读取每个 Vue 文件，按 Phase 27 TSDoc 规范补充注释。

    **各文件处理：**

    **SeckillIndex.vue**：`/** 秒杀首页组件。\n *\n * 展示秒杀活动列表和商品信息，提供用户参与秒杀的入口。\n */`

    **SeckillDetail.vue**：`/** 秒杀商品详情组件。\n *\n * 展示单个秒杀商品的详细信息，包含倒计时和抢购按钮。\n */`

    **SeckillRecord.vue**：`/** 用户抢购记录组件。\n *\n * 展示当前用户的秒杀参与记录和状态。\n */`

    **MyReservations.vue**：`/** 我的预约列表组件。\n *\n * 展示用户已预约的秒杀活动，支持取消预约操作。\n */`

    **SeckillCart.vue**：`/** 秒杀购物车组件。\n *\n * 管理用户在秒杀活动中添加的商品，支持数量调整和下单。\n */`

    **ActivityForm.vue**：`/** 秒杀活动表单组件。\n *\n * 支持新增和编辑两种模式，通过 route params 中的 id 区分。\n */`

    **ActivityList.vue**：`/** 秒杀活动列表组件。\n *\n * 展示和管理秒杀活动，支持创建、编辑、发布和结束操作。\n */`

    **GoodsForm.vue**：`/** 秒杀商品表单组件。\n *\n * 支持新增和编辑两种模式，管理秒杀商品的价格、库存等信息。\n */`

    **GoodsList.vue**：`/** 秒杀商品列表组件。\n *\n * 展示和管理秒杀商品信息，支持新增、编辑和库存管理。\n */`

    **OrderList.vue**：`/** 订单列表组件。\n *\n * 展示用户订单信息，支持查看详情和取消订单操作。\n */`

    **OrderConfirm.vue**：`/** 订单确认页面组件。\n *\n * 展示订单确认信息，确认后创建正式订单。\n */`

    说明（遵循 D-06）：
    - 先检查文件是否已有 `<script lang="ts">` 块，有则跳过
    - 如果组件有 defineProps / defineEmits，为每个参数添加行内 JSDoc
    - 无 Prop/Emits 的页面组件（通过 useRouter/useRoute 传参）无需添加行内 JSDoc
    - 所有描述使用中文陈述句
  </action>
  <verify>
    <automated>
      cd app-vue && for f in \
        src/views/seckill/SeckillIndex.vue \
        src/views/seckill/activity/ActivityForm.vue \
        src/views/order/OrderList.vue; do \
        echo "=== $f ===" && grep -c '<script lang="ts">' "$f" || echo "NO DOC BLOCK"; \
      done
    </automated>
  </verify>
  <done>11 个 seckill + order 页面组件全部包含 `<script lang="ts">` 组件注释块。</done>
</task>

<task type="auto">
  <name>Task 3：为 system/audit/monitor/rocketmq 域的页面组件添加组件注释</name>
  <files>
    app-vue/src/views/system/UserManagement.vue
    app-vue/src/views/system/UserDetail.vue
    app-vue/src/views/system/RoleManage.vue
    app-vue/src/views/system/MenuManage.vue
    app-vue/src/views/audit/AuditLog.vue
    app-vue/src/views/monitor/MonitorDashboard.vue
    app-vue/src/views/rocketmq/Dashboard.vue
    app-vue/src/views/rocketmq/TopicList.vue
    app-vue/src/views/rocketmq/ConsumerGroupList.vue
    app-vue/src/views/rocketmq/ConsumerGroupDetail.vue
    app-vue/src/views/rocketmq/MessageList.vue
    app-vue/src/views/rocketmq/ResetOffsetDialog.vue
    app-vue/src/views/rocketmq/BrokerStatusTable.vue
    app-vue/src/views/rocketmq/TopicBacklogTable.vue
    app-vue/src/views/rocketmq/OverviewCard.vue
    app-vue/src/views/rocketmq/QpsChart.vue
  </files>
  <action>
    读取每个 Vue 文件，按 Phase 27 TSDoc 规范补充注释。

    **各文件处理：**

    **UserManagement.vue**：`/** 用户管理页面组件。\n *\n * 展示用户列表，支持筛选、分页、新增、编辑、删除和状态切换操作。\n */`

    **UserDetail.vue**：`/** 用户详情页面组件。\n *\n * 展示单个用户的详细信息，包含基本资料、角色信息和操作日志。\n */`

    **RoleManage.vue**：`/** 角色管理页面组件。\n *\n * 展示角色列表，支持创建、编辑、删除角色和权限分配。\n */`

    **MenuManage.vue**：`/** 菜单管理页面组件。\n *\n * 以树形结构展示和管理系统菜单，支持拖拽排序和权限绑定。\n */`

    **AuditLog.vue**：`/** 审计日志页面组件。\n *\n * 展示系统操作审计日志，支持按用户、操作类型和时间范围筛选。\n */`

    **MonitorDashboard.vue**：`/** 监控大盘页面组件。\n *\n * 展示系统运行状态、性能指标和资源使用情况的实时监控面板。\n */`

    **Dashboard.vue**（RocketMQ）：`/** RocketMQ 监控面板组件。\n *\n * 聚合展示 RocketMQ 集群概览、Broker 状态和消息堆积量等监控数据。\n */`

    **TopicList.vue**：`/** Topic 管理页面组件。\n *\n * 展示 RocketMQ Topic 列表，支持创建、删除和配置更新操作。\n */`

    **ConsumerGroupList.vue**：`/** Consumer Group 列表组件。\n *\n * 展示 RocketMQ 消费组列表和消费进度信息。\n */`

    **ConsumerGroupDetail.vue**：`/** Consumer Group 详情组件。\n *\n * 展示消费组的详细消费进度、堆积量和消费者连接状态。\n */`

    **MessageList.vue**：`/** 消息管理页面组件。\n *\n * 提供消息查询、详情查看、轨迹追踪和消息重发功能。\n */`

    **ResetOffsetDialog.vue**：`/** 重置消费位点弹窗组件。\n *\n * 提供按时间或指定位点重置 Consumer Group 消费进度的功能。\n */`

    **BrokerStatusTable.vue**：`/** Broker 状态表格组件。\n *\n * 展示 RocketMQ Broker 的运行状态、角色和负载信息。\n */`

    **TopicBacklogTable.vue**：`/** Topic 消息堆积量表格组件。\n *\n * 展示各 Topic 的消息堆积数量和消费延迟情况。\n */`

    **OverviewCard.vue**：`/** 集群概览卡片组件。\n *\n * 展示 RocketMQ 集群的关键指标摘要信息。\n */`

    **QpsChart.vue**：`/** QPS 图表组件。\n *\n * 展示 RocketMQ 集群的每秒消息吞吐量趋势图。\n */`

    说明（遵循 D-06）：
    - 对于带 defineEmits 的弹窗组件（如 ResetOffsetDialog.vue），必须为每个 emit 添加行内 JSDoc
    - 对于带 defineProps 的组件（如 OverviewCard.vue、QpsChart.vue、BrokerStatusTable.vue），必须为每个 prop 添加行内 JSDoc 说明用途
    - 子组件中存在的业务逻辑 enum/type 无需额外添加注释（Prop 的行内 JSDoc 已说明）
    - 先检查文件是否已有 `<script lang="ts">` 块，有则跳过避免重复
  </action>
  <verify>
    <automated>
      cd app-vue && for f in \
        src/views/system/UserManagement.vue \
        src/views/audit/AuditLog.vue \
        src/views/rocketmq/TopicList.vue \
        src/views/rocketmq/ResetOffsetDialog.vue; do \
        echo "=== $f ===" && grep -c '<script lang="ts">' "$f" || echo "NO DOC BLOCK"; \
      done
    </automated>
  </verify>
  <done>16 个 system/audit/monitor/rocketmq 页面组件全部包含 `<script lang="ts">` 组件注释块，Props/Emits 组件包含行内 JSDoc。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 注释内容 → 开发者理解 | 错误的组件职责描述可能导致开发者误用组件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | Tampering | `<script lang="ts">` 块 | mitigate | 单独的非 setup script 块不会影响组件运行时行为，但需确认位置正确（在 template 之前） |

本 Plan 仅修改注释内容，不修改运行时行为。`<script lang="ts">` 非 setup 块是 Vue 3 原生支持的语法，不会影响组件编译。
</threat_model>

<verification>
1. 所有 38 个 Vue 文件被修改
2. 每个文件包含 `<script lang="ts">` 块（非 setup）或确认已有此块
3. 有 defineProps 的文件每个 prop 上方有行内 JSDoc
4. 有 defineEmits 的文件每个 emit 上方有行内 JSDoc
5. `npm run lint` 通过（注释变更不影响 lint 结果）
6. `npm run build` 通过（语法保持不变）
</verification>

<success_criteria>
- FRNT-01 完全满足：38 个 Vue 组件全部包含符合 Phase 27 规范的组件注释
- 每个组件文件包含 `<script lang="ts">` 块 + JSDoc 组件职责说明
- 每个 defineProps 的 prop 有行内 JSDoc
- 每个 defineEmits 的 emit 有行内 JSDoc（含 @param 当 emit 带参数时）
- `npm run lint` 和 `npm run build` 不受影响
</success_criteria>

<output>
After completion, create `.planning/phases/29-Service接口-Vue-TS注释/29-02-SUMMARY.md`
</output>
