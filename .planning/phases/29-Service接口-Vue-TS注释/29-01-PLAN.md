---
phase: 29
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - springboot/src/main/java/cn/coderstory/springboot/service/seckill/SeckillService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/seckill/ActivityService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/seckill/GoodsService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/seckill/PreheatService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/seckill/SignService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/seckill/stock/ReconcileService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/seckill/stock/StockService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/order/CartService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/order/OrderService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/order/TimeoutCancelService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/audit/AuditService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/auth/AuthService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/knowledge/KnowledgeService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/menu/MenuService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/monitor/MonitorService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/role/RoleService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/user/UserService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/rocketmq/RocketMQAdminService.java
  - springboot/src/main/java/cn/coderstory/springboot/service/rocketmq/impl/RocketMQSubService.java
  - springboot/src/main/java/cn/coderstory/springboot/config/BlacklistService.java
  - springboot/src/main/java/cn/coderstory/springboot/config/IdempotentService.java
  - springboot/src/main/java/cn/coderstory/springboot/lock/DistributedLockService.java
  - springboot/src/main/java/cn/coderstory/springboot/sse/seckill/SeckillSseService.java
autonomous: true
requirements:
  - BKND-08

must_haves:
  truths:
    - "每个 Service 文件（接口或类）包含类级 Javadoc，说明接口/类的职责和核心功能域"
    - "每个公开方法包含方法级 Javadoc（方法说明 + @param + @return），描述方法做什么而非如何做"
    - "CRUD 方法注释简洁（如 @param userId 用户 ID），复杂业务方法（秒杀下单、Token 刷新、RocketMQ 管理等）略详细但不超过 3 句话"
    - "现有 Javadoc 中的 @author 标签全部删除，@since 统一为 1.7.0"
    - "所有注释遵循 Phase 27 规范：中文陈述句 + 英文技术术语保留，无中英混用，无空骨架注释"
  artifacts:
    - path: "所有 23 个 Service 文件"
      provides: "类级 Javadoc（职责说明 + @since 1.7.0）+ 方法级 Javadoc（说明 + @param + @return）"
  key_links:
    - from: "类级 Javadoc"
      to: "方法级 Javadoc"
      via: "一致的 @since 1.7.0 版本标记"
    - from: "@author 标签"
      to: "删除"
      via: "Phase 27 禁止 @author 规范"
---

<objective>
为所有后端 Service 接口和 Service 类添加符合 Phase 27 规范的类级和方法级 Javadoc。

Purpose: 让新开发者通过阅读类级 Javadoc 快速了解每个 Service 的职责范围，通过方法级 Javadoc 了解参数含义和返回值语义，无需逐行阅读源码。同时清理现有代码中的 @author 标签和错误格式的版本信息。
Output: 23 个 Service 文件全部包含符合 Phase 27 标准的 Javadoc。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md

## 现有注释状态

以下文件已有部分 Javadoc（需规范化）：
- SeckillService.java：有类级和方法级 Javadoc，含 `@author system` @version 标签（删除），中文描述偏多（精简）
- StockService.java：有完整 Javadoc，含 `@author system` @version 标签（删除）
- DistributedLockService.java：有完整 Javadoc，无 @author，保留
- SeckillSseService.java：有部分类级 Javadoc，方法缺完整的 @param/@return
- ActivityService.java：部分方法有 Javadoc，部分方法无，类级缺 Javadoc
- GoodsService.java、CartService.java：部分方法有 Javadoc

其余 17 个文件无任何 Javadoc。

## 注释规范要点（Phase 27）

- 类级格式：`/** 类职责说明。\n * <p>\n * 详细描述（可选）。\n * @since 1.7.0 */`
- 方法级格式：`/** 方法说明。\n * @param name 参数说明\n * @return 返回值说明 */`
- 禁止 `@author`、禁止空骨架注释（有标签无内容）
- CRUD 方法简洁，复杂业务方法可略详细
- 中文陈述句 + 英文技术术语保留，无中英混用
</context>

<tasks>

<task type="auto">
  <name>Task 1：为 seckill 和 order 域的 Service 文件添加类级和方法级 Javadoc</name>
  <files>
    springboot/src/main/java/cn/coderstory/springboot/service/seckill/SeckillService.java
    springboot/src/main/java/cn/coderstory/springboot/service/seckill/ActivityService.java
    springboot/src/main/java/cn/coderstory/springboot/service/seckill/GoodsService.java
    springboot/src/main/java/cn/coderstory/springboot/service/seckill/PreheatService.java
    springboot/src/main/java/cn/coderstory/springboot/service/seckill/SignService.java
    springboot/src/main/java/cn/coderstory/springboot/service/seckill/stock/ReconcileService.java
    springboot/src/main/java/cn/coderstory/springboot/service/seckill/stock/StockService.java
    springboot/src/main/java/cn/coderstory/springboot/service/order/CartService.java
    springboot/src/main/java/cn/coderstory/springboot/service/order/OrderService.java
    springboot/src/main/java/cn/coderstory/springboot/service/order/TimeoutCancelService.java
  </files>
  <action>
    读取每个文件，按 Phase 27 规范添加或规范化 Javadoc。

    **统一处理规则：**
    - 类级 Javadoc：接口声明或类声明上方添加 `/** 职责说明。\n * <p>\n * 详细描述（可选）。\n * @since 1.7.0 */`
    - 方法级 Javadoc：每个公开方法上方添加 `/** 方法说明。\n * @param name 说明\n * @return 说明 */`
    - 已存在 Javadoc 的文件：删除 @author 标签，将 @version/@since 统一为 `@since 1.7.0`，保留原始描述
    - 非公开方法（private/package-private）使用行内注释，不添加完整 Javadoc
    - 描述使用中文陈述句，说明"做什么"而非"如何做"，技术术语保留英文

    **各文件具体处理：**

    **SeckillService.java**（已有 Javadoc，需规范化）：
    - 类级：保留现有详细架构说明，删除 `@author system` 和 `@version 1.1.0`，添加 `@since 1.7.0`
    - 方法级：`seckill()` 方法已有详细 @param/@return，保留
    - 确保无中英混用

    **StockService.java**（已有 Javadoc，需规范化）：
    - 类级：保留现有 Javadoc，删除 `@author system` 和 `@version 1.1.0`，添加 `@since 1.7.0`
    - 方法级：现有 Javadoc 保留

    **ActivityService.java**（部分 Javadoc）：
    - 类级：添加 `/** 秒杀活动管理服务接口。\n * <p>\n * 提供活动的 CRUD、状态管理（开始/结束/发布）和库存查询功能。\n * @since 1.7.0 */`
    - 方法级：`getActivity()`、`getActivityPage()`、`createActivity()`、`updateActivity()`、`deleteActivity()`、`startActivity()`、`endActivity()`、`publishActivity()` 添加 Javadoc
    - `getActivityDetail()` 和 `getActivityStock()` 已有 Javadoc，保留

    **GoodsService.java、PreheatService.java、SignService.java、CartService.java、OrderService.java、TimeoutCancelService.java、ReconcileService.java**（无或少量 Javadoc）：
    - 根据接口中的方法签名推断职责，逐方法添加 Javadoc
    - 类级添加职责说明
    - CRUD 方法使用简洁描述

    说明（遵循 D-04/D-05 决策）：
    - CRUD 方法：`@param id 记录 ID` / `@return 是否操作成功` 等简洁风格
    - 复杂业务方法（如预热、签名生成验证）：说明业务逻辑上下文的 1-2 句描述
    - 有 @throws 签名的方法添加 @throws 标签
  </action>
  <verify>
    <automated>
      cd springboot && for f in \
        src/main/java/cn/coderstory/springboot/service/seckill/SeckillService.java \
        src/main/java/cn/coderstory/springboot/service/seckill/ActivityService.java \
        src/main/java/cn/coderstory/springboot/service/order/CartService.java; do \
        echo "=== $f ===" && grep -c '@since 1.7.0' "$f" && grep -c '@param' "$f"; \
      done
    </automated>
  </verify>
  <done>10 个 seckill + order Service 文件全部包含类级 Javadoc（@since 1.7.0），每个公开方法包含 @param 和 @return。无 @author 标签残留。</done>
</task>

<task type="auto">
  <name>Task 2：为 audit/auth/knowledge/menu/monitor/role/user/rocketmq 域的 Service 文件添加 Javadoc</name>
  <files>
    springboot/src/main/java/cn/coderstory/springboot/service/audit/AuditService.java
    springboot/src/main/java/cn/coderstory/springboot/service/auth/AuthService.java
    springboot/src/main/java/cn/coderstory/springboot/service/knowledge/KnowledgeService.java
    springboot/src/main/java/cn/coderstory/springboot/service/menu/MenuService.java
    springboot/src/main/java/cn/coderstory/springboot/service/monitor/MonitorService.java
    springboot/src/main/java/cn/coderstory/springboot/service/role/RoleService.java
    springboot/src/main/java/cn/coderstory/springboot/service/user/UserService.java
    springboot/src/main/java/cn/coderstory/springboot/service/rocketmq/RocketMQAdminService.java
  </files>
  <action>
    读取每个文件，按 Phase 27 规范添加 Javadoc。

    **统一处理规则：**
    - 类级 Javadoc：职责说明 + `@since 1.7.0`
    - 方法级 Javadoc：每个公开方法有方法说明 + @param（有参数时）+ @return（有返回值时）
    - 非公开方法（private/package-private）使用行内注释

    **各文件具体处理：**

    **AuditService.java**（具体类 — 同时也是该域的唯一 Service 类）：
    - 类级：`/** 审计日志服务。\n * <p>\n * 提供审计日志的异步记录和分页查询功能。\n * @since 1.7.0 */`
    - `log(Long, String, String, String, String, String)`：`@param userId 操作用户 ID`... 6 个 @param
    - `log(Long, String, String, String, String, String, String)`：同上 + description
    - `getAuditLogPage()`：`@param page 分页参数` / `@param operator 操作人用户名（可选）`... 完整 @param / `@return 审计日志分页结果`

    **AuthService.java**（具体类 — 认证服务）：
    - 类级：`/** 认证服务。\n * <p>\n * 提供用户登录、Token 刷新、登出和当前用户查询功能。\n * @since 1.7.0 */`
    - `login()`：方法说明 + @param username/password/ipAddress + @return 包含 token 和用户信息的 Map
    - `refreshToken()`：方法说明 + @param refreshToken + @return 包含新 token 的 Map
    - `logout()`：方法说明 + @param userId/username/ipAddress
    - `getCurrentUser()`：方法说明 + @param userId + @return User 对象

    **KnowledgeService.java**（接口 — 知识库服务）：
    - 类级：`/** 知识库管理服务接口。\n * <p>\n * 提供文章和分类的 CRUD 操作以及标签管理功能。\n * @since 1.7.0 */`
    - 每个方法添加对应的 @param 和 @return

    **MenuService.java、RoleService.java、UserService.java、RocketMQAdminService.java**（接口 — CRUD 服务）：
    - 类级：根据业务域添加职责说明
    - 方法级：简洁风格，如 `@param id 菜单 ID` / `@return 菜单详情 DTO`

    **MonitorService.java**（具体类 — 监控服务）：
    - 类级：`/** 系统监控服务。\n * <p>\n * 提供系统状态和性能数据查询功能。\n * @since 1.7.0 */`
    - 每个方法添加 Javadoc

    说明（遵循 D-04/D-05 决策）：
    - AuthService 中的 login/refreshToken 方法含业务异常抛出逻辑，添加 @throws BusinessException 标签
    - CRUD 方法使用简洁 @param 描述，非 CRUD 方法（如 login、refreshToken）使用 1-2 句说明
  </action>
  <verify>
    <automated>
      cd springboot && for f in \
        src/main/java/cn/coderstory/springboot/service/auth/AuthService.java \
        src/main/java/cn/coderstory/springboot/service/user/UserService.java \
        src/main/java/cn/coderstory/springboot/service/rocketmq/RocketMQAdminService.java; do \
        echo "=== $f ===" && grep -c '@since' "$f" && grep -c '@param' "$f"; \
      done
    </automated>
  </verify>
  <done>8 个 audit/auth/knowledge/menu/monitor/role/user/rocketmq Service 文件全部包含类级和方法级 Javadoc，每个有参方法含 @param。</done>
</task>

<task type="auto">
  <name>Task 3：为 config/lock/sse 目录下的 Service 文件添加 Javadoc</name>
  <files>
    springboot/src/main/java/cn/coderstory/springboot/config/BlacklistService.java
    springboot/src/main/java/cn/coderstory/springboot/config/IdempotentService.java
    springboot/src/main/java/cn/coderstory/springboot/lock/DistributedLockService.java
    springboot/src/main/java/cn/coderstory/springboot/sse/seckill/SeckillSseService.java
    springboot/src/main/java/cn/coderstory/springboot/service/rocketmq/impl/RocketMQSubService.java
  </files>
  <action>
    读取每个文件，按 Phase 27 规范添加 Javadoc。

    **各文件具体处理：**

    **DistributedLockService.java**（接口 — 已有完整 Javadoc）：
    - 类级：现有 Javadoc 保留，无 @author 需删除，仅核实现有 @since 标签。如果 @since 不存在或非 1.7.0，添加 `@since 1.7.0`
    - 方法级：现有 Javadoc 保留

    **SeckillSseService.java**（具体类 — 已有部分类级 Javadoc）：
    - 类级：保留现有 Javadoc，添加 `@since 1.7.0`
    - 方法级：为 `createEmitter()`、`sendEvent()`、`removeEmitter()` 等方法补充完整的 @param 和 @return
    - 保留现有行内注释

    **BlacklistService.java**（具体类）：
    - 类级：`/** IP 黑名单服务。\n * <p>\n * 基于 Redis BloomFilter 和 Set 实现黑名单的高效判断和管理。\n * @since 1.7.0 */`
    - `isBlacklisted()`：方法说明 + @param ip 待检查的 IP 地址 + @return 是否在黑名单中
    - `addToBlacklist()`：方法说明 + @param ip 要加入黑名单的 IP 地址
    - `removeFromBlacklist()`：方法说明 + @param ip 要移出黑名单的 IP 地址

    **IdempotentService.java**（具体类）：
    - 类级：`/** 幂等性检查服务。\n * <p>\n * 基于 Redisson PermitExpirableSemaphore 实现请求幂等性控制。\n * @since 1.7.0 */`
    - `tryAcquire(String, Duration)`：方法说明 + @param idempotentKey 幂等键 + @param expireTime 过期时间 + @return 是否获取成功
    - `tryAcquire(String)`：方法说明 + @param idempotentKey 幂等键 + @return 是否获取成功

    **RocketMQSubService.java**（package-private 标记接口）：
    - 当前已有 marker interface 注释，保留
    - 类级 Javadoc 保持现有内容，确保无 @author，添加 `@since 1.7.0`
    - 此接口无方法，无需方法级 Javadoc

    说明（遵循 D-04/D-05 决策）：
    - 配置/基础设施类 Service 的方法注释关注业务语义，而非实现细节
    - 对于重载方法（IdempotentService 的 tryAcquire），方法说明应区分参数差异
  </action>
  <verify>
    <automated>
      cd springboot && for f in \
        src/main/java/cn/coderstory/springboot/lock/DistributedLockService.java \
        src/main/java/cn/coderstory/springboot/sse/seckill/SeckillSseService.java \
        src/main/java/cn/coderstory/springboot/config/BlacklistService.java; do \
        echo "=== $f ===" && grep -c '@since 1.7.0' "$f" || echo "NO @since FOUND"; \
      done
    </automated>
  </verify>
  <done>5 个 Service 文件全部包含符合 Phase 27 规范的类级和方法级 Javadoc。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Javadoc 内容 → 开发者阅读 | Javadoc 描述可能误导开发者，错误的参数描述导致误用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | @author 标签 | mitigate | 逐文件确认 @author 被删除，执行 `grep -rn '@author'` 扫描 |
| T-29-02 | Information Disclosure | 注释内容 | mitigate | 注释描述仅说明"做什么"，不暴露实现细节和敏感信息 |

本 Plan 仅修改注释内容，不修改运行时行为，无新增安全边界。威胁仅涉及注释内容本身的质量和合规性。
</threat_model>

<verification>
1. 所有 23 个 Service 文件被修改
2. `grep -rn '@author' springboot/src/main/java/cn/coderstory/springboot/` 在 service/config/lock/sse 目录下返回空
3. 每个 Service 文件包含类级 Javadoc（interface 或 class 上方有 `/** */` 块）
4. 每个公开方法包含 `@param`（有参数时）和 `@return`（有返回值时）
5. `./gradlew.bat build -x test` 编译通过（注释变更不影响编译）
</verification>

<success_criteria>
- BKND-08 完全满足：23 个 Service 文件全部包含符合 Phase 27 规范的类级和方法级 Javadoc
- 每个 Service 文件至少有类级 Javadoc（职责说明 + @since 1.7.0）
- 每个公开方法有方法说明 + @param（有参数时）+ @return（有返回值时）+ @throws（有 checked exception 时）
- 已有 Javadoc 的文件完成规范化（删除 @author，统一 @since 版本）
- 无空骨架注释（@param/@return 后均有描述内容）
- 描述使用中文陈述句，无中英混用
- `./gradlew.bat build -x test` 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/29-Service接口-Vue-TS注释/29-01-SUMMARY.md`
</output>
